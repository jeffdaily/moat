# PORTING_GUIDE

Living best practices for porting CUDA projects to ROCm/HIP. The planner reads this before every project. Any agent that learns a generalizable lesson appends it here (see Changelog) in the same run. Project-specific quirks go in `projects/<name>/notes.md` instead.

ROCm is AMD's GPU compute stack; HIP is its CUDA-like C++ runtime and kernel language. Most CUDA C++ has a 1:1 HIP spelling (`cudaMalloc` -> `hipMalloc`), so a port is usually mechanical plus a handful of real semantic differences listed under Fault classes.

## Naming: ROCm vs HIP

These name two different facets and are not interchangeable. The confusion is structural: NVIDIA uses one brand ("CUDA") for the language, the runtime, and the platform, while AMD split that into two names, so no single word maps cleanly.

- **HIP** is the programming model -- the kernel-language dialect (`__global__`, `__device__`, `<<<>>>`) and the `hipXxx` host runtime API. It is the analogue of *CUDA C++ plus the CUDA Runtime API*. HIP is portable: it compiles to AMD (via ROCm) or to NVIDIA (a thin wrapper over CUDA), so "HIP" alone does not imply AMD hardware.
- **ROCm** is the platform/toolkit -- the amdgpu/KFD driver, the ROCr/HSA runtime, the amdclang/LLVM AMDGPU compiler, HIP itself, and the domain libraries. It is the analogue of the *CUDA Toolkit*, and it is what pins execution to an AMD GPU.
- **roc\* vs hip\* libraries**: `roc*` (rocBLAS, rocFFT, rocSOLVER, rocPRIM, rocThrust) are AMD-native libraries with AMD-shaped APIs; `hip*` (hipBLAS, hipFFT, hipSOLVER, hipCUB) are thin portability wrappers exposing a CUDA-library-compatible API that dispatch to `roc*` on AMD (or `cu*` on NVIDIA). hipBLAS-on-AMD is rocBLAS underneath.

Mapping: CUDA C++ and the CUDA runtime -> **HIP**; nvcc, the CUDA Toolkit, and cuBLAS/cuFFT/cuDNN/CUB -> **ROCm** (hipBLAS+rocBLAS, hipFFT+rocFFT, MIOpen, hipCUB+rocPRIM).

How to say it in MOAT prose, commits, and PRs:
- The **code transformation** is a port **to HIP** (hipify, the `cuda_to_hip.h` shim, the `cudaXxx -> hipXxx` runtime layer).
- The **target/platform/build flag/libraries** are **ROCm** ("validated on gfx90a, ROCm 7.2.1", the `USE_HIP`/`USE_ROCM` option, "swap cuFFT for hipFFT").
- A pure language+runtime port is most precisely "a HIP port targeting ROCm." Do not call the platform "HIP" or the kernel dialect "ROCm".
- Commit prefix stays `[ROCm]` as the umbrella tag (it signals "adds AMD support" to upstream maintainers and matches AMD/PyTorch usage); name a specific `roc*`/`hip*` library only when the port actually substitutes it.

PyTorch is the canonical conflation to NOT imitate: its build macro is `USE_ROCM`, `torch.version.hip` exists, yet the runtime device is still literally `"cuda"` -- it brands the platform ROCm while keeping CUDA's names and uses "HIP"/"ROCm" interchangeably in build code. Be precise instead.

Upstream-visible text (commit messages, code comments, PR bodies) carries no MOAT-internal vocabulary -- no "lead"/"follower", "Strategy A/B", "head_sha", "curated commit", "validated_sha", "revalidate". Keep the technical rationale, drop the in-house labels; this text is read by external maintainers.

## Before porting: assess existing AMD support

A "port" is not always a fresh CUDA-to-HIP conversion. Check first:
- Mature ROCm/HIP support already upstream -> skip it (disposition already-supported); no work to do.
- AMD supported only via OpenCL, Vulkan, or SYCL, with no HIP path -> a ROCm/HIP port of the CUDA code is still valuable.
- An abandoned or incomplete ROCm/HIP port (stale branch, unmerged PR, old fork) -> finish it rather than starting over.
- A ROCm/HIP port exists but is below the best practices here -> improve it (minimal footprint, warp_size abstraction, and so on).
The planner makes this call per project and records it in plan.md. "Already supports AMD via OpenCL/Vulkan" alone does not mean skip.

Performance-critical kernels (attention, GEMM, quantization) are often tuned to NVIDIA-specific features (CUTLASS/CuTe, Hopper sm90 wgmma/MMA, warp specialization). A straight CUDA-to-HIP translation will compile and run but can leave large performance on the table versus an AMD-native implementation (rocWMMA, Composable Kernel, MFMA intrinsics). For these the planner decides between a mechanical port (correctness first) and an AMD-native rewrite of the hot kernels, and says which in plan.md.

## Build classification

Decide which strategy applies before touching code.

- Pure CMake project (not tied to pytorch): a standalone CMake build with `.cu` sources and CUDA libraries. Use Strategy A.
- pytorch extension: builds via setup.py using `torch.utils.cpp_extension` (`CUDAExtension` / `BuildExtension`), or a CMake build that finds Torch and uses its extension machinery. Use Strategy B.

How to tell: look for `find_package(Torch)`, `torch.utils.cpp_extension`, `CUDAExtension`, or a torch dependency in setup.py / pyproject.toml. If present, it is a pytorch extension. Otherwise treat it as a pure CMake (or Makefile) project.

## Strategy A: pure CMake, colmap model (preferred, minimal footprint)

Goal: only `.cu`/`.hip` translation units see the HIP toolchain; host C++ is untouched; the diff stays small.

1. Add one CUDA-to-HIP compat header (e.g. `src/.../cuda_to_hip.h`). On ROCm it aliases the CUDA spellings the project uses to their HIP equivalents and includes the HIP runtime; on NVIDIA it is a no-op include of the CUDA runtime. Everywhere else keep the plain CUDA spelling (`cudaXxx`, `curand*`, `cublas*`). This header is the only file that knows about HIP.

       #pragma once
       #if defined(USE_HIP) || defined(__HIP_PLATFORM_AMD__)
       #include <hip/hip_runtime.h>
       #define cudaMalloc        hipMalloc
       #define cudaFree          hipFree
       #define cudaMemcpy        hipMemcpy
       #define cudaStream_t      hipStream_t
       #define cudaError_t       hipError_t
       #define cudaSuccess       hipSuccess
       // ... only the symbols the project actually uses
       #else
       #include <cuda_runtime.h>
       #endif

   Use hipify's mapping tables as the authoritative cuda->hip name source when adding aliases: `torch/utils/hipify/cuda_to_hip_mappings.py` in a pytorch checkout lists 3000+ symbol mappings.

2. In CMake, gate the language on a HIP option instead of renaming files:

       option(USE_HIP "Build with HIP for AMD GPUs" OFF)
       if(USE_HIP)
         enable_language(HIP)
         # Never hardcode the lead arch as a literal here: a literal "gfx90a"
         # overrides -DCMAKE_HIP_ARCHITECTURES, so every follower (gfx1100,
         # gfx1151) is forced to edit this file to build -- churning the curated
         # commit's head_sha and forcing already-passed platforms to revalidate.
         if(NOT DEFINED CMAKE_HIP_ARCHITECTURES OR CMAKE_HIP_ARCHITECTURES STREQUAL "")
           set(CMAKE_HIP_ARCHITECTURES "gfx90a")  # default the lead arch only when unset
         endif()
         set_source_files_properties(${CUDA_SOURCES} PROPERTIES LANGUAGE HIP)
         set_target_properties(<tgt> PROPERTIES HIP_ARCHITECTURES "${CMAKE_HIP_ARCHITECTURES}")
       else()
         enable_language(CUDA)
       endif()

   Marking the existing `.cu` files `LANGUAGE HIP` keeps the diff minimal and the NVIDIA build intact. Configure with `-DUSE_HIP=ON -DCMAKE_HIP_ARCHITECTURES=gfx90a` (add `-DCMAKE_HIP_COMPILER=/opt/rocm/llvm/bin/clang++` if CMake does not find it). Because the target reads `${CMAKE_HIP_ARCHITECTURES}`, the same lead-port commit builds for any AMD target with only `-DCMAKE_HIP_ARCHITECTURES=<arch>` (gfx90a, gfx1100, gfx1151) and no source change, so a follower validation needs no commit. Also pass every target arch you can test at planning time so the lead bringup is right the first time.

3. Guard genuinely divergent code with `#if defined(USE_HIP)`; keep such guards rare. Dispatch sites that accept either backend use `#if defined(USE_CUDA) || defined(USE_HIP)`.

This is how colmap was ported (PR 4420 plus follow-ups): one compat header, `.cu` marked `LANGUAGE HIP`, a few guarded fixes. PyTorch validated that this isolates HIP: on an MI250 build only the HIP translation units receive `-x hip`; host files are untouched.

### Large libraries: `--offload-compress` to fit the host image under the 2 GiB x86-64 relocation reach

A heavily-templated library (cudf: rocThrust/rocPRIM `DeviceReduce`/`DeviceScan`/`DeviceRadixSort` instantiated over a full type x op matrix) embeds an enormous device code object PER translation unit. The HIP offload bundle is stored UNCOMPRESSED by default, so a single reduce `.o` can reach ~200 MB (~159 MB of which is the `.hip_fatbin`). Linking many such TUs into one shared object can push the HOST image past the +/-2 GiB reach of x86-64 32-bit PC-relative relocations -- the link fails with `R_X86_64_PC32` overflow first, then (if you reach for `-mcmodel=large`, which only fixes data refs) `R_X86_64_TLSGD`/`TLSLD`/`__tls_get_addr` and `.eh_frame` "PC offset too large", neither of which the large code model covers. nvcc fatbins are compact, so the CUDA build never hits this; it is a ROCm-specific build-scale wall, not a HIP-correctness one.

Fix: add `--offload-compress` to the HIP compile options (`target_compile_options(<tgt> PRIVATE $<$<COMPILE_LANGUAGE:HIP>:--offload-compress>)`). A documented ROCm clang HIP flag (7.2.1+: "Compress offload device binaries (HIP only)") that stores the embedded device bundle in the compressed CCOB format; the HIP runtime decompresses it on load. Measured on a cudf rocPRIM reduce TU @ gfx90a: object 207.8 MB -> 52.1 MB (3.99x), `.hip_fatbin` 158.6 MB -> 2.89 MB (54.9x) -- the device ISA matrix is hugely redundant so it compresses ~55x. This let cudf's full reductions + aggregation + groupby + join surface link into ONE ~540 MB libcudf.so with no multi-.so split and no `-mcmodel=large`. For a non-RDC build (no `-fgpu-rdc` device-link step) the per-TU compile is the only place the bundle is formed, so there is no separate device-link to also flag. Prefer this BEFORE splitting a library or capping the template matrix. Also build a single target arch (no multi-arch fatbin) and add `-ffunction-sections -fdata-sections` + `--gc-sections` to prune unreachable host instantiations. (cudf.)

## Strategy B: pytorch extension

Torch hipifies extension sources at build time. Do not add a compat header and do not hand-rename symbols.

- Building a `CUDAExtension` on a ROCm torch automatically runs `torch.utils.hipify` on the extension's `.cu`/`.cuh` sources and links the HIP runtime (`amdhip64`, `c10_hip`, `torch_hip`). See `torch/utils/cpp_extension.py`.
- Keep sources in CUDA spelling; hipify translates them. Fix only what hipify cannot (warp size, see below) in source, guarded by `USE_ROCM`.
- Build against a ROCm torch. If the tree was hipified once and is stale after edits, re-run the project's hipify step before rebuilding (a known incremental-build gotcha: edits to `.cu` can recompile the stale hipified mirror unless you re-hipify first).
- For projects shipping their own `.cu` plus a setup.py, the change is often just: build against a ROCm torch and fix the fault classes below.

## Fault classes (AMD is strict where CUDA is lenient)

These are the real semantic differences. Most porting bugs are here, not in symbol names.

- Warp size. NVIDIA warp = 32 lanes always. AMD wavefront = 64 on CDNA (gfx90a, gfx94x) and 32 on RDNA (gfx10xx, gfx11xx, including gfx1100 and gfx1151). Never hardcode 32.
  - Host code (kernel launch, host-side shared-mem sizing): query at runtime, `hipGetDeviceProperties(&prop, dev); prop.warpSize`. PyTorch exposes `at::cuda::warp_size()`.
  - Device code: use a per-arch constant. PyTorch's `C10_WARP_SIZE` is 32 on CUDA, and on HIP is 64 for `__GFX9__` else 32 (`torch/headeronly/macros/Macros.h`). Replicate it as:

        #if defined(__HIP_PLATFORM_AMD__)
        #if defined(__GFX9__)
        static constexpr int kWarpSize = 64;   // CDNA: gfx90a, gfx94x
        #else
        static constexpr int kWarpSize = 32;   // RDNA: gfx10xx, gfx11xx
        #endif
        #else
        static constexpr int kWarpSize = 32;   // CUDA
        #endif

    `__GFX9__` is defined only during device compilation; for host-only code use the runtime query.
  - Static shared-memory arrays sized by warp count must use a compile-time upper bound (PyTorch's `C10_WARP_SIZE_UPPER_BOUND`) or size to 64, since the runtime value is not a constant expression.
  - Lane masks: `__shfl*`, `__ballot`, `__activemask`. On a 64-wide wavefront a `uint32` mask is wrong; use 64-bit masks (`unsigned long long`) where the API takes one.
  - Multi-arch is the test, not single-arch. The canonical correctness check for any warp-size work is to BUILD `-DCMAKE_HIP_ARCHITECTURES="gfx90a;gfx1100"` (one fat binary) and confirm BOTH code objects are emitted (`llvm-objdump --offloading lib.so` shows gfx90a AND gfx1100) plus a correct single-arch run. A port that bakes a single warp width into a build variable (a CMake-injected `-DWARP_SIZE=64`, or any host+device-shared compile constant) silently FAILS this: it forces single-arch and mis-sizes host buffers in a fat binary. The device width must come from the `__GFX8__||__GFX9__`->64 / `__GFX10__-12__`->32 guards (constexpr, device-pass only); the host width from the runtime `hipGetDeviceProperties().warpSize` / `hipDeviceAttributeWarpSize` query -- NEVER a single shared constant. There is no `__AMDGCN_WAVEFRONT_SIZE__` macro in ROCm 7.2.x; the `__GFX*__` guards are the supported per-arch compile-time selector. Width-32 LOGICAL-warp ops (`__shfl*(...,32)`, `cub::WarpReduce<,32>`, `cg::tiled_partition<32>`) are arch-agnostic and fine -- they operate within a 32-lane subgroup regardless of the physical wavefront.
  - Do NOT hardcode wave64 lane GEOMETRY (the inverse failure of hardcoding 32). A fix that assumes a 64-lane wavefront -- packing two logical 32-thread rows into one wavefront via `wflane = threadIdx.x + ((threadIdx.y&1)<<5)`, a `1ull<<wflane` prefix mask, a width-64 `__shfl(...,0,64)`, a `__popcll` over a 64-bit ballot, or a `wflane==0` leader test -- COMPILES on wave32 (gfx1100/gfx1151) but silently miscomputes: on wave32 each 32-thread row is its OWN wavefront, so the odd-row `wflane` lands at 32..63, the leader test never fires (lost atomics), the width-64 shuffle is out of range, and the prefix mask reads phantom bits. Make wave-collective geometry `warpSize`-generic (lane = `(threadIdx.y*blockDim.x + threadIdx.x) % warpSize`, ballot/popc/shuffle widths = `warpSize`, leader = lane 0 of the actual wavefront) or give it an explicit `__GFX9__` wave64 branch AND a wave32 `#else`. A wave64-only fix that passes CDNA is not done until it is correct on RDNA. (popsift s_extrema extrema-counter.)
  - VALIDATE wave-size correctness on BOTH wave widths, and for a port with no CPU/host reference use a CROSS-ARCH CONSISTENCY gate, not just "sane output". A reference-less follower check of "deterministic + non-zero + plausible" passes a wrong wave32 result (popsift's gfx1100 reported deterministic non-zero features from a miscounted extrema path -- a false pass). Where the algorithm is deterministic, the follower (gfx1100/gfx1151) must diff its output against the lead's (gfx90a) output for the same input; a divergence is the bug the loose gate misses.
  - Special case -- warp-size-dependent SERIALIZED DATA FORMAT. If the warp width determines an on-disk/in-memory format (not just launch geometry), the format must be pinned arch-independent or carry the producing device's warp size at runtime; it cannot follow the per-arch device constant. Example: dietgpu's rANS archive serializes one entropy-coder state per lane (`ANSWarpState::warpState[kWarpSize]`), so its header geometry was warp-width-coupled. Fix: pin the serialized layout to a fixed max width (`kMaxWarpSize = 64`, `warpState[64]`) so `sizeof()` and every header offset are arch-independent constants the host and device agree on; a wave32 device writes/reads only the first 32 slots (its device kWarpSize stride, unchanged) and leaves 32-63 unused. This keeps the wave64 archive byte-identical to its prior format and makes the host sizing path independent of warp width entirely. (dietgpu.)

- Rule-of-five on resource handles. CUDA tolerates a default-constructed or double-destroyed texture/stream/event handle; AMD faults. Give RAII wrappers explicit default init (`handle = 0`), move-only semantics, and a guarded destructor. (colmap CuTexObj bug.)

- Out-of-bounds reads. CUDA often tolerates a read one element past an allocation; AMD faults. Kernels that read index +/- 1 or +/- width at edges (stencils, neighbor gathers) must clamp indices. (colmap ComputeDOG bug.)

- Texture pitch alignment. AMD requires 256-byte row pitch for pitched 2D texture binds; widths that work on CUDA can fail. If a kernel only point-samples, a linear (`tex1Dfetch`-style) bind avoids pitch entirely. (colmap BindTexture2D bug.)

- Layered cudaArray collapse (CONFIRMED clr bug, gfx90a/CDNA2, ROCm 7.2.1). A `hipArrayLayered` array written one layer at a time via `surf2DLayeredwrite` reads back the LAST-written layer for every layer index on a later launch -- through `tex2DLayered`, `surf2DLayeredread`, and host `hipMemcpy3D` alike; the layer dimension collapses. Filed as ROCm/clr#275 (popsift was the motivating case: the collapse zeroed the DoG -> 0 features). Workaround: allocate the pyramid arrays as NON-layered 3D (`surf3Dwrite`/`surf3Dread`/`tex3D` with the layer as a real z coordinate), which is fully coherent across launches. AMD's candidate fix (ROCm/rocm-systems#6683) corrects only the `surf2DLayered` builtins; `tex2DLayered` uses a different builtin and may collapse independently -- so re-verify per ROCm version, and treat the 3D-array workaround as needed until proven fixed on your stack. NOT yet confirmed on RDNA (gfx1100/gfx1151) -- the follower porter must re-run the repro to establish arch scope.

- Hardware linear-filter texture over an element-read float array. Observed on gfx90a (ROCm 7.2.1): creating a texture with `filterMode=cudaFilterModeLinear` + `readMode=cudaReadModeElementType` over a float array is rejected at creation. Workaround: create the texture point-filtered and do the bilinear interpolation in software (sample the 4 neighbors, lerp, with CUDA's -0.5 texel-center convention). EMPIRICAL, no upstream issue filed yet, and NOT confirmed beyond gfx90a -- scope the claim to what is tested and have the follower porter re-verify on RDNA. (popsift readTex.)

- Texture hardware linear filtering. CUDA accepts a `cudaFilterModeLinear` + `cudaReadModeElementType` texture over a float array; HIP/ROCm rejects it at creation (`hipCreateTextureObject` -> "operation not supported") -- AMD hardware does not support linear filtering on element-read float textures. Fix: on HIP create the texture `cudaFilterModePoint` and do manual bilinear interpolation in software (point-sample the 4 neighbors and lerp), matching CUDA's unnormalized -0.5 texel-center convention: a coordinate c samples texel `floor(c-0.5)` and `floor(c-0.5)+1` with weight `(c-0.5)-floor(c-0.5)`. Put the interpolation behind the project's texture-fetch helper so callers are unchanged. (popsift sift_octave linear textures.)

- Layered-image arrays are broken across kernel launches (use a non-layered 3D array instead). HIP runtime bug, confirmed on gfx90a/CDNA2 (ROCm 7.2.1): for a `cudaArrayLayered | cudaArraySurfaceLoadStore` float array written one layer at a time via `surf2DLayeredwrite`, a read in a LATER kernel launch returns ONE single (last-written) layer's data for EVERY layer index -- `tex2DLayered`, `surf2DLayeredread`, AND even host `hipMemcpy3D` all collapse the layer dimension. `hipDeviceSynchronize` between writes and recreating the texture/surface do NOT help. (An earlier "stale texture but surface is fresh" reading was an artifact of a repro that only ever touched ONE layer, so the last-written layer happened to equal the layer read; reading surf2DLayeredread does NOT actually fix it.) A NON-layered 3D array -- `hipMalloc3DArray` WITHOUT `cudaArrayLayered`, accessed with `surf3Dwrite`/`surf3Dread`/`tex3D` and the level as a real z coordinate -- IS fully per-slice coherent across launches (so is a tall 2D array, W x H*L, slice k at y+k*H). FIX: on HIP drop `cudaArrayLayered` and make the array 3D; map every `surf2DLayeredwrite(v,s,x,y,layer)` -> `surf3Dwrite(v,s,x,y,layer)` (1:1; do it once in the compat header's write wrapper so call sites are untouched) and route every layered read through the project's fetch helper to `surf3Dread`/`tex3D`. With a 3D array `tex3D` is also coherent, so textures can be kept. CUDA keeps the real layered array, byte-for-byte. Standalone repros: agent_space/popsift_run/{multilayer_check*,array3d_check,tall2d_check}.cpp. (popsift: the whole Gaussian pyramid + DoG were layered arrays; without this the DoG was all-zero / 0 features.)

- Library swaps. cuBLAS -> hipBLAS, cuFFT -> hipFFT, cuRAND -> hipRAND, cuSPARSE -> hipSPARSE, cuDNN -> MIOpen, Thrust/CUB -> rocThrust/hipCUB. APIs are mostly 1:1; watch handle types and a few signature differences (for example hipBLAS v2 enums).

- Texture-pitch alignment attribute is 256 on gfx90a vs 32 on NVIDIA -- strided-buffer/padding-compare divergence (distinct from the texture-bind pitch class above). `cudaDevAttrTexturePitchAlignment` (hipDeviceAttributeTexturePitchAlignment) reports 256 on gfx90a but 32 on the supported NVIDIA setups. Libraries that derive a tensor/image ROW-PITCH alignment from this attribute (e.g. CV-CUDA NVCV) pad rows more on AMD: a tight 640-byte uchar row becomes 768. Many gtests fill the valid WxH region, run the op, then compare the WHOLE strided buffer (including row-stride padding the op never writes) against a zeroed CPU reference -- so they bake in the CUDA tight pitch and mismatch the wider AMD stride. If no tensor is actually bound to a HW texture object, the 256B pitch is unnecessary: clamp the attribute query to 32 in a compat shim (`#define cudaDeviceGetAttribute` -> wrapper that sets `*value=32` for the pitch-alignment attribute) so AMD pitches match CUDA. (CV-CUDA: fixed Erase/SIFT/Gaussian/FindHomography/Normalize/TensorBatchWrap and many Interpolation cases.)

- `__fsqrt_rn` is not always correctly rounded on gfx90a (1-ULP high). CUDA's `sqrt.rn.f32` is IEEE correctly-rounded; gfx90a `__fsqrt_rn` can be 1 ULP off (e.g. `sqrtf(93606.0f)` -> 0x4398f9b9 on gfx90a vs the correctly-rounded 0x4398f9ba from host `std::sqrt` and `(float)__dsqrt_rn`). Bit-exact gtests then pass on NVIDIA and fail on gfx90a. Fix: route the f32 device sqrt through the correctly-rounded f64 `__dsqrt_rn` and cast back (`(float)__dsqrt_rn((double)x)`); CDNA has fast f64 sqrt so the cost is negligible. (CV-CUDA: OpNormalize and the L2 PairwiseMatcher distance exactness.)

- clang(HIP) defaults to `-ffp-contract=fast`; nvcc contracts expression-only. clang for HIP forms FMAs ACROSS separate statements by default (`fast`), whereas nvcc with default `--fmad=true` only contracts within a single expression. The extra contraction drifts HIP float results ~1 ULP from the CUDA build and CPU gold (e.g. a multi-statement bicubic weight chain), breaking bit-exact compares. Fix: pin `-ffp-contract=on` (expression-only) in the HIP compile flags (`CMAKE_HIP_FLAGS`) to match CUDA/host FMA formation. (CV-CUDA: OpWarpPerspective cubic + the cubic-math half of the Interpolation tests.)

- HIP device `cuda::min`/`cuda::max` NaN-selection can differ from host `std::min`/`std::max`. Tests that fill float buffers with random bytes (producing NaN/inf) and compute the same min/max on host gold and device require identical NaN/signed-zero selection. Host `std::min(a,b)` is `b<a?b:a` and `std::max(a,b)` is `a<b?b:a`; a device implementation written as `a<b?a:b` / `a>b?a:b` picks the OPPOSITE operand on a NaN compare, so device != host bit-for-bit. Fix: spell the device ternaries to exactly match the host `std::min`/`std::max` forms. (CV-CUDA: OpMorphology/OpMorphologyVarShape CLOSE on RGBAf32.)

- cub/hipCUB block-collective `TempStorage` reuse races on a 64-thread (wave64) block without an explicit `__syncthreads()`. On a wave64 arch a block of 64 threads is a single wavefront, so a block-collective (BlockReduce, BlockRadixSort) lowers to a single-wavefront op with no syncing epilogue. Reusing the same `TempStorage` union across two back-to-back collective calls then races (the second call's writes overlap the first's reads). CUDA's 32-wide warp split the same block into 2 warps, masking it. Fix: add an explicit `__syncthreads()` after each collective and between reused-TempStorage calls. (CV-CUDA: OpPairwiseMatcher crossCheck BlockReduce/BlockRadixSort.)

- `__smid()` (HIP, the hardware CU-slot id) can EXCEED `multiProcessorCount` on AMD, unlike NVIDIA where smid < SM count. NVIDIA's `%smid` is dense in `[0, multiProcessorCount)`, so host code commonly sizes a per-SM scratch pool to `multiProcessorCount` and a kernel indexes it as `pool + smid()*POOL_SIZE` to give each resident block a private, contention-free slot. On AMD, `__smid()` returns a SPARSE `(SE,CU)` (and on gfx94x `(XCC,SE,CU)`) packing: gfx9 computes `(se_id << 4) | cu_id` with `cu_id` in `[0,15]` and `se_id` up to 7, so the value range is `[0,127]` even though only ~104 CUs are active (measured: max `__smid()` = 125 on a gfx90a GCD with `multiProcessorCount` = 104). A pool sized to `multiProcessorCount` is then written PAST its end by any block landing on a high-id CU -- a SILENT out-of-bounds device-heap write (no fault, corruption surfaces later when the clobbered allocation is read), and a green test run does NOT prove safety. Fix: under ROCm size every `__smid()`-indexed pool to the smid UPPER BOUND, never `multiProcessorCount`. Prefer a one-time RUNTIME PROBE -- launch a saturating max-occupancy kernel that `atomicMax`es `get_smid()`, take `max+1`, cache it -- then round up (e.g. to the next power of two) so the slot count provably covers the full encodable id range; this is robust across archs without hardcoding bit layouts. Do NOT `% workers` the index into `[0,workers)`: that reintroduces the per-CU contention/races the private-slot pool exists to prevent. Apply at EVERY allocation site that pairs with a `__smid()`-indexed kernel (audit the kernels for `smid()*STRIDE`); leave pools indexed by `blockIdx.x` with a `gridDim == multiProcessorCount` launch alone (those are self-consistent). Keep the CUDA host path at `multiProcessorCount` (NVIDIA invariant holds). Prove the fix with a sentinel guard region placed immediately after the pool: it stays intact after exercising the pool-write branch with the resized pool, and is clobbered if you force the old `multiProcessorCount` sizing. (gpu4pyscf: get_smid()-indexed int3c2e fill pool, ~4MB silent OOB on the d-shell HF/df integral path; sized via `__config__.pool_slots` + `probe_smid_range()`.)

- `cudaMemcpy*Async` from a soon-freed pageable host buffer: CUDA stages synchronously, HIP does not. CUDA's `cudaMemcpyAsync`/`cudaMemcpy2DAsync` from PAGEABLE host memory is effectively synchronous (the driver stages it before returning), so a test that issues an async H2D copy from a local `std::vector` and lets the vector go out of scope at the end of the loop iteration still works -- the copy completed before the free. On ROCm `hipMemcpy*Async` from pageable memory can be genuinely asynchronous, so the freed (and reused) host buffer is read after the copy is enqueued, writing garbage into the destination. The corruption surfaces only in bytes the kernel does NOT overwrite (e.g. row-stride padding compared against a zeroed reference), and the failing set varies run-to-run. This is a test-fixture use-after-free that is latent UB on CUDA and exposed on HIP -- not a production-operator or allocator defect; confirm by keeping the source buffer alive (or syncing after the copy) and watching the failures vanish. (CV-CUDA: InterpolationVarShapeWrap correct_shift, conclusively a test artifact; production zero-init covers the full padded buffer and the operator writes only valid pixels.)

- A functor/operator that returns a reference to a by-value or forwarded parameter (lifetime UB) is tolerated by nvcc/CUB but reads garbage through rocThrust/rocPRIM. A device functor whose `operator()` returns `T&&`/`const T&` of a local-by-value or perfectly-forwarded parameter binds a reference to a temporary that expires at the return. nvcc and CUB happen to keep the value live (registers); rocPRIM block-loads the dangling reference and the kernel reads garbage. Surfaced in cudf: `cast_functor.cuh`'s `cast_fn<T>` had a same-type overload `operator()(T&&) -> T&&` (a copy-elision micro-opt) used as the element transformer of the `thrust::transform_iterator` feeding `hipCUB DeviceReduce::Reduce`. It only mattered when the reduction output type equals the input element type (so that overload, not the by-value templated one, is selected): min/max returned the seeded identity (INT32_MIN/MAX) and a float/double sum returned nan, while an int32->int64 sum was correct. Fix: return BY VALUE (correct on CUDA too -- it is UB everywhere). Localize by computing the same reduction three ways (the library wrapper; a raw-pointer hipCUB call; thrust::reduce over the same iterator + bare operator): if the raw and bare-operator paths agree and only the wrapper is wrong, the bug is in the wrapper's transform/op, not the iterator, operator, or rocPRIM. A standalone microbench often will NOT reproduce it (the dangle manifests only through the library's nested iterator instantiation), so an in-test multi-path probe is the decisive tool. (cudf: reductions/groupby min/max/mean correctness.)

- `char` vs `signed char` vector base type: HIP `char4` members are `char`, CUDA's are `signed char`. HIP defines `using char4 = HIP_vector_type<char, N>` whose members are plain `char`; NVIDIA's `vector_types.h` defines `char4` with `signed char` members. `char` and `signed char` are DISTINCT C++ types even though identically represented on these targets. A type-machinery layer (CV-CUDA MakeType/BaseType) must report `char` as the base type of the canonical `charN` on HIP so a reference accessor (`signed char&` will not bind to a `char` member) compiles. A test asserting `is_same_v<signed char, BaseType<MakeType<signed char,N>>>` then fails on HIP. This is dictated by the upstream HIP vector-types header, not the porting layer: forcing it would require either breaking the reference accessor or introducing a non-canonical `HIP_vector_type<signed char,N>` family into shared code (a per-platform hack). Numeric behavior is identical. Document as a true upstream-header type-identity deferral, not a bug. (CV-CUDA: TypeTraitsMakeTypeVectorTest/3.)

## Validation policy

- A port is validated only when the project's real test suite builds and passes on a real AMD GPU for the target arch, with no regression in non-GPU tests.
- A follower validation (gfx1100/gfx1151) must not introduce a NEW fork commit for anything non-essential (CI YAML, formatting, comments). Advancing head_sha forces every already-passed platform back to `revalidate` for a zero-GPU-effect change -- pure churn. If a follower needs no code change, leave the curated commit untouched; amend in only a genuinely necessary build/source fix (e.g. configurable arch).
- A CPU-only docker build (image `rocm/dev-ubuntu-24.04:7.2.4-complete`) proves the code compiles and links under ROCm. It cannot observe any Fault-class bug above, since no GPU runs, so it is never a validation gate. Do NOT wire it into the fork's GitHub Actions: a yml change bumps the fork HEAD sha and forces every platform to revalidate (churn), and the run just fails and emails. Disable Actions on the fork instead; run a CPU-only docker build locally if you want a manual compile check.
- gfx90a is CDNA (wave64); gfx1100/gfx1151 are RDNA (wave32). A change that passes on gfx90a can still fail on RDNA via the warp-size class, which is why followers re-validate on their own hardware.

## Per-arch notes

- gfx90a: MI200-class CDNA2, wavefront 64. Lead platform.
- gfx1100: RDNA3 (Radeon), wavefront 32. Watch warp-size assumptions and RDNA occupancy.
- gfx1151: RDNA3.5 APU, wavefront 32. The Windows HIP SDK is less complete than Linux ROCm; some libraries may be unavailable. Best-effort, after Linux is proven.

## Changelog

Append `YYYY-MM-DD -- lesson -- source project` when you learn a generalizable lesson. Seed entries below come from prior ports and are not yet re-derived inside MOAT.

- seed -- warp-size abstraction: runtime `warpSize` on host, per-arch constant in device code, compile-time upper bound for static arrays -- pytorch, FBGEMM
- seed -- colmap model: `enable_language(HIP)` + a single `cuda_to_hip.h`, `.cu` marked `LANGUAGE HIP` -- colmap
- seed -- rule-of-five on texture handles; clamp out-of-bounds neighbor reads; 256-byte texture pitch -- colmap
- 2026-05-30 -- before dismissing the 256B-pitch fault class, confirm the ACTUAL texture resource type: a cudaResourceTypePitch2D bind is subject to it (even if the pitch happens to satisfy it); do not assume the texture is cudaArray-backed -- CudaSift
- 2026-06-02 -- wave64 packs N logical 32-warps per wavefront, so a cross-logical-warp shared-memory producer->consumer runs in implicit lockstep and a missing barrier is invisible; on wave32 each logical warp is its OWN wavefront with no implicit ordering, so add an explicit barrier at every cross-logical-warp shared boundary. SCOPE the barrier by the dependency: intra-logical-warp LDS write->read (e.g. a CBD sampler writes a poly to __shared__ then the in-place NTT reads it back) needs only `__syncwarp` (the wavefront IS the logical warp on wave32; a sound superset on wave64/CUDA); a dependency spanning two logical warps in different wavefronts needs a block `__syncthreads`. Crypto KATs (bit-exact) fingerprint it as a NON-DETERMINISTIC wrong result confined to the one buffer the unordered warp produced (icicle: k=768 keygen dk_pke byte 0..383 = s_hat[0] varied every run while s_hat[1..2] stayed exact). Tell: one code path already fences (icicle eta=3 sampler had the trailing __syncwarp) and the sibling path that "looks the same" does not -- diff them -- icicle
- 2026-05-30 -- review (code/strategy/analysis) and validate (real GPU run) are separate MOAT stages; the reviewer does not block on a missing GPU run, the validator provides it -- CudaSift
- 2026-05-30 -- perf-critical kernels (attention/GEMM/quant, often CUTLASS/CuTe/Hopper-tuned): a straight HIP translation may underperform an AMD-native (rocWMMA/CK/MFMA) approach; the planner decides port-vs-rewrite -- per jeff
- 2026-05-30 -- CUTLASS does NOT port to ROCm and never will: do not attempt a CUTLASS->ROCm port or a CUTLASS->ROCm shim. Reimplement CUTLASS/CuTe-based kernels (e.g. raft pairwise-distance / fused_distance_nn / neighbors, which gate cuvs/cuml) against Composable Kernel (CK). CK is NOT a 1:1 API match, so first fully understand what the CUTLASS kernel does (tiling, epilogue, data movement, fusion), then write the equivalent from CK's many in-repo examples. Prefer ck_tile (within CK) for new work over older CK APIs -- per jeff
- 2026-05-30 -- HIP rejects cudaFilterModeLinear + element-read float textures; replace the hardware linear fetch with manual bilinear interpolation (point fetch 4 neighbors + lerp, -0.5 texel-center convention) -- popsift
- 2026-05-30 -- kernels that pack two 32-thread rows into one block break on wave64 (one 64-lane wavefront): __ballot/__popc/__any/warp-bitonic-sort must operate per 32-lane half; treat the wavefront as two independent 32-lane groups so each row matches a 32-lane NVIDIA warp -- popsift (confirmed: ballot_group/any_group helpers + width-32 shuffles restore run-to-run determinism and remove the -nan descriptors)
- 2026-05-30 -- HIP runtime bug (confirmed, gfx90a/CDNA2, ROCm 7.2.1): a cudaArrayLayered+SurfaceLoadStore array written layer-by-layer collapses its layer dimension on any later-launch read (tex2DLayered, surf2DLayeredread, AND host hipMemcpy3D all return the last-written layer for every index); sync and recreating the texture/surface do not help. surf2DLayeredread does NOT fix it (the earlier "surface is fresh" claim was a one-layer-repro artifact). A non-layered 3D array (drop cudaArrayLayered; surf3Dwrite/surf3Dread/tex3D, level as z) IS coherent. Fix: 3D array + map surf2DLayeredwrite->surf3Dwrite in the compat header (call sites untouched) and reads->surf3Dread/tex3D via the project's fetch helper. Repros agent_space/popsift_run/{multilayer_check*,array3d_check,tall2d_check}.cpp; bug report findings/popsift-texsurf-coherency -- popsift
- 2026-05-30 -- wave64 leader-election trap: a kernel that packs two 32-thread rows into one 64-lane wavefront (block (32,HEIGHT)) and does per-row leader election with `lane=threadIdx.x&31; if(lane==0) atomicAdd(...)` FIRES THE ATOMIC ON EVERY SET BALLOT BIT, not just one leader, inflating a global counter ~32-64x (popsift extrema_count: real 575 extrema -> count 36691; downstream walked uninitialized index slots -> a few keypoints emitted ~9000x and run-to-run-nondeterministic totals). When all rows feed the SAME counter, just do ONE full-wavefront 64-bit ballot: atomicAdd `__popcll(ballot)` by wavefront-lane-0, broadcast `__shfl(.,0,64)`, exclusive prefix `__popcll(ballot & ((1<<wflane)-1))`, wflane=threadIdx.x+(threadIdx.y&1)*32. Symptom that fingerprints it: stable primary count but wildly non-deterministic secondary count + massive duplicate outputs -- popsift
- 2026-05-30 -- RootSift/normalization NaN on AMD: sqrt(bin/sum) NaNs when the descriptor accumulation's round-toward-+inf intrinsics (mapped to round-to-nearest in the compat header) push a bin slightly negative, or an all-flat window gives sum==0 (sqrt(neg), 0/0). Default popsift norm is RootSift, not L2 -- check which path actually runs. Fix: clamp `sqrt(fmaxf(bin/sum,0))` and guard sum<=0 / non-finite frsqrt to 0 -- popsift
- 2026-05-30 -- inside a .cu compiled as HIP, host-side memcpy/memset can resolve to HIP's __device__ overloads once <hip/hip_runtime.h> is in scope (host helper fails to compile); include <cstring>/<cstdlib> BEFORE hip_runtime in the compat header so the libc host decls win -- gpuRIR
- 2026-05-30 -- IPO/LTO + the HIP toolchain breaks pybind11 modules: INTERPROCEDURAL_OPTIMIZATION leaves the .so as slim LTO bitcode with no PyInit_* (ImportError, tiny .so), because the HIP link step does not finalize LTO; disable IPO for the HIP build (it is usually optional) -- gpuRIR
- 2026-05-31 -- huge HIP shared library that overflows the +/-2 GiB x86-64 relocation reach at link (R_X86_64_PC32, then TLS, then .eh_frame; -mcmodel=large only fixes the first): the HIP offload bundle is stored UNCOMPRESSED by default, so a heavily-templated TU (rocPRIM DeviceReduce over a type x op matrix) embeds a ~159 MB .hip_fatbin. Add `--offload-compress` (documented ROCm 7.2.1 clang HIP flag) to the HIP compile options -- it stores the device bundle CCOB-compressed (runtime decompresses on load), measured 55x on the device fatbin (158.6->2.89 MB) and ~4x on the .o. Fit the whole library in ONE .so with no split and no -mcmodel=large. Prefer it before splitting or capping the template matrix; non-RDC builds have no separate device-link to also flag -- cudf
- 2026-05-31 -- a device functor/operator returning a reference (T&&/const T&) to a by-value or forwarded parameter is lifetime UB that nvcc/CUB tolerate but rocThrust/rocPRIM read as garbage (block-loads the dangling ref). cudf cast_fn's same-type `operator()(T&&)->T&&` (a copy elide) as the transform of a transform_iterator feeding hipCUB DeviceReduce::Reduce made min/max return the seeded identity and float/double sum return nan, ONLY when reduction output type == input element type. Fix: return by value (UB everywhere, so correct on CUDA too). Localize by reducing the same column three ways (library wrapper vs raw-pointer hipCUB vs thrust::reduce over the same iterator + bare op); a standalone microbench may not reproduce it (needs the library's nested-iterator instantiation), so an in-test multi-path probe is decisive -- cudf
- 2026-05-30 -- the 1D-texture linear-filter rejection is the same fault class as 2D (popsift): a cudaFilterModeLinear + cudaReadModeElementType float texture is rejected at create time on AMD regardless of dims; create it cudaFilterModePoint and lerp in software (point-fetch 2 neighbors, -0.5 texel-center convention) -- gpuRIR
- 2026-05-30 -- RIR/audio validation: the direct path is the FIRST significant arrival (round(dist/c*Fs)), NOT the global max; constructive early reflections can exceed it in a reverberant room, so a global-argmax peak check gives a false negative -- gpuRIR
- 2026-05-30 -- bake a configurable HIP arch into the LEAD port: set HIP_ARCHITECTURES from ${CMAKE_HIP_ARCHITECTURES} (default the lead arch only when unset), never a literal "gfx90a". A hardcoded arch overrides the cache var, so every follower validation is forced to edit CMake -- churning the curated commit's head_sha and forcing already-passed platforms to revalidate. Design the lead bringup for all target arches up front to avoid this churn -- CudaSift, Gpufit (per jeff)
- 2026-05-30 -- int atomicMin/atomicMax (and unsigned) are SILENTLY DROPPED on coarse-grained memory on gfx90a/CDNA2 (default hipMallocManaged): the RMW no-ops, the value never updates (atomicAdd and atomicCAS are unaffected). Emulate int min/max with an atomicCAS loop on HIP; use the UNSIGNED comparison when the sentinel is (uint32_t)-1. Micro-test: same op PASSES on hipMalloc device memory, FAILS on hipMallocManaged -- cudaKDTree (spatial builder bounds box stayed empty -> degenerate tree -> OOB)
- 2026-05-30 -- hipCUB DeviceRadixSort::SortKeys with a NONZERO begin_bit (e.g. bits [32,64) of a 64-bit {hi:lo} key) does not sort correctly on ROCm (gfx90a): output is not ordered by the selected bits. Sort the FULL key width instead (begin_bit 0); the high bits still dominate. Direct test: full-64-bit PASS, [32,64) BROKEN (9974/10000 misordered) -- cudaKDTree
- 2026-05-31 -- cudaDevAttrTexturePitchAlignment is 256 on gfx90a vs 32 on NVIDIA. Libraries that derive a tensor/image row-pitch from this attribute pad rows wider on AMD; gtests that compare the whole strided buffer (incl. padding) against a zeroed CPU ref then mismatch. If no tensor is HW-texture-bound, clamp the attribute query to 32 in a compat shim so AMD pitches match CUDA -- CV-CUDA (added to Fault classes)
- 2026-05-31 -- gfx90a __fsqrt_rn is not always correctly rounded (1 ULP; e.g. sqrtf(93606.f)); CUDA sqrt.rn.f32 is. Route the f32 device sqrt through f64 __dsqrt_rn and cast back for bit-exact gtests; CDNA f64 sqrt is fast -- CV-CUDA
- 2026-05-31 -- clang(HIP) defaults to -ffp-contract=fast (FMAs across statements); nvcc contracts expression-only. Pin -ffp-contract=on in CMAKE_HIP_FLAGS so HIP float math matches CUDA/host bit-for-bit (multi-statement weight chains like bicubic drift ~1 ULP otherwise) -- CV-CUDA
- 2026-05-31 -- HIP device cuda::min/max NaN-selection can differ from host std::min/max; spell the device ternary exactly as the host (std::min = b<a?b:a, std::max = a<b?b:a) so device==host on NaN/signed-zero (tests fill float buffers with random bytes) -- CV-CUDA
- 2026-05-31 -- cub/hipCUB block-collective TempStorage reuse races on a 64-thread (wave64) block: a 64-thread block is one wavefront, so BlockReduce/BlockRadixSort lower with no syncing epilogue and back-to-back reuse of the same TempStorage races (2 warps masked it on CUDA). Add __syncthreads() between reused-TempStorage collective calls -- CV-CUDA
- 2026-05-31 -- cudaMemcpy*Async from a soon-freed PAGEABLE host buffer: CUDA stages it synchronously (works), HIP can be truly async (reads the freed/reused buffer -> garbage). Surfaces only in bytes the kernel does not overwrite (e.g. padding vs a zeroed ref) and the failing set varies run-to-run. Test-fixture use-after-free (latent UB on CUDA, exposed on HIP), not a production/allocator defect; prove by keeping the source alive or syncing after the copy and watching it pass -- CV-CUDA (InterpolationVarShapeWrap, dispositioned a test artifact)
- 2026-05-31 -- HIP char4 members are `char`, CUDA's are `signed char` (distinct C++ types, same representation). A MakeType/BaseType layer must report `char` for the canonical charN on HIP so a reference accessor compiles; a test asserting is_same_v<signed char, BaseType<MakeType<signed char,N>>> then fails. Dictated by the upstream HIP vector-types header (HIP_vector_type<char,N>), not the porting layer; document as a true type-identity deferral, do not hack the test -- CV-CUDA
- 2026-05-30 -- __both__ / __host__ __device__ macros keyed on `#if defined(__CUDA_ARCH__)` break under HIP: __CUDA_ARCH__ is NOT defined during HIP device compilation (HIP uses __HIP_DEVICE_COMPILE__), so __both__ collapses to host-only and you get "reference to __host__ function in __host__ __device__ function". Make __both__ unconditionally __host__ __device__ on HIP, and add one CUKD_DEVICE_CODE-style macro (= __HIP_DEVICE_COMPILE__ on HIP, __CUDA_ARCH__ on CUDA) for the device-intrinsic-vs-host-fallback `#ifdef`s -- cudaKDTree
- 2026-05-30 -- clang (hence HIP) enforces two-phase name lookup where nvcc/MSVC do not: an unqualified call to an inherited member of a DEPENDENT base (e.g. base method from a class template parameterized base) needs `this->` or explicit qualification ("explicit qualification required to use member ... from dependent base class"). Also: a function-template explicit specialization must match the primary template's __host__/__device__ attributes (a __device__-only specialization of a __both__ primary -> "no function template matches") -- cudaKDTree
- 2026-05-30 -- rocThrust is a true drop-in for Thrust: same `thrust::` API and the same `<thrust/...>` header paths under /opt/rocm/include, so CUDA sources using thrust::sort/device_vector/zip_iterator compile unchanged on HIP with no source swap (only the compat-header CUB alias `#define cub hipcub` is needed for cub::) -- cudaKDTree
- 2026-05-30 -- CUKD_CUDA_CALL(X)-style macros that paste `cuda##X` hide the real symbol names from a grep for `cuda[A-Z]`; expand the macro usages (e.g. CUKD_CUDA_CALL(MemsetAsync) -> cudaMemsetAsync) when enumerating the symbol surface for the compat header, or the build will fail on a few missed symbols -- cudaKDTree
- 2026-05-30 -- a project may already ship an upstream HIP path (makefile.hip + a gpu_macro.cuh-style compat header + USE_HIP guards); do NOT auto-skip as already-supported. These HIP makefiles bitrot against the ROCm in use, so the MOAT value is to actually build+GPU-validate them and fix the rot. Most common rot: the HIP build inherits the CUDA `-std=c++14`, but rocThrust/rocPRIM/hipCUB hard-`#error` "rocPRIM requires at least C++17" (rocprim/config.hpp; headers use std::variant/std::is_same_v). CUDA Thrust accepts C++14 so upstream never caught it. Fix: bump the HIP build to -std=c++17 (only the TUs including thrust/cub fail) -- GPUMD
- 2026-05-30 -- the classic CUDA unrolled warp-synchronous reduction (a `volatile float* sdata` helper doing sdata[t]+=sdata[t+16/8/4/2/1] with NO __syncwarp, dispatched once the __syncthreads tree shrinks to <=32) RACES on AMD wave64: the low 32 lanes of a 64-lane wavefront are not guaranteed lockstep across those unsynced steps, so the reduction is wrong AND non-deterministic. Calling __syncwarp() between the steps is not a clean fix because the tail runs with only 32 of 64 lanes active (divergent full-mask sync). Cleanest correct fix: on HIP drop the warp-synchronous tail entirely and let the __syncthreads()-synchronized tree run to size 1 (same add order, block-wide barrier, correct on any wave size). Verify with a fixed-seed determinism check: two runs must be bit-identical -- MPPI-Generic
- 2026-05-30 -- header-only CUDA libs often use the idiom `#if __CUDACC__  #include "foo.cu"  #endif` at the bottom of each foo.cuh to pull device definitions in only under the device compiler. hipcc defines __HIPCC__ (both passes) but NOT __CUDACC__, so every kernel/device helper goes undeclared. Define __CUDACC__ when __HIPCC__ is set, in the compat header. Pair it with __CUDA_ARCH__ for device-vs-host `#ifdef`s: define __CUDA_ARCH__=1 ONLY in the HIP device pass, guarded by `#if defined(__HIP_DEVICE_COMPILE__) && __HIP_DEVICE_COMPILE__` (1 in the device pass, 0 in the host pass -- confirmed by compiling a __host__ __device__ probe). Force-include the compat header on every HIP TU (CMAKE_HIP_FLAGS `-include path/to/compat.h`) so both defines precede any use regardless of each file's include order -- MPPI-Generic
- 2026-05-30 -- defining __CUDA_ARCH__ for HIP (above) makes Eigen take its CUDA path and `#include <math_constants.h>` (a CUDA toolkit header absent on ROCm). Eigen has a native HIP device path keyed on __HIP_DEVICE_COMPILE__; force it by defining EIGEN_NO_CUDA in the compat header (Eigen's `#if defined(__CUDA_ARCH__) && !defined(EIGEN_NO_CUDA)` then skips CUDA and falls through to its HIP path, which ships its own math_constants.h). Eigen otherwise compiles unchanged under hipcc, including in __device__ code -- MPPI-Generic
- 2026-05-30 -- HIP's HIP_vector_type provides the FULL set of float2/3/4 operators (arithmetic, compound-assign, unary-minus, equality, vector-vector AND vector-scalar, all __host__ __device__), whereas CUDA's vector_types.h provides NONE. A project that hand-rolls these operators compiles on CUDA but hits "use of overloaded operator '*' is ambiguous" on HIP. Fix: USE_HIP-guard the project's vector-operator overloads (keep its non-operator helpers like dot/cross/norm, which HIP does not provide). Verify HIP's coverage (incl. host-callability) with a tiny probe before deleting -- MPPI-Generic
- 2026-05-30 -- the same toolkit angle-bracket includes (`<cuda_runtime.h>`, `<curand.h>`, `<cufft.h>`, `<device_launch_parameters.h>`, `<cooperative_groups.h>`) appear in dozens of files; ROCm ships no CUDA-named shim for them. Instead of editing every include line, put forwarding shim headers of those exact names in a hip_compat/ dir (each just `#include "cuda_to_hip.h"`, cooperative_groups -> `<hip/hip_cooperative_groups.h>`) and add that dir to the HIP include path ONLY (target_include_directories under `if(USE_HIP)`). On CUDA the dir is absent so the real toolkit headers win -> CUDA path byte-for-byte unchanged -- MPPI-Generic
- 2026-05-30 -- to flip all .cu in a CMake tree to LANGUAGE HIP without editing every per-target CMakeLists (each test/example dir globs *.cu into add_executable), override add_executable/add_library at the top-level scope under `if(USE_HIP)`: the override forwards to _add_executable/_add_library then retags the target's .cu sources LANGUAGE HIP and sets HIP_ARCHITECTURES (skip ALIAS/IMPORTED/INTERFACE targets). Zero churn in the subdir CMakeLists, CUDA build is a pure passthrough -- MPPI-Generic
- 2026-05-30 -- clang/HIP rejects `this->STATIC_CONST` (member access through `this`) as a constant expression where nvcc accepts it: breaks `kernel<this->STATE_DIM,...>` (non-type template arg) and `case this->DIM:` (case label). Use the class-qualified static member instead (`CLASS::STATE_DIM`). Same two-phase-lookup family as the `typename DependentBase::X` and `this->`-for-inherited-members rules -- MPPI-Generic
- 2026-05-30 -- hipFFT/hipRAND status-enum coverage is NOT 1:1 with cuFFT/cuRAND: e.g. CUFFT_LICENSE_ERROR/CUFFT_MISSING_DEPENDENCY have no HIPFFT_ equivalent, so a project's `cufftGetErrorString` switch referencing them fails to compile if you blanket-#define the codes. Map only the codes that exist and USE_HIP-guard the orphan `case` labels. Separately, hipFFT does not validate a garbage plan handle the way cuFFT does -- a negative test that passes an uninitialized plan to get back CUFFT_INVALID_PLAN will bus-error on HIP instead (robustness diff, not a functional break) -- MPPI-Generic
- 2026-05-30 -- when validating on a SINGLE assigned GPU, run the project's ctest SERIALLY (`ctest` not `ctest -jN`): N parallel test processes hammering one GPU cause contention-induced transient timeouts/failures that look like real bugs (seen ~1-2% flap between -j4 and -j1). Also: a closed-loop control/sim example may default its RNG seed to wall-clock time (e.g. seed_ = system_clock::now()), so run-to-run output legitimately varies -- to prove determinism, set a FIXED seed in a small harness and assert the optimized output is bit-identical across two runs, rather than diffing the stock example -- MPPI-Generic
- 2026-05-30 -- chaotic GPU MD (and any Lyapunov-unstable sim) cannot be bit-validated: run-to-run on the SAME GPU diverges because neighbor-list cell binning uses `ind = atomicAdd(&cell_count[cell],1); contents[base+ind]=atom` (neighbor.cu:80) and atomicAdd ordering across threads is non-deterministic on every GPU (CUDA included), so force-summation order, hence ULP-level forces, differ each run and chaos amplifies them. Upstream `diff -q thermo.out thermo1.out` regression tests therefore can't pass across vendors/runs. Validate by PHYSICS instead: total-energy conservation in NVE (rel drift ~1e-6), temperature at target, and PE/spectra agreement with the reference within tolerance (PE ~1e-5, DOS ~5e-2). Each run independently conserving energy (|dEtot| small while KE/PE decorrelate) is the proof the forces are correct -- GPUMD
- 2026-05-30 -- GLM used in device code: GLM 0.9.9.x emits __host__ __device__ on its math fns (dot/length/normalize/mat*vec) ONLY when it thinks it is the NVIDIA CUDA compiler, which it keys on __CUDACC__ + CUDA_VERSION>=7000 in glm/simd/platform.h. hipcc/clang defines neither, so every glm:: call from a kernel fails "call to __host__ function from __device__ function". You CANNOT fix it by pre-defining GLM_FUNC_QUALIFIER -- detail/setup.hpp redefines it unconditionally. Working fix: in the compat header, AFTER <hip/hip_runtime.h> is fully parsed, `#define __CUDACC__ 1`, `#define CUDA_VERSION 8000`, `#define GLM_FORCE_CUDA`, then let GLM be included afterwards (force-include the compat header on the HIP target so it precedes GLM). This steers GLM into its CUDA qualifier path (GLM_CUDA_FUNC_DEF -> `__device__ __host__`). Defining __CUDACC__ after the HIP runtime is already parsed does not disturb HIP. Newer GLM (0.9.9.9+) adds defined(GLM_FORCE_CUDA) to the branch so GLM_FORCE_CUDA alone suffices there -- 3DUNDERWORLD-SLS
- 2026-05-30 -- an UPSTREAM GPU path can be bit-rotted against the project's own CPU-side refactors, not just against ROCm -- so it fails to build on stock nvcc too; do NOT mistake "the GPU code does not compile" for a HIP problem. Here the CUDA reconstructor still referenced a deleted base header and a pre-refactor base-class contract (renamed Camera->ImageProcessor, FileReader->ImageFileProcessor; Reconstructor reduced from an owns-cameras/projector class to a pure reconstruct(Buckets) interface), with a half-finished renamed file left unused. Repointing the GPU classes at the current bases (host-side, minimal) is in-scope MOAT work and a prerequisite to any GPU validation. Tell: grep the GPU sources for base headers/types that no longer exist in core/ -- 3DUNDERWORLD-SLS
- 2026-05-30 -- dual CPU+GPU app with no GPU unit test: validate the GPU by running the GPU binary and the CPU binary on the SAME shipped dataset and comparing the output point clouds by nearest-neighbor coverage (the two paths often differ in point ORDER and in whether empty slots are emitted, so a line-by-line/array diff gives false negatives -- match as a point SET). Cross-check the CPU path against the project's own gtest reference clouds so the reference you compare to is itself trusted. Determinism: a per-bucket float average summed in atomicInc-determined fill order varies run-to-run only at ULP/print-precision (~1e-4 here, under the 6-sig-fig ASCII PLY ceiling and 4 orders under the project MAX_DIFF=10); stable point count + stable geometry to output precision is the right determinism bar, not bitwise file equality -- 3DUNDERWORLD-SLS
- 2026-05-30 -- code on the cuSPARSE GENERIC api (cusparseCreateCsr/SpMV/SpSV/CreateDnVec) ports most cleanly through hipSPARSE, NOT the lower-level rocSPARSE: hipSPARSE mirrors cuSPARSE name-for-name (cusparse->hipsparse, CUSPARSE_->HIPSPARSE_, cudaDataType/CUDA_R_*->hipDataType/HIP_R_* from <hip/library_types.h>), so the file ports line-for-line. Decisive for SpSV: hipSPARSE keeps cuSPARSE's PERSISTENT hipsparseSpSVDescr_t (createDescr/analysis/solve), whereas rocSPARSE's generic SpSV is stage-based with NO persistent descriptor, which would force a structural rewrite. Pick hipSPARSE for generic-api cuSPARSE code; reserve rocSPARSE for new AMD-native work -- amgcl
- 2026-05-30 -- hipSPARSE descriptor TYPE ALIASING gotcha: hipsparseMatDescr_t AND hipsparseDnVecDescr_t are BOTH `typedef void*` (hipsparse-types.h, hipsparse-generic-types.h), while in cuSPARSE they are distinct opaque struct pointers. A single deleter/visitor that overloads operator() on both cuSPARSE types fails to compile under hipSPARSE ("class member cannot be redeclared"). Fix: keep one void* overload and give the other descriptor its own deleter; std::shared_ptr<remove_pointer<...>::type> is shared_ptr<void> for both, so the distinct deleter callable at .reset() is what disambiguates, not the type -- amgcl
- 2026-05-30 -- there is no generic-dispatch hipsparseXcsrilu02_bufferSize/_analysis/hipsparseXcsrilu02 (nor the cuSPARSE X-forms); only type-suffixed hipsparse{S,D,C,Z}csrilu02* exist. cuSPARSE code typically hides this behind static `cusparseXcsrilu02_*` D/S overload wrappers -- port those wrappers verbatim to hipsparse* names. BUT the type-agnostic hipsparseXcsrilu02_zeroPivot DOES exist (matches cuSPARSE). Same pattern for csrsv2 in the pre-generic path -- amgcl
- 2026-05-30 -- for header-only template libs with mutually-exclusive backends (e.g. amgcl's SOLVER_BACKEND_*), mirror the CUDA backend header into a sibling HIP header (backend/cuda.hpp -> backend/hip.hpp) rather than #ifdef-ing one file: keeps the NVIDIA path byte-identical (zero regression risk) and the diff purely additive. The Thrust vector-op _impl specializations are identical between the two (rocThrust device_vector == thrust device_vector), so they only coexist safely because the backends are never compiled into the same TU; do not include both backend headers in one unit -- amgcl
- 2026-05-30 -- AMG/sparse-solver GPU validation that actually proves correctness: solve a 7-point 3D Poisson system (SPD, every AMG lib ships a generator) with the new HIP backend AND the builtin CPU backend, same matrix; assert (a) CONVERGES (final rel resid <= solver tol), (b) iteration count + final residual MATCH the CPU backend (same algorithm -> should be near-identical), (c) ||x_hip - x_cpu||inf/||x_cpu||inf ~ machine eps (~1e-15), (d) run-to-run bitwise determinism (achievable here because the deterministic SpMV alg rowsplit/CSR_ALG1 + Thrust reductions give a fixed reduction order). amgcl hip vs builtin matched to 1.4e-15 with 0 run-to-run diff at n=64 (262144 unknowns) -- amgcl
- 2026-05-30 -- hipEvent_t timing calls (hipEventCreate/Record/Synchronize/ElapsedTime) are marked nodiscard in the HIP headers whereas the cudaEvent* equivalents are not, so a CUDA timing helper that ignores the return code warns under HIP; wrap them in the project's existing error-check macro instead of casting to void -- amgcl
- 2026-05-30 -- HIP `__shfl_sync`/`__any_sync`/`__all_sync`/`__ballot_sync` REQUIRE a 64-bit mask (ROCm 7.x static_asserts `sizeof(MaskT)==8`, "The mask must be a 64-bit integer"), regardless of the active wave width -- so the literal `0xffffffff` that every CUDA warp-sync uses fails to COMPILE on HIP even before any wave64-correctness question. Define a compat full-warp mask keyed on USE_HIP (0xffffffffffffffffULL), NOT on warp width; CUDA keeps the 32-bit literal. The error surfaces during hipcc's HOST compile pass of the .cu (clang compiles the TU for host first), pointing at the macro -- AutoDock-GPU
- 2026-05-30 -- wave64 with atomicAdd-combined warp reductions: when per-warp partials are combined block-wide via `atomicAdd` into a shared accumulator (not assembled positionally), port the reduction NATIVELY to 64 lanes (warpmask=63/warpbits=6, add the stride-32 shuffle/exchange step) rather than treating the wave as two 32-lane halves -- the atomicAdd-combine makes warp granularity flexible, the true 64-lane warp is fewer steps, and it matches a `cData.warpmask`/`warpbits`-parameterized design. (Contrast popsift, where rows are packed positionally 2-per-block and you MUST keep 32-lane half-warps.) Pick the axis by how partials recombine, not reflexively -- AutoDock-GPU
- 2026-05-30 -- a project parameterized for warp size (device reads cData.warpmask/warpbits) can still hardcode them on the HOST (performdocking.cpp set them to 31/5) and in a stray device literal (kernel4 `blockDim.x/32`). Grep BOTH sides: device for the `& warpmask`/`>> warpbits` reads AND the host for where they are assigned, plus bare `/32`,`*32`,`>>5`,`<<5`,`&31`. Set them from props.warpSize at runtime so the same source yields 31/5 on CUDA and 63/6 on gfx90a with zero NVIDIA behavior change -- AutoDock-GPU
- 2026-05-30 -- Strategy A on a project whose HOST .cpp is full of `#ifdef USE_CUDA` GPU-driver blocks (alloc/memcpy/launch) that HIP genuinely shares: define USE_CUDA FOR the HIP build (one line) so all those blocks compile unchanged, and gate only the NVIDIA-only includes (cuda.h/curand.h) to `defined(USE_CUDA) && !defined(USE_HIP)`. The compat header retargets the cuda* runtime symbols to hip*. Far smaller footprint than converting 30 guards to `defined(USE_CUDA)||defined(USE_HIP)` -- AutoDock-GPU
- 2026-05-30 -- a hipcc-compiled kernel .o linked into a host executable by g++ needs `-fPIE` (or -fPIC): modern g++ links PIE by default, and a non-PIC device object trips `relocation R_X86_64_32 against .bss can not be used when making a PIE object`. Add -fPIE to the hipcc kernel-compile in the Makefile -- AutoDock-GPU
- 2026-05-31 -- hipify-perl (ROCm 7.x) emits a DOUBLED `hip/` prefix for a CUDA header that already lives under hip/: a source `#include <device_functions.h>` (inside a USE_AMD_ROCM guard) becomes `<hip/hip/device_functions.h>` -> "fatal error: file not found"; the correct header is `<hip/device_functions.h>`. When a project drives its own hipify script (Meta-style faiss/gpu/hipify.sh that translates *.cu->*.hip in place at cmake configure), fix it with ONE post-hipify sed in that script, right beside the script's existing path corrections (<hipblas.h> -> <hipblas/hipblas.h>, etc.), so it stays in the project's translation layer and survives a re-hipify. Watch for any other doubled-prefix `<hip/hip/...>` (excluding the legit `hip/hip_runtime.h` family) with `grep -rn 'hip/hip/' | grep -v 'hip/hip_'` -- faiss
- 2026-05-31 -- a configure-time in-place hipify (the cp-to-gpu-backup-then-translate kind) is NOT idempotent: re-running it on an already-hipified tree COMPOUNDS the translation (device_functions.h -> hip/.. -> hip/hip/..) AND overwrites its own backup with the already-translated state, so the backup no longer restores pristine. Two consequences: (1) it edits TRACKED files in place (the .cu/.cuh/.cpp CUDA includes get rewritten) and creates untracked .hip + a gpu-backup/ -- NONE belong in the fork commit (they regenerate at configure), so the curated commit must carry ONLY your hand edits; restore the rest with `git checkout -- <gpu dirs>`. (2) To re-hipify by hand, first restore pristine from git, delete the generated .hip + backup/tmp dirs, THEN run the script once; a clean cmake reconfigure from a pristine tree does this correctly on its own -- faiss
- 2026-05-31 -- ENABLE-AND-ADAPT confirmation for a project with mature merged upstream ROCm support (Meta-maintained FAISS): driving the existing FAISS_ENABLE_ROCM path on gfx90a/ROCm-7.2.1 needed ZERO source edits beyond the one hipify-script header fix above -- no arch-drift CMake edit (`-DCMAKE_HIP_ARCHITECTURES=gfx90a` propagates to the device TUs as --offload-arch automatically under CMake 3.24, so followers change only the cache var), no hipBLAS GemmEx fixup (MatrixMult-inl.cuh already version-guards HIPBLAS_COMPUTE_32F vs HIPBLAS_R_32F), no --offload-compress (single-arch fatbin links fine). The FAISS warp/block select (gpu/utils/Select.cuh + WarpShuffles.cuh + DeviceDefs.cuh) is genuinely wave-agnostic: kWarpSize = rocprim::arch::wavefront::max_size() (64 on CDNA), and on HIP CUDA_VERSION is undefined so WarpShuffles takes its MASKLESS `__shfl`/`__shfl_xor` branch (the `__shfl_sync(0xffffffff,...)` lines are dead) -- no __hip_check_mask abort. TestGpuSelect green on real gfx90a (6/6, deterministic over 2 runs) is the canonical confirmation that this kWarpSize select is wave64-correct, directly DE-RISKING the raft and cuvs ports that vendor a CUDA-only COPY of these exact files -- faiss
- 2026-05-31 -- FAISS GPU gtest harness needs per-process isolation; validate via `ctest` (one process per case via gtest_discover_tests), never the monolithic per-binary run. Monolithic TestGpuIndexFlat exits 139 AFTER "[ PASSED ] 18 tests" (HIP-runtime atexit teardown crash in the combined process), and TestGpuIndexIVFPQ.{Float16Coarse,Add_IP} only fail when many cases share ONE process (the global faiss RNG state advances across cases and pushes the float16 PQ approximate index past its 3.5% relative-error tolerance for that data realization); each passes in its own ctest process. Separately, the host CPU BLAS reference can abort the run: OpenBLAS 0.3.26 built NUM_THREADS=64 on a 128-core box hits a >NUM_THREADS auxiliary-allocation path that corrupts the glibc heap (`malloc(): corrupted top size`) in TestGpuIndexFlat.LargeIndex; set OPENBLAS_NUM_THREADS=1 for the gate. Both are host/harness artifacts (identical on the upstream CUDA build on the same host), NOT HIP/wave64 defects -- faiss
- 2026-05-30 -- ship-test cross-check is not always available: AutoDock-GPU's pre-existing OpenCL backend (the path that "already supports AMD") FAILS to compile its kernels under the ROCm 7.2 OpenCL runtime (upstream arg-type bug in gradient_inter_z), so it could not serve as a same-machine reference -- validate the HIP port directly against the published physics instead (1stp streptavidin-biotin: best binding ~-8.3 kcal/mol, best-pose reference RMSD <0.5A, all runs in one cluster). A broken OpenCL path also reinforces why the native HIP backend is worth adding -- AutoDock-GPU
- 2026-05-30 -- HIP vector types ship arithmetic operators that CUDA's plain structs lack: a project that hand-rolls e.g. `operator+=(float3&,float3)` (CUDA's float3 has no operators) makes EVERY `+=` ambiguous on HIP, because HIP's float3 is `HIP_vector_type<float,3>` with a member `operator+=` (clang: "use of overloaded operator '+=' is ambiguous"). Fix: `#if !defined(USE_HIP)`-guard only the colliding overload (HIP's built-in has identical componentwise semantics). Check each overload individually -- HIP provides vector-vector `*`/`/` as friends but NOT the float3*scalar forms, so `operator*(float3,float)` etc. do not collide and must stay -- Fast-Poisson-Image-Editing
- 2026-05-30 -- pybind11 + HIP LTO (refines the gpuRIR lesson): pybind11_add_module injects `-flto=auto -fno-fat-lto-objects` into the compile AND link flags directly, NOT via CMake's IPO property, so `set_target_properties(tgt INTERPROCEDURAL_OPTIMIZATION OFF)` does NOT remove it and the module still links without an exported PyInit_* under HIP (ImportError "dynamic module does not define module export function"). The working fix is `pybind11_add_module(tgt NO_EXTRAS ...)`, which drops pybind11's LTO flags entirely -- Fast-Poisson-Image-Editing
- 2026-05-30 -- a shared-memory-only stencil/convolution kernel (block (16,16)=256 threads, all cross-thread exchange via __shared__ + block.sync(), per-thread register ring buffers for the third axis) has NO wave64 fault class: it never touches __shfl/__ballot/warpSize, so torch hipify + build against a ROCm torch is the WHOLE port, zero source edits. Do not be misled by a literal `32` that is only a loop-tiling stride (`warp_id=tid/32; col+=32` to spread 256 threads over a 26x26 tile): it reads no hardware warpSize and does no cross-lane op, so it covers the tile correctly on wave64 unchanged. Confirm by checking the grep for warp primitives is empty AND the backward-gradient validation passes, not by the variable name -- fused-ssim
- 2026-05-30 -- "already supports ROCm" in a README/acknowledgements can be build-config-only (here the sole AMD commit changed setup.py by 1 line) and STALE against later features: fused-ssim's README says 3D is "CUDA only", but ext.cpp gates the 3D bindings on a define that setup.py sets for HIP too, so the 3D path actually compiles+exports+runs on ROCm and was simply never GPU-checked. Build and GPU-validate the CURRENT code (esp. paths added after the AMD enablement) rather than trusting the prose -- fused-ssim
- 2026-05-30 -- Strategy B + a CUDA project using CUDA Cooperative Groups: torch hipify maps `<cooperative_groups.h>` -> `<hip/hip_cooperative_groups.h>` and CG basics work (tiled_partition<32>, thread_rank, any, shfl_*), BUT HIP's CG (ROCm 7.2.1) has NO cg::reduce, no <cooperative_groups/reduce.h>, and no cg::labeled_partition -- grep the hip headers, they are simply absent. Replace cg::reduce(warp,v,plus/greater) with a butterfly `for(o=warp.size()/2;o>0;o>>=1) v op= warp.shfl_xor(v,o)` (USE_ROCM-guarded; keep cg::reduce on CUDA). Replace cg::labeled_partition(warp,label)+group-reduce+`if(rank==0)atomicAdd` by rebuilding the labeled group from `warp.match_any(label)` (mask of same-label lanes within the tile) + a masked all-reduce over that mask + ONE atomicAdd per label by the lowest-set-bit lane. CAUTION: the tempting shortcut of a trivial SelfLaneGroup where every lane does its own atomicAdd (the float sum is associative so the VALUE is identical, only the atomic COUNT differs) is WRONG for gradient-tolerance-sensitive code -- the larger atomic fan-out changes the float accumulation ORDER and flapped gsplat's v_viewmats gradient tolerance past its test bound; match CUDA's one-atomic-per-label granularity. Both are wave64-correct because tiled_partition<32>'s shfl_xor uses width=numThreads=32 and stays within the tile's 32 lanes on a 64-lane wavefront -- gsplat
- 2026-05-30 -- Strategy B GLM-in-a-bundled-submodule: torch's CUDAExtension hipify walks every .hpp under the build cwd + the extension include_dirs into its file set and then content-rewrites any GLM header a source pulls in -- which DROPS GLM's .inl files (hipify copies only .hpp/.h) and corrupts GLM's __CUDACC__/CUDA_VERSION compiler detection ("GLM requires CUDA 7.0", "scalar_constants.inl: No such file", "no matching function for 'length'/'quat_cast' -- host fn from device fn"). GLM 1.0.x ALREADY detects __HIP__ (glm/simd/platform.h sets GLM_COMPILER_HIP -> __device__ __host__) and compiles verbatim under -x hip, so the fix is to leave GLM untouched: torch's AOT hipify call (cpp_extension.py) forwards no `ignores`, so monkeypatch `hipify_python.hipify` in setup.py to add the glm dir to ignores AND strip it from header_include_dirs; GLM then resolves from the pristine source tree via -I. (Also: an uninitialized GLM submodule silently falls back to system GLM 0.9.9, which has NO HIP detection -- init the submodule first.) -- gsplat
- 2026-05-30 -- two more hipify/clang gaps in Strategy B: (1) hipify rewrites `std::min`/`std::max` -> the global CUDA spelling `::min`/`::max`; on the HIP HOST pass (g++) global ::min/::max(float,float) do not exist, so host TUs including the device headers fail. Provide host-only (`#if defined(USE_ROCM) && !defined(__HIP_DEVICE_COMPILE__)`) global min/max forwarding to std::min/std::max; the device pass keeps HIP's builtins. (2) `cudaFuncSetAttribute(kernel,...)` -> hipFuncSetAttribute takes the kernel as `const void*` only (CUDA has a templated T* overload), so cast `(const void*)kernel<...>` -- portable, CUDA also accepts const void*. Also no libcu++ on ROCm: `cuda::std::*` (numeric_limits/type_traits) -> alias to `std::` on HIP, but cuda::std::optional has no std device drop-in (exclude those kernels via the project's build flags) -- gsplat
- 2026-05-30 -- clang/HIP rejects two std constructs in __host__ __device__ code that nvcc accepts: `std::array{...}` CTAD (the deduction guide is __host__-only -> "reference to __host__ function '<deduction guide for array>'"; spell the type `std::array<T,N>{...}`) and `std::array::at()` (its bounds-check pulls host-only __throw_out_of_range_fmt; use operator[]). Same family as the __CUDA_ARCH__-undefined-on-HIP trap: a `#ifdef __CUDA_ARCH__` picking assert(device) vs TORCH_CHECK/throw(host) takes the HOST branch on the HIP device pass (HIP uses __HIP_DEVICE_COMPILE__), pulling host-only throws into device code -- use `#if defined(__CUDA_ARCH__) || defined(__HIP_DEVICE_COMPILE__)` -- gsplat
- 2026-05-30 -- a Strategy-A project on the LEGACY FindCUDA module (`find_package(CUDA REQUIRED)` + `cuda_add_library(...)`, driving CUDA_NVCC_FLAGS) does not fit the modern `set_source_files_properties(.. LANGUAGE HIP)` recipe. Bridge with zero per-module edits: under USE_HIP define a `cuda_add_library()` SHIM macro that calls add_library() + marks sources LANGUAGE HIP, so every `cuda_add_library(foo ...)` call is untouched and the NVIDIA path is byte-identical. Mark BOTH .cu AND .cpp LANGUAGE HIP -- cuda_add_library mixed both and several .cpp include rocThrust/stdgpu headers that only clang parses (nvcc drove the whole call, so mirror that; clang compiles plain host C++ fine). Map CUDA_LIBRARIES->hip::host so existing `${CUDA_LIBRARIES}` link lines keep working -- cupoch
- 2026-05-30 -- hip::device (and roc::rocthrust, which pulls it) put `-x hip --offload-arch=...` in INTERFACE_COMPILE_OPTIONS, which propagate to ALL languages of a consuming target -> g++ chokes on `--offload-arch` for any host .cpp in that target ("unrecognized command-line option"). Link only hip::host (runtime, no -x hip); rocThrust/hipCUB/rocPRIM are header-only under /opt/rocm/include (the HIP compiler's default search path), so a LANGUAGE-HIP .cu needs no rocThrust link target at all. Symptom fingerprint: the .cu files compile but the sibling .cpp in the same target fail with `--offload-arch` -- cupoch
- 2026-05-31 -- a very large HIP shared library (heavy type-dispatched template code, esp. on rocPRIM where each cub/rocPRIM DeviceReduce/DeviceRadixSort instantiation embeds its own gfx ISA -> some single .cu objects are 200+ MB, dominated by a ~190 MB .hip_fatbin) can outgrow the +/-2 GiB reach of x86-64 32-bit relocations at link, in THREE escalating classes you hit one at a time: (1) `R_X86_64_PC32 out of range` (.text -> .rodata.str/.data) -- fixable with `-mcmodel=large` on the HIP HOST pass (64-bit absolute data refs; the amdgcn device pass carries its own code model and is unaffected; clang's HIP driver applies -mcmodel only to the host sub-compile); (2) then `-mcmodel=large` EXPOSES `R_X86_64_TLSGD`/`R_X86_64_TLSLD`/`R_X86_64_PLT32`->__tls_get_addr overflow on thread_local accesses (large CODE model != large TLS model; clang has no >2 GiB x86-64 TLS access model); (3) then `.eh_frame` "PC offset is too large" in the unwinder FDEs. So -mcmodel=large alone is NOT a full fix above ~2 GiB of host image. The real fix is structural: split the library into multiple .so so no single image exceeds 2 GiB, or cap the per-TU device-code fatbin (limit the instantiated type matrix). Add -ffunction-sections/-fdata-sections + --gc-sections first (prunes unreachable instantiations) but it does not raise the 2 GiB ceiling. Confirm the .o bloat with `size -A x.o` (look at .hip_fatbin) before blaming the host code -- cudf
- 2026-05-31 -- a ROCm-ported header-only CCCL library (cuCollections) can be a NEWER generation than a consumer pins on CUDA, changing two contracts: (a) its stream-ordered allocator now wants the CCCL 2-arg `allocate(num, cuda::stream_ref)` / `deallocate(ptr, num, cuda::stream_ref)` form, but rmm's stream_allocator_adaptor exposes only the single-arg standard-C++ form (fixed stream bound at construction) -> on HIP subclass the rmm adaptor and add the 2-arg overloads (forward the caller's stream via rmm::cuda_stream_view{stream.get()} to the underlying polymorphic_allocator), keeping the 1-arg form via `using base::allocate/deallocate`; (b) template orders/namespaces shift (e.g. only modern `cuco::static_multimap<Key,T,Extent,Scope,KeyEqual,ProbingScheme,Allocator,Storage>`, no `cuco::legacy::`) -> re-spell the consumer's type aliases under USE_HIP so dependent headers parse, even if the TU that instantiates them stays scoped out. Keep the CUDA spelling on the NVIDIA path -- cudf
- 2026-05-31 -- rocPRIM's DeviceReduce invokes the user binary functor through a CONST wrapper (hipcub::detail::convert_binary_result_type_wrapper) where CUB calls a non-const operator(); a reduction functor with a non-const `operator()` then fails to compile on HIP ("no matching function"/const-qual mismatch). Make the functor's operator() const (correct on CUDA too). Pair with clang two-phase-lookup: a dependent member template needs `typename T::template member<Arg>` where nvcc accepts the bare `T::member<Arg>` -- cudf
- 2026-05-30 -- rocThrust namespace deltas that bite a CUDA Thrust port: `thrust::cuda::par` / `thrust::system::cuda::unique_eager_event` do NOT exist on the HIP device system -- they are `thrust::hip::par` / `thrust::system::hip::` (guard with USE_HIP). Thrust's CUDA pinned_allocator (thrust::cuda::experimental::pinned_allocator, often vendored into a project) includes <thrust/system/cuda/error.h> = CUDA-only cudaSuccess/cudaError* enums that break EVERY TU including it under HIP; replace its body with `using pinned_allocator = thrust::hip::universal_host_pinned_allocator<T>` (from <thrust/system/hip/memory.h>) under the same name. Otherwise rocThrust device_vector/sort/reduce_by_key/transform are true header drop-ins -- cupoch
- 2026-05-30 -- stdgpu 1.3.0 (vendored as a submodule in cupoch and others) bitrots against ROCm 7, all fixable from the PARENT project's CMake with NO submodule edit: (1) its bundled Findthrust.cmake, seeing rocThrust THRUST_VERSION>=2.0.0, demands CUDA-only cub/version.cuh + cuda/std/version that ROCm lacks -> drop a HIP-aware Findthrust (locate only thrust/version.h, expose thrust::thrust) earlier on CMAKE_MODULE_PATH; (2) its `find_package(hip 5.1 REQUIRED)` is rejected by ROCm 7's SameMajorVersion hip-config (major 7 != 5) -> a tiny hip-config + hip-config-version SHIM on CMAKE_PREFIX_PATH that reports PACKAGE_VERSION_COMPATIBLE TRUE (the real hip::host/device already exist from the top-level find_package(hip)); (3) it compiles its generic impl/*.cpp with the CXX compiler, but the HIP backend's headers reach rocPRIM which only clang parses (__builtin_amdgcn_wavefrontsize) -- the stock HIP backend assumes hipcc IS CXX. Force every stdgpu TU LANGUAGE HIP via `set_source_files_properties(<abs paths> TARGET_DIRECTORY stdgpu PROPERTIES LANGUAGE HIP)`; the paths MUST be absolute (a relative source name silently no-ops when set from a different directory than where the target was defined) -- cupoch
- 2026-05-30 -- a vendored CUDA float-vector helper header (cutil_math.h-style, ~270 operator/min/max defs that CUDA's operator-less float3/float4 structs need) is REDUNDANT under HIP and only causes "ambiguous operator '*'/'/'" + "redefinition of max/min": HIP's HIP_vector_type<float,N> already ships componentwise + - * / for BOTH vector-vector AND vector-scalar, plus make_floatN and .x/.y/.z/.w. Don't #ifdef-guard 270 defs -- drop a tiny HIP replacement header on a target-PRIVATE BEFORE include dir that defines ONLY the NAMED helpers the project actually calls (grep for them; here just dot/length/fabs on vectors) on top of HIP's operators. Same class as the Fast-Poisson float3 operator collision, at header scale -- cupoch
- 2026-05-30 -- per-target PRIVATE+BEFORE compat include dirs are the clean way to retarget a vendored lib's CUDA-only includes (<cuda.h>/<cuda_runtime.h>/<vector_functions.h>/<vector_types.h>) to the HIP runtime/vector headers WITHOUT a global shadow: a global `cuda_runtime.h` shim breaks rocThrust's own internals (thrust/system/cuda/error.h legitimately includes the REAL <cuda_runtime.h> on the CUDA system path). Keep the shim dir on only the one flann/lbvh target via target_include_directories(tgt BEFORE PRIVATE ...). For headers included BEFORE the runtime (flann result_set.h uses __forceinline__/__int_as_float at parse time), force-include a HIP prelude with `-include <prelude.h>` (full path, not a bare header name, or clang's HIP driver treats it as a compile input -> "cannot specify -o when generating multiple output files") -- cupoch
- 2026-05-30 -- the __CUDACC__-vs-__HIPCC__ guard trap, third variant (cf. cudaKDTree __both__ and gsplat __CUDA_ARCH__): vendored headers gate device-vs-host code on `#if defined(__CUDACC__)` (e.g. lbvh_index vec_math.h: `__host__ __device__` decorations under __CUDACC__, else plain host `max`/`min` that collide with HIP's builtins). HIP clang defines __HIPCC__ and __HIP_DEVICE_COMPILE__ but NOT __CUDACC__, so the host branch is taken on the device pass. Extend each such guard: `defined(__CUDACC__) || defined(__HIPCC__)`. Also: a function-template/overload whose DEFINITION omits the __host__ __device__ its DECLARATION carries fails on clang ("__host__ function cannot overload __host__ __device__ function") where nvcc merged them -- the .inl defs must repeat the attributes -- cupoch
- 2026-05-30 -- breadth-scoping a large Strategy-A library to a validatable CORE: a CUPOCH_CORE_ONLY-style option that builds only the GPU libs on the target op's dependency closure (here utility+camera+knn+geometry) + a minimal hand-rolled third-party subset, skipping the full third_party graph, is the right move when the full build hard-requires host libs absent on the headless ROCm host (OpenGL GL/glfw/glew here gate visualization + the pybind11 module). Verify the closure by grep of cross-module #includes first. Defer the few files that need deeper clang fixes off the validated path (here distancetransform.cu __shared__-of-nontrivial-type, and trianglemesh/occupancygrid/voxelgrid via the .inl attribute bug) by list(FILTER ... EXCLUDE REGEX) under USE_HIP, documenting covered-vs-deferred -- cupoch
- 2026-05-30 -- point-cloud / geometry GPU validation that actually proves correctness on real hardware: (a) VoxelDownSample (Thrust transform->sort_by_key->reduce_by_key) -- replicate the exact grid-key formula floor((p-min_bound)/voxel) on CPU, average per occupied voxel, compare centroid SETS order-independently (sort both) to ~float eps and assert bitwise-identical output across >=3 runs; (b) EstimateNormals (kdtree KNN + covariance) -- CPU brute-force KNN + 3x3 covariance smallest-eigenvector, compare SIGN-FREE `|dot(n_gpu,n_cpu)|`~1 (normal orientation is arbitrary) over all points + run-to-run determinism. cupoch matched to 2e-7 (downsample) and worst |dot| 0.999998 (normals), 1836 kernel dispatches under AMD_LOG_LEVEL=3 confirming device execution. CAUTION writing the CPU reference: Eigen's Vector3f default ctor does NOT zero-initialize, so a std::map accumulator pattern (`bins[key].first += p`) sums onto garbage -> inf/nan; emplace the first point instead of += onto a default-constructed value -- cupoch
- 2026-05-30 -- the SINGLE highest-value HIP fault for a library that splits __device__ work between a header struct and code gated on `#ifdef __CUDA_ARCH__`: __CUDA_ARCH__ is NOT defined during HIP device compilation (HIP uses __HIP_DEVICE_COMPILE__), so EVERY `#ifdef __CUDA_ARCH__` device-vs-host data-pointer selection (e.g. Attribute::size() returning m_d_patches_info on device vs m_h_patches_info on host) silently takes the HOST branch on the GPU and dereferences a host pointer -> memory access fault inside the kernel (here all mesh queries faulted). Mechanically rewrite every `#ifdef __CUDA_ARCH__` -> `#if defined(__CUDA_ARCH__) || defined(__HIP_DEVICE_COMPILE__)` and `#ifndef __CUDA_ARCH__` -> `#if !defined(__CUDA_ARCH__) && !defined(__HIP_DEVICE_COMPILE__)` across include/. Leave numeric `#if __CUDA_ARCH__ >= 700` (atomics/ISA) alone but neuter a paired `#if (__CUDA_ARCH__ < 700) #error` so it does not fire on HIP (`#if defined(__CUDA_ARCH__) && (__CUDA_ARCH__ < 700)`). WATCH: enabling previously-dead device code (it was `#ifdef __CUDA_ARCH__` = false on HIP, hence a silent no-op pre-port) can surface a real runtime bug that was simply never executed before -- RXMesh
- 2026-05-30 -- wave64 32-bit-word bitmask ballot (generalizes the popsift two-32-rows lesson to participation/deletion masks): a warp-cooperative mesh/graph kernel that stores a per-element bitmask as 32-bit words (one word per 32 elements) and writes it with `mask = __ballot_sync(0xFFFFFFFF, pred); if (threadIdx.x%32==0) bitmask[id/32]=mask;` truncates lanes 32-63 on a 64-lane wavefront AND never writes the 2nd word (id/32 differs for lanes 0-31 vs 32-63). Fix with a helper: 64-bit `__ballot(pred)` then each 32-lane sub-group extracts its own 32 bits `(b >> 32*((threadIdx.x%warpSize)/32)) & 0xffffffff` and its lane-0 (threadIdx.x%32==0) writes its own word. Works for both CDNA wave64 (two halves) and RDNA wave32 (half always 0). Word alignment holds because block sizes are multiples of 64 -- RXMesh
- 2026-05-30 -- a project that declares __device__ members in headers and DEFINES them in separate .cu TUs (explicit-instantiation pattern) MUST be built with relocatable device code on HIP (`-fgpu-rdc` + CMake HIP_SEPARABLE_COMPILATION ON, applied to the lib AND its consumers; the CUDA path uses CUDA_SEPARABLE_COMPILATION/-rdc=true for the same reason). Without it the device link fails: "undefined hidden symbol: Foo::method()" for every cross-TU __device__ symbol. Mark consumer .cu LANGUAGE HIP too (CMake silently DROPS unmarked .cu from the link when the project enables HIP not CUDA -- the test exe linked with zero test objects and fell back to gtest_main's main, "does NOT link in any test case") -- RXMesh
- 2026-05-30 -- a block-wide spin-lock (`__shared__ int` mutex, `while(atomicCAS(m,0,1)!=0){}` ... `atomicExch(m,0)`) that several lanes of the SAME wavefront contend for DEADLOCKS on AMD CDNA (gfx90a, wave64) and is the #1 suspect when a warp-cooperative CUDA kernel hangs on HIP. CUDA Volta+ has Independent Thread Scheduling so the lane that wins the CAS makes forward progress to the unlock even while its warp-siblings spin; CDNA has NO per-lane forward-progress guarantee within a wavefront, so the winner is stuck at SIMT reconvergence waiting on the losing lanes, which spin forever. (RXMesh's ShmemMutex even ships `#error ... requires sm70 ... Independent Thread Scheduling` -- heed it: it means "deadlocks without ITS", i.e. on CDNA.) DIAGNOSIS PITFALLS that cost a prior pass here: the hang is NOT -fgpu-rdc, NOT a cross-TU boundary, NOT a garbage/per-lane `m_mutex`. A standalone repro of the exact pattern swept {helper inlined vs separate-TU} x {rdc on/off} x {static vs dynamic __shared__} HANGS in ALL 8 configs incl. inlined+rdc-off+static, and the pointer is a valid lane-uniform shared address (the `0x1000000000000` rocgdb prints is just the shared-address-space tag; low32==0). The earlier "garbage pointer under rdc" reading was a misread of registers in already-deadlocked/divergent lanes. FIX (USE_HIP/__HIP_DEVICE_COMPILE__-guarded; CUDA path unchanged): wave-serialize the lock -- elect one active lane of the wavefront at a time and let it fully acquire/run/release before the next: `int lane=__lane_id(); unsigned long long need=__ballot(1); while(need){ int L=__ffsll((long long)need)-1; if(lane==L){ while(atomicCAS(m,0,1)!=0){} __threadfence(); <crit>; __threadfence(); atomicExch(m,0);} need&=~(1ull<<L);}`. Only one lane contends -> no intra-wave hazard; cross-wavefront contention is fine (wavefronts schedule independently). WARNING: the tempting half-fix of folding the critical section INTO the CAS loop (`while(!done){if(atomicCAS==0){<crit>;exch;done=1;}}`) does NOT work -- the compiler peels acquiring lanes off the exec mask (`s_andn2_b64 exec,...`) and keeps the lock held until the reconverged body runs (verified 10/10 hang at -O3); only explicit one-lane-at-a-time serialization is robust. Expose it as `critical_section(func)` taking the body as a lambda so the call sites stay lock()/unlock()-shaped, and wave-serialize the array form (`critical_section(loc,func)`) identically -- RXMesh
- 2026-05-30 -- HIP/clang has NO __nv_is_extended_{device,host_device}_lambda_closure_type builtin and NO SFINAE-detectable substitute (decltype/std::is_invocable on a `[]__device__` lambda does NOT fail in host context -- clang defers the host/device diagnostic), so a `for_each(location, lambda)` API that uses `if constexpr(IS_D_LAMBDA(L))` to gate its device kernel launch cannot be made to compile-time-distinguish a device lambda on HIP. Defining it true forces the device kernel to instantiate for the library's own internal for_each(HOST, [&]{...}) host lambdas too (they call host-only members -> "reference to __host__ function in __host__ __device__"). Pragmatic resolution: disable the device-for_each branch on HIP (true if the lib's hot path uses a different device-iteration entry, e.g. a query dispatch), and exclude the few tests that call for_each(DEVICE,...) -- RXMesh
- 2026-05-30 -- a few small HIP-runtime gaps to expect in CUDA mesh/graph code: hipProfilerStart/Stop return hipErrorNotSupported unless a profiler tool is attached (a CUDA_ERROR-wrapped cudaProfilerStart in a benchmark loop is then FATAL, and the exit() can hang HIP teardown -> looks like a kernel hang) -- guard the profiler calls out on HIP; HIP cooperative groups has no memcpy_async/wait (fall back to a synchronous block-strided copy + block.sync()); __grid_constant__ kernel-param annotation has no HIP equivalent (define it empty, else clang mis-parses the param: "expected ')'" + cascading "undeclared identifier" on the param name); cusolverSp sparse-direct solvers (csrchol/csrqr/csrlsv*) have NO hipSOLVER equivalent (scope those tests out; hipBLAS L1 + hipSPARSE generic-API still cover dense_matrix) -- RXMesh
- 2026-05-30 -- the libcu++ / `cuda::std` gap on ROCm is FILLED by the header-only ROCm/libhipcxx (github.com/ROCm/libhipcxx), which supersedes the earlier gsplat lesson of "exclude cuda::std::optional kernels via build flags". It provides the full `cuda::std::` namespace (`<cuda/std/optional>`, `<cuda/std/limits>`, `<cuda/std/type_traits>`, ...; `hip::std::` aliases too), tested for ROCm 7.2/gfx90a/gfx942/gfx950, hipcc, C++17 and C++20, no special macros. It is NOT in the ROCm 7.2.x release, so VENDOR it: `git clone --branch amd-develop https://github.com/ROCm/libhipcxx` (validated on ROCm 7.2.1; fallbacks `release/2.2.x` then tag `v2.2.0`) and add `-I<clone>/include` to the hipcc compile -- header-only, nothing to build, no link flags. For a Strategy-B torch CUDAExtension, add that -I to the device (`nvcc`-key / extra_cuda_cflags) args USE_ROCM-guarded; `<cuda/std/*>` and the `cuda::std::` namespace pass through torch's hipify UNCHANGED (no hipify mapping), so no source edits are needed for the cuda::std symbols. NOT supported by libhipcxx: `<cuda/ptx>`/`cuda::ptx`, and `<cuda/std/{latch,barrier,semaphore}>`. Validated by re-enabling gsplat's 3DGUT path (was deferred purely for `cuda::std::optional`): the kernels compile + the ops register + the device-side 3DGUT tests pass on gfx90a -- gsplat / libhipcxx
- 2026-05-30 -- a base library with a MATURE upstream HIP backend can still be bitrotted against the CURRENT ROCm's CMake TARGET layout (not the kernels): two distinct breakages on STRUMPACK v8.0.0 / ROCm 7.2.1, both pure-CMake, NVIDIA-safe, no kernel edit. (1) The `hip::device` `-x hip` propagation (cupoch fault class) reaches the project transitively: `roc::rocthrust` -> `roc::rocprim_hip` -> `hip::device`, whose INTERFACE_COMPILE_OPTIONS carry `-x hip` + `--offload-arch`, so linking `roc::rocthrust` PUBLIC hands every host .cpp of the target `--offload-arch=gfx90a` and g++ dies ("unrecognized command-line option '--offload-arch'"). rocblas/hipblas/hipsparse correctly link only `hip::host`; ONLY rocThrust/rocPRIM pull `hip::device`. rocThrust is a header-only INTERFACE target on the HIP default include path, so DROP `roc::rocthrust` from the link list and re-add just its include dir via `target_include_directories(tgt PRIVATE $<get_target_property roc::rocthrust INTERFACE_INCLUDE_DIRECTORIES>)` (plain -I, no -x hip); the .hip TUs still find thrust. (2) The INSTALLED package config is broken for HIP consumers: the project's `xxx-config.cmake.in` has an `if(@USE_CUDA@) find_dependency(CUDAToolkit)` block but NO HIP equivalent, yet the installed target's INTERFACE_LINK_LIBRARIES references `roc::hipblas` etc., so a dependent's `find_package(PKG)` fails ("target roc::hipblas not found" / "cannot find -lroc::hipblas"). Mirror the CUDA block: `if(@USE_HIP@) find_dependency(hip hipblas hipsparse rocsolver rocblas)`. ALWAYS test the consumption path end-to-end (a 3-line downstream `find_package(PKG)+target_link_libraries` project built with plain g++) before declaring a base library consumable -- a clean local build can still leave find_package broken. Sequential (non-MPI) scope-down: STRUMPACK MPI=ON pulls ParMetis/PTScotch/SLATE/ButterflyPACK; `-DSTRUMPACK_USE_MPI=OFF` auto-disables them (CMakeLists.txt:74-95), enough to validate the single-GPU sparse direct solver -- STRUMPACK
- 2026-05-30 -- sparse DIRECT solver (multifrontal LU) GPU validation that proves correctness: STRUMPACK ships a 3D Poisson regular-grid generator + `max_scaled_residual` and `enable_gpu()/disable_gpu()` toggles on ONE binary, so factor+solve the same SPD system GPU vs CPU-reference and assert (a) componentwise scaled residual ||Ax-b|| at solver tol (~1e-15 here, converged in 1 Krylov iter), (b) ||x_gpu-x_cpu||inf/||x_cpu||inf ~ machine eps (~2-5e-15), (c) ||x_gpu-x_exact|| from a known RHS, (d) run-to-run BITWISE identical. Confirm the factorization is actually on-GPU via AMD_LOG_LEVEL=3: a multifrontal solver dispatches its own named device kernels (here `strumpack::gpu::{assemble_kernel,extend_add_kernel,LU_block_kernel_batched,Schur_block_kernel_batched,solve_block_kernel_batched}` -- the LU + Schur-complement + triangular-solve fronts), not just library GEMMs. STRUMPACK (BSD, multifrontal sparse LU + HSS/BLR) is the open ROCm substitute for proprietary NVIDIA cuDSS (which cannot be ported) -- it is RXMesh's solver-path dependency -- STRUMPACK
- 2026-05-30 -- RAPIDS-on-ROCm, the rapids-cmake/CPM CCCL redirect (recurs for EVERY RAPIDS lib -- rmm/raft/cudf/cuvs/cugraph/cuml): a RAPIDS project's stock CMake FETCHES NVIDIA CCCL at configure time (cpp/CMakeLists.txt bootstraps rapidsai/rapids-cmake via cmake/RAPIDS.cmake, then rapids_cpm_init()+get_cccl, all gated on CUDAToolkit REQUIRED + enable_language(CUDA)). Do NOT try to redirect CCCL INSIDE the CPM fetch -- bypass the whole rapids-cmake bootstrap with a small standalone HIP CMakeLists reached by a top-of-file `option(USE_HIP) ... if(USE_HIP) include(cmake/hip/<proj>_hip.cmake) return() endif()` (NVIDIA path below untouched, byte-for-byte). CCCL is satisfied by INCLUDE PATHS, no fetch: Thrust->rocThrust and CUB->hipCUB are header drop-ins already on hipcc's default path (/opt/rocm/include); libcudacxx's cuda::std AND cuda::mr come from VENDORED ROCm/libhipcxx (-I<clone>/include). Two activation requirements for the cuda::mr memory-resource concepts: the -I AND `-DLIBCUDACXX_ENABLE_EXPERIMENTAL_MEMORY_RESOURCE` (the headers #error without it, and it is what exposes <cuda/memory_resource>'s cuda::mr::resource_ref/device_accessible/forward_property/has_property). The standalone CMake must regenerate the headers rapids-cmake would (e.g. version_config.hpp, and logger_macros.hpp via rapids-logger's own create_logger_macros.cmake template) -- rmm validated this way: 27 gtest exes / 658 cases / 100% on gfx90a -- rmm
- 2026-05-30 -- rocThrust execution-policy namespace delta (refines the cupoch thrust::hip::par lesson with the exact CCCL-2.x spellings RAPIDS uses): a CUDA project's thrust exec policy uses thrust::cuda::par[_nosync] + thrust::cuda_cub::execute_on_stream[_nosync]_base and includes <thrust/system/cuda/execution_policy.h>; that CUDA-backend header HARD-FAILS on ROCm ("cub/detail/detect_cuda_runtime.cuh not found") so you must swap the include to <thrust/system/hip/execution_policy.h>, and rocThrust spells the symbols thrust::hip::par[_nosync] + thrust::hip_rocprim::execute_on_stream[_nosync]_base. Cleanest minimal-diff fix that keeps the CUDA path byte-identical: a USE_HIP-guarded include swap + two backend namespace ALIASES (namespace be=thrust::hip / be_cub=thrust::hip_rocprim on HIP, =thrust::cuda / =thrust::cuda_cub on CUDA) so the policy class BODIES are unchanged -- rmm
- 2026-05-30 -- rapids_logger (the RAPIDS logging dep, fetched by rapids-cmake) is cleanly portable: its PUBLIC header rapids_logger/logger.hpp hides spdlog entirely (printf-style formatting + opaque sink wrappers), so ONLY its src/logger.cpp pulls spdlog. Build it as a tiny lib from that one .cpp linking spdlog::spdlog (conda-forge spdlog+fmt), expose its include/, and generate the consumer's logger_macros.hpp by include()-ing the clone's own cmake/create_logger_macros.cmake and calling create_logger_macros(<PREFIX> "<ns>::default_logger()" include/<dir>). No HIP content in it at all -- rmm
- 2026-05-30 -- HIP-vs-CUDA stream robustness diff that fails a DEATH test (not a real bug): hipStreamSynchronize on a stream handle ALREADY destroyed by hipStreamDestroy returns hipSuccess on gfx90a (HIP tolerates the freed handle), whereas CUDA errors and trips an assert()-based check. A negative/death test that destroys a stream then expects synchronize_no_throw() to abort the process (EXPECT_DEATH) therefore does NOT die on HIP. The no-throw contract itself still holds; USE_HIP-guard out just that one death test with a comment. Same family as the MPPI hipFFT "garbage plan bus-errors instead of returning INVALID_PLAN" robustness diff. Probe with a 6-line create/destroy/sync-after-destroy program -- rmm
- 2026-05-30 -- for a header-heavy ROCm base lib that VENDORS libhipcxx, install the libhipcxx include/ INTO the lib's own install prefix (install(DIRECTORY <clone>/include/ DESTINATION include)) and make the lib's hip_compat forwarding shims (cuda_runtime_api.h etc.) PUBLIC+installed (include/<proj>/hip_compat) with an $<INSTALL_INTERFACE:...> include dir, plus a $<INSTALL_INTERFACE:> HIP force-include of the INSTALLED compat header (build TUs use $<BUILD_INTERFACE:> the source copy). Then a dependent consumes it with ONLY find_package(<proj>) + link <proj>::<proj> + CMAKE_PREFIX_PATH=<install> -- the exported target propagates the cuda::std/cuda::mr includes, the USE_HIP/LIBCUDACXX_ENABLE_EXPERIMENTAL_MEMORY_RESOURCE defs, the compat force-include, and hip::host. Verify by building a standalone consumer against ONLY the install tree and running it on GPU (caught a missing-shim-in-install bug that the in-tree test build masked) -- rmm
- 2026-05-30 -- RAPIDS-on-ROCm FEASIBILITY TRIAGE, do this BEFORE any code (cudf, applies to cugraph/cuml too): the libcudf "Arrow wall" is a MYTH at v25.08 -- `cpp/cmake/thirdparty/get_arrow.cmake` exists but is DEAD (the main cpp/CMakeLists.txt never include()s it; grep for get_arrow/find_package(Arrow) = nothing). Interop is on **nanoarrow** (apache/arrow-nanoarrow, a tiny standalone C lib vendored + namespaced `cudf`, no CUDA, builds with plain CMake) -- satisfiable. The REAL blockers, in order: (1) **cuCollections** -- NVIDIA/cuCollections, a CCCL-native concurrent hashtable (static_set/static_map/static_multimap on `cuda::atomic` thread-scoped RMW + cooperative probing + `cuda::std`), with **NO ROCm fork** (github.com/ROCm/cuCollections = repo not found). cudf pulls it into join/groupby/hash/stream_compaction/search/reductions (23 core files) -- i.e. every interesting algorithm -- so porting cuco to HIP is a PREREQUISITE sub-project (its own MOAT target), not inline work. (2) **nvCOMP** -- NVIDIA PROPRIETARY binary (`rapids_cpm_nvcomp USE_PROPRIETARY_BINARY`), no AMD build; but it is ENTIRELY inside `src/io/` (0 includes outside io/), so it is a cuIO blocker, NOT a core blocker -- disabling cuIO removes it + KvikIO (GPUDirect/cuFile, also io-only) cleanly. (3) jitify/NVRTC (binaryop/transform/rolling JIT). Decision rule: a RAPIDS lib that pulls cuCollections cannot reach a meaningful GPU-validated core until cuco is ported first -- set-blocked on "cuCollections (no ROCm fork)" rather than burning the window. Quantify the wall cheaply: grep cuco/ + jitify + nvcomp + kvikio reach, and probe-compile the SINGLE smallest core TU -- cudf 493-file monolithic add_library, the column_device_view.cu probe alone already fails -- cudf
- 2026-05-30 -- cudf's own host/device macro `CUDF_HOST_DEVICE` (cpp/include/cudf/types.hpp:19) and `CUDF_KERNEL` are gated `#ifdef __CUDACC__`, so under hipcc (defines __HIPCC__, NOT __CUDACC__) they expand to EMPTY -> every `__host__ __device__` member (e.g. all string_view ctors, reached transitively from column_device_view) becomes host-only -> "call to __host__ function from __device__ function" across 71 headers. This is the cudakdtree/MPPI __CUDACC__-undefined family at RAPIDS scale (46 `__CUDACC__`/`__CUDA_ARCH__` guard sites in 22 headers). CAUTION: the blanket `#define __CUDACC__ 1` workaround CASCADES -- it then steers type_dispatcher.hpp base_type_to_id specializations down a CUDA template path that no longer matches ("no function template matches", ~20 errors), confirming the MPPI lesson that defining __CUDACC__/__CUDA_ARCH__ for HIP needs per-site `||__HIPCC__` / `||__HIP_DEVICE_COMPILE__` guards, not a global define. Probe: agent_space/cudf_probe/ -- cudf
- 2026-05-30 -- raft (2nd RAPIDS base, depends on rmm) ports Strategy A like rmm: same USE_HIP standalone-CMake bypass of the rapids-cmake/CCCL bootstrap; consumes rmm via find_package(rmm)+rmm::rmm (which force-includes rmm's compat header AND carries the vendored libhipcxx + LIBCUDACXX_ENABLE_EXPERIMENTAL_MEMORY_RESOURCE def, so a dependent needs NO separate libhipcxx flag). raft adds cuco + CUTLASS deps, but NEITHER is pulled by libraft's compiled TUs nor the core/linalg/random/util/label header subset (CUTLASS only in distance/detail/fused_distance_nn; cuco in 1 sparse file), so a meaningful validatable core builds WITHOUT porting cuco/CUTLASS -- defer those to the distance/neighbors bringup. Math libs: cuBLAS->hipBLAS, cuSOLVER->hipSOLVER, cuSPARSE->hipSPARSE, cuRAND->hipRAND, cublasLt->hipblasLt all mirror name-for-name; generate the 250+ symbol alias block from pytorch hipify cuda_to_hip_mappings filtered to the project's grep'd symbol surface (agent_space/raft_gen_compat.py) + a hand tail for what hipify omits (cusolverStatus_t/CUSOLVER_STATUS_*, syevjInfo_t, cublasLt enum constants, EIG_RANGE, SDDMMAlg_t, syevdx). Result: ~614 gtest cases on gfx90a (CORE 165, RANDOM 250, LINALG 149 incl. cuBLAS gemv/dot/axpy, UTILS 36, LABEL 14) -- raft
- 2026-05-30 -- the SINGLE highest-leverage RAPIDS-on-HIP runtime fault: a lib that gates device math on `#ifdef __CUDA_ARCH__` (raft core/math.hpp: `::cos(x)` device vs `std::cos(x)` host) silently runs the HOST branch on the GPU under hipcc (__CUDA_ARCH__ undefined) -> calls a host fn from device code -> RUNTIME COREDUMP (not a compile error; MathDevice.Cos crashed while MathHost passed). When the device branches are pure device-math (no template-dispatch cascade like cudf's type_dispatcher), the clean fix IS a global `#define __CUDA_ARCH__ 800` GUARDED to the HIP device pass only (`#if defined(__HIP_DEVICE_COMPILE__) && __HIP_DEVICE_COMPILE__ && !defined(__CUDA_ARCH__)`) in the force-included compat header -- value >=800 so `#if __CUDA_ARCH__>=530/800` half/bf16 intrinsic branches activate (HIP has ::hcos/::hsin/::hexp/::hsqrt/::hlog; ::sincos is double-only so route float to ::sincosf). It WILL activate previously-dead NVIDIA device code (raft: __dp4a, __nanosleep, mul.hi.u64 PTX, __ldcv/__stwt) -- each with a portable fallback gets `&& !defined(USE_HIP)` on its intrinsic-arch guard to take the fallback; the cache-streaming load/store intrinsics with no fallback get plain-load/store device-fn-template shims in the compat header. (Contrast cudf where the analogous global define CASCADED into template mismatch -- so this is safe only when device code is math, not dispatch.) -- raft
- 2026-05-30 -- hipSOLVER API-coverage gaps to expect porting a cuSOLVER-using RAPIDS lib (raft linalg): (a) NO generic 64-bit `cusolverDnXsyevd` (the typed hipsolverDn{S,D}syevd DO exist) -- raft has both an X-path eigDC and a typed eigDC_legacy, so route eigDC->eigDC_legacy under USE_HIP (identical eigendecomp); (b) NO cusolverSp batched-QR (csrqrsvBatched/csrqrInfo_t) -- guard those wrapper groups out (no in-tree callers of the typed eig path); (c) hipBLAS getriBatched/gels take NON-const A[]/ipiv where cuBLAS takes const -> const_cast at the HIP call site; (d) hipBLAS status enum is not 1:1 (no CUBLAS_STATUS_LICENSE_ERROR) -> USE_HIP-guard the orphan switch `case`; (e) CUSOLVERAPI export-decoration macro is undefined on hipSOLVER -> `#define CUSOLVERAPI` empty. hipBLASLt needs its own find_package(hipblaslt)+link roc::hipblaslt (separate from hipblas) or the cublasLt symbols are undefined at LINK time even when they compile -- raft
- 2026-05-30 -- raft's wave64 fixes are the canonical RAPIDS set: `WarpSize`/`warp_size()` 32->64 on `__GFX9__` (device) -- but these read 32 in the HOST pass where __GFX9__ is undefined, acceptable since raft uses them in device code; `laneId()` inline-PTX `mov %%laneid`-> `__lane_id()`; and CRUCIALLY every full-warp mask must be 64-bit because HIP's `__ballot_sync`/`__shfl_*_sync`/`__any/all_sync` static_assert `sizeof(mask)==8` and the CUDA `0xffffffffu` literal fails to COMPILE -- centralize a `WarpMask`/`RAFT_LANE_MASK_ALL` (uint64 on HIP) in the warp-primitive wrapper, fix `warp_full_mask()`->64-bit, and any direct `__ballot_sync(~0,...)`->64-bit ballot + `__popcll`. These fix COMPILE; the harder wave64 ALGORITHM-correctness bugs (bitonic sort, warp reductions, bitset/popc kernels packing 32-lane assumptions) surface only at RUNTIME (coredump on the wave64 binary) and are deferred per-suite, not blanket-fixed -- raft
- 2026-05-30 -- the MOAT cross-port dependency mechanism WORKS end-to-end and the rmm "## Install as a dependency" recipe is accurate as written: clone the ported fork into a CONSUMER-PRIVATE prefix (`_deps/<consumer>-rmm/src`, NOT shared `_deps/rmm/`, because concurrent RAPIDS ports collide on the shared path -- raft was using `_deps/raft-rmm/` at the same time), build with -DBUILD_TESTS=OFF + the libhipcxx/rapids_logger/conda flags into `_deps/<consumer>-rmm/install`, and rmm::rmm + the vendored libhipcxx (include/cuda/) + rapids_logger land in the prefix exactly as documented. A dependent then consumes it with find_package(rmm) + -DCMAKE_PREFIX_PATH=<install>;/opt/rocm;$CONDA_PREFIX. No gaps found in the rmm dependency contract -- cudf/rmm
- 2026-05-30 -- a LATENT __shared__ buffer overflow that is BENIGN on nvcc but corrupts adjacent static-shared scalars on hipcc/clang -- the cleanup-mask "INVALID16 (0xFFFF) count" fingerprint. RXMesh's remove_surplus_elements declared `__shared__ uint32_t s_patch_stash[PatchStash::stash_size]` (=64) but cleared it with `fill_n(s_patch_stash, LPHashTable::stash_size, INVALID32)` -- the WRONG length constant (LPHashTable::stash_size=128). The 256-thread block wrote INVALID32 (0xFFFFFFFF) into words 0..127 of a 64-word array, overrunning 64 words / 256 bytes past the end. nvcc happened to lay out that overrun onto dead padding; hipcc/clang placed the kernel's `__shared__ uint32_t s_num_vertices/edges/faces` scalars IMMEDIATELY after s_patch_stash (confirmed by printing &s_patch_stash, its end, and &s_num_*: end and s_num_edges/faces/vertices were contiguous at +0/+4/+8), so overflow words 64-66 clobbered all three recomputed element-count accumulators with 0xFFFFFFFF. Stored back to the uint16 PatchInfo::num_*[0] they truncate to 0xFFFF = INVALID16, and the next edit cycle's element-strided loops (`for i<num_*`) run on a ~65535 bound -> PatchSlicing runaway-spin and the cavity tests' update_host cudaMemcpy (size = 2*num_edges) faults "invalid argument". The defect was UPSTREAM (not introduced by the port) and looked exactly like a "wave64 active-bitmask stray bit"; it is not -- the Bitmask here is uniformly 32-bit-word indexed by element id (local_id/32, atomicOr(word+id/32)) and width-independent, and the count recompute uses atomicMax(v+1) with no ballot. FIX is the one-char-class change: fill the array with its OWN size constant (PatchStash::stash_size). LESSON: when a post-edit/cleanup count reads back as all-ones (0xFFFF/0xFFFFFFFF) and the value matches a sentinel a nearby fill/memset writes, suspect a static-__shared__ array cleared/filled with a sibling array's length constant -- clang's shared-memory layout differs from nvcc's, so a length bug that overruns into harmless padding on CUDA lands on a live neighbor on HIP. Confirm in 2 min by printing the array base/end and the neighbor scalar addresses, not by auditing ballot/popc -- RXMesh
- 2026-05-30 (run 2) -- fitting a CAPACITY-DRIVEN shared-memory kernel under the CDNA 64 KB/block cap, and the sticky-error-misattribution trap that makes you blame the wrong kernel: a CUDA mesh/graph editing library sizes per-block dynamic shared memory from per-patch capacities (vertex/edge/face caps -> connectivity buffers + masks + an in-shared LP hashtable). On NVIDIA the launch opts in to 96-227 KB via cudaFuncAttributeMaxDynamicSharedMemorySize; CDNA2 gfx90a caps dynamic shared memory at a HARD 65536 B/block (sharedMemPerBlockOptin == 65536, and a func's maxDynamicSharedSizeBytes is even less once static shared is subtracted), so a kernel that asks for >64 KB is rejected at launch with "invalid argument" that no opt-in can grant. BEFORE assuming you must restructure/tile/split the kernel, MEASURE where the bytes actually go: print the per-buffer byte breakdown AND the final smem_bytes_dyn the host passes to `<<<...,smem>>>`, per launch. RXMesh's "slice_patches needs a <64 KB rewrite" was a MISDIAGNOSIS that cost a full prior pass -- slice_patches' real budget was ~23 KB and fit fine; the >64 KB (82477 B) request belonged to the SEPARATE cavity editing kernels (random_flips/collapse/tri_refine), and was not even a genuine requirement: RXMeshDynamic::update_launch_box carefully computes the CavityManager footprint (~15-34 KB here) and then UNCONDITIONALLY clobbers it with a debug leftover `launch_box.smem_bytes_dyn = 80 * 1024;` (verbatim in pristine upstream). 80 KB is harmless slack inside CUDA's opt-in but un-launchable on gfx90a. The reason it LOOKED like slice_patches: the failed editing-kernel launch leaves a STICKY cuda error that is only observed at the NEXT kernel's pre-launch cudaGetLastError (here slice_patches'), so the error surfaces attributed to an innocent later launch -- always confirm WHICH launch actually exceeded the cap (cudaFuncGetAttributes(kernel).maxDynamicSharedSizeBytes vs the requested dyn) rather than trusting where cudaGetLastError fired. FIX, minimal + NVIDIA-safe: `#if defined(__HIP_PLATFORM_AMD__)` drop the 80 KB override and use the already-computed per-capacity value (it IS the kernel's true ShmemAllocator footprint, and check_shared_memory reads the cap from cudaDeviceProp so it scales to any AMD arch -- no 65536 hardcode); `#else` keep the 80 KB line so CUDA occupancy is byte-for-byte unchanged. update_launch_box is HOST code, so this is a host-side #if on __HIP_PLATFORM_AMD__ (the same macro HIP defines host+device for AMD targets; the porter already uses it in shmem_allocator.cuh). GENERAL RULE: a `smem = <constant> * 1024;` (or any fixed-KB shared/opt-in size) in a CUDA port is a red flag on CDNA -- grep for `* 1024`, `<< 10`, and literal byte constants assigned to a launch's shared-memory size; if the constant exceeds 65536 it will silently fail on gfx90a where it was free slack on NVIDIA. Real per-capacity requirements that genuinely exceed 64 KB (RXMesh's VF-query ribbon check, ~82 KB) are the separate, legitimate case -- those you guard-and-skip (non-fatal) or actually restructure; a hardcoded over-allocation you just stop over-allocating -- RXMesh
- 2026-05-30 (run 2) -- a hardcoded test-input mesh that ships NOWHERE in the repo is a test-DATA gap, not a port failure -- don't burn the window chasing the file: RXMesh's TriangleRefinement test opens input/rocker-arm.obj, which is absent from the working tree, untracked in upstream (git ls-files shows 18 bundled .obj, rocker-arm not among them), and 404 on raw.githubusercontent. The other two editing tests use the bundled input/sphere3.obj. To VALIDATE the kernel path on hardware, repoint the test at a bundled mesh that exercises the same path at least as hard (here input/bumpy-cube.obj, ~60k lines / 227 patches >> rocker-arm), confirm pass + determinism, and DOCUMENT the substitution in notes + report rather than fetching an arbitrary large binary from the internet (brittle, unversioned, out of scope). Distinguish this cleanly from a real port blocker so the reviewer/user can drop the original asset back in if they have it -- RXMesh
- 2026-05-30 -- CCCL-native `cuda::atomic`/`cuda::atomic_ref` (the foundation of cuCollections and any lock-free GPU data structure) WORK on ROCm via vendored ROCm/libhipcxx: `cuda::atomic_ref<T, cuda::thread_scope_device>` with explicit memory orders + compare_exchange_strong/fetch_add/load/store codegens correctly on gfx90a for 4- and 8-byte T (libhipcxx forwards to the clang `__hip_atomic_*` builtins; thread_scope_device->__HIP_MEMORY_SCOPE_AGENT, _block->WORKGROUP, _system->SYSTEM, in cuda/std/__atomic/functions/atomic_hip_generated.h). Probe a minimal slot-claim CAS + contended fetch_add + acquire/release EARLY (agent_space/cuco_probe/atomic_probe.cu) -- this is the single biggest risk for a CCCL-atomics port and it passed, de-risking the whole thing. CRITICAL GAP: sub-word (1-byte int8 / 2-byte int16) atomic CAS is BROKEN on gfx90a/CDNA2 -- libhipcxx does NOT emulate it (NVIDIA libcu++ does a containing-word CAS loop; libhipcxx forwards the narrow type straight to __hip_atomic_compare_exchange_*, which RMWs at 32-bit-word granularity and corrupts the sibling sub-word slots packed in the same word). Sweep proves it: int8 claims 64/256 distinct slots, int16 128/256, int32 256/256, int64 256/256 (agent_space/cuco_probe/atomic_size_sweep.cu). So a concurrent hashtable with int8/int16 KEYS silently corrupts on AMD; keys >=4 bytes are correct. cudf hashes to 32/64-bit keys so its core is unaffected -- scope out the small-key test cases (Catch2 `"~*int8_t*" "~*int16_t*"`) and document, don't treat as a port defect -- cuCollections / libhipcxx
- 2026-05-30 -- HIP cooperative-groups vs CUDA, the concurrent-DS-relevant deltas (cuco probes cooperatively via cg::tiled_partition<CGSize>): (1) HIP's thread_block_tile<N> IS correctly tile-relative on wave64 -- ballot()/any()/all()/shfl()/shfl_xor() use the tile size as width and `adjust_mask` compacts active lanes to bits [0,N), so a CUDA kernel's `__ffs(tile.ballot(pred))` lane-election is wave64-correct UNCHANGED for tiles <=32 (no per-site edit, unlike raw `__ballot(0xffffffff)` code). (2) BUT HIP tile.ballot() returns `unsigned long long` where CUDA returns `unsigned int`, so `__ffs(ballot)` is AMBIGUOUS between HIP's __ffs(int)/__ffs(unsigned) -- add one `__ffs(unsigned long long)->__ffsll` overload in the compat header (correct since the tile-relative bits live in the low 32 for <=32-lane tiles). (3) HIP tile.shfl(T) only overloads arithmetic T; a CUDA kernel that shfls a scoped enum (cuco insert_result : int8_t) needs an enum-constrained `__shfl(E)` shim that round-trips through int. (4) HIP CG (ROCm 7.2) has NO cg::reduce and NO cg::plus/greater/less (cf. gsplat) -- supply a `<cooperative_groups/reduce.h>` shim implementing reduce as a tile shfl_xor butterfly (wave64-correct, the tile owns its lane span). (5) `<cooperative_groups.h>`+`<cooperative_groups/reduce.h>` map to `<hip/hip_cooperative_groups.h>`. All four fixes live once in the force-included compat header / shim dir; call sites untouched, CUDA byte-identical. Determinism check (two fixed-seed runs bit-identical) confirms the wave64 CG port -- cuCollections
- 2026-05-30 -- vendored ROCm/libhipcxx (amd-develop fa4ccc6) provides MORE than its README "Limitations" table admits: `cuda/atomic`, `cuda/atomic_ref`, `cuda/barrier`, `cuda/std/atomic`, `<nv/target>`, and CCCL_VERSION 3000002 are all present and usable (the README still lists barrier/latch/semaphore as unported). Check the clone's actual include/ tree, don't trust the table. STILL genuinely missing/broken: (a) the CCCL-3.x `cuda::` fancy-iterator namespace -- `<cuda/iterator>` (cuda::counting/constant/transform/discard_iterator) and `<cuda/utility>` (cuda::static_for) are absent; supply shims aliasing to rocThrust iterators and a 5-line integer_sequence static_for. (b) `NV_DISPATCH_TARGET(NV_IS_HOST, (... multi-statement ...))` from libhipcxx's `<nv/target>` MIS-PARSES a parenthesized multi-statement host block ("type name does not allow constexpr specifier") where CCCL's nv/target expands it fine -- blocks any header using the NV_DISPATCH_TARGET host/device split (cuco bloom_filter). (c) sub-word atomics (see the cuda::atomic lesson). cuco's `detail/__config` NVCC-gate (`__CUDACC_VER_MAJOR__`/`__CUDACC_EXTENDED_LAMBDA__` #errors) is the cudf CUDF_HOST_DEVICE family -- skip under `__HIPCC__`, set CUCO_CUDA_MINIMUM_ARCH=700 (not via blanket #define __CUDACC__), and leave CUCO_HAS_CUDA_BARRIER undefined so the cuda::memcpy_async-over-barrier path takes its plain-strided-copy fallback (HIP CG has no working memcpy_async backend) -- cuCollections / libhipcxx
- 2026-05-30 -- rocPRIM CANNOT process `cuda::std::tuple` as a value/key type in DeviceSelect/DevicePartition/DeviceTransform (the wall for compacting PAIRED slots): cuco's static_map/dynamic_map `retrieve_all` filters filled key+value slots via `cub::DeviceSelect::If` over a `cuco::pair` (which is a cuda::std::tuple); rocPRIM's `partition_scatter` does `get<0>(output)[...] = scatter_storage[i]` where scatter_storage is a `raw_storage<cuda::std::tuple<...>>` and the assign fails ("no viable operator=", plus "no type named value_type in std::iterator_traits<cuda::std::tuple<...>>" for the DeviceTransform path). hipCUB forwards DeviceSelect/Transform to rocPRIM, which assumes thrust::tuple-shaped values, not cuda::std::tuple. static_SET is unaffected (scalar key slot -> retrieve_all works); only the pair-slot map ops break, AND only at the retrieve_all/select call -- insert/find/count/contains/erase/for_each over pairs are fine (they don't go through DeviceSelect). Scope out the retrieve_all-over-pairs TUs and document; the map CORE still validates. Also pre-Volta-class atomic limits recur on CDNA: a >8-byte non-packable map slot's insert_and_find spin-waits on the payload (needs ITS, which CDNA lacks -> cuco's `#if __CUDA_ARCH__<700` static_assert correctly fires; __CUDA_ARCH__ is UNDEFINED on HIP so `<700` is true, the right answer here), and a 16-byte composite key lowers to LLVM "unsupported cmpxchg" on gfx90a (128-bit atomic CAS is sm_90+ only) -- cuCollections
- 2026-05-30 -- Catch2 + clang `TEMPLATE_TEST_CASE_SIG` "call to 'get_wrapper' is ambiguous": Catch2's SIG macro emits two get_wrapper overloads (a variadic `TypeList<Ts...>` and the signature's `Nttp<...>`) that clang rejects as ambiguous where nvcc/gcc/MSVC pick the more specialized -- it fires on ANY hipcc/clang build of a project using TEMPLATE_TEST_CASE_SIG (cuco's whole test suite), single- AND multi-type-param forms. Catch2 v3.3.0..v3.8.1 all fail; the fix landed on Catch2 `devel` (priority_tag<1>/<0> disambiguation in catch_preprocessor.hpp's get_wrapper). FIX: build the tests against a Catch2 clone at/after that fix (devel tip validated). Test-HARNESS issue, orthogonal to the port. Other rocThrust/clang test-only strictness fixes that recur: use `thrust::get` not `cuda::std::get` on a zip-iterator deref (rocThrust derefs to thrust::tuple_of_iterator_references); pass iterators variadically to `thrust::make_zip_iterator(a,b)` not `make_zip_iterator(cuda::std::tuple{a,b})` (rocThrust's tuple-overload wants thrust::tuple); paren-init `cuco::pair<K,V>(a,b)` not brace-init (clang -Wc++11-narrowing); `obj.template method<N>()` on a dependent type -- cuCollections
- 2026-05-30 -- sub-word (int8/int16) atomic compare-exchange is BROKEN on CDNA2 (gfx90a) under libhipcxx: `cuda::atomic_ref<int8_t|int16_t>::compare_exchange_strong` forwards straight to `__hip_atomic_compare_exchange_*`, which RMWs the whole containing 32-bit word and clobbers the sibling sub-word slots packed in that word (NVIDIA libcu++ emulates this; libhipcxx does NOT). Symptom: a contended slot-claim over a packed 1-byte array claims only 64/256 distinct slots (4 slots/word collide), 2-byte claims 128/256; e.g. cuco static_set/static_map with int8/int16 KEYS silently lose insertions. FIX: emulate with the PyTorch aligned-word CAS-loop technique (`ATen/cuda/Atomic.cuh` `AtomicIntegerImpl<T,1>`/`<T,2>`) adapted from RMW to compare-exchange -- locate the containing word (`off = addr & 3` for 1B, `& 2` for 2B; `w = (uint32_t*)(addr - off)`; `shift = off*8`), read+compare the sub-field, splice the desired value in, `atomicCAS` the whole word, and on failure re-read the word and retry while OUR slot still matches `expected` (the retry serializes concurrent writers to the other sub-word slots in the word, so neighbors are never lost). Mask/extract with the unsigned width and cast back to T for signedness. Guard the emulation with `#if defined(__HIP_DEVICE_COMPILE__)` AND `sizeof(T) < 4` so the NVIDIA path and all >= 4-byte slots forward verbatim to `cuda::atomic_ref` -- zero impact on native-hardware atomics. Route it in via a small `cuco::detail::atomic_compare_exchange_relaxed<Scope>(obj, expected, desired)` free function at the few relaxed key/slot CAS sites; do NOT monkeypatch libhipcxx's atomic_ref. >= 4-byte and 8-byte CAS work natively on CDNA2; only 1-/2-byte are broken -- cuCollections
- 2026-05-31 -- wave64 host/device WarpSize SPLIT is a silent-corruption + coredump trap distinct from the lane-mask class: a per-arch warp-size constant keyed on `#if defined(__GFX9__)` (raft WarpSize=64 / warp_size()=64) is correct ONLY in the device pass -- `__GFX9__` (and every gfxNNN macro) is UNDEFINED in hipcc's HOST compile pass, so the host falls back to 32. A kernel that computes its LAUNCH GEOMETRY on the host from that constant (block_dim, dynamic-smem bytes, per-warp data partition, num_of_warp) then disagrees with the 64-lane device kernel -> small inputs GPU-fault/coredump, large inputs silently mis-partition (wrong top-k). raft matrix/detail/select_warpsort is the case: block_dim = num_of_warp * min(capacity, WarpSize), smem from num_of_warp, all host-side. FIX: the per-arch HIP build knows the target, so derive a host warp size from CMAKE_HIP_ARCHITECTURES (gfx9* -> 64, gfx10*/11*/12* -> 32) and inject it as a compile def (RAFT_HOST_WARP_SIZE); have the warp-size constant use it in the host branch (`#elif defined(__HIP_DEVICE_COMPILE__)` keeps the RDNA device value, `#elif defined(<the def)` for the host pass, else fall back to 64 = the CDNA/safe-upper-bound) so host==device for a single-arch build. A follower passes its own arch so no source edit. DIAGNOSTIC fingerprint: a top-k/segmented kernel that PASSES for len>=warp but coredumps for tiny len, and a host-printed `WarpSize` reading 32 on a gfx90a build -- print the constant from HOST code to catch it in 30s. Distinct from the lane-mask/ballot class (which is purely device-side) -- raft, cuvs select_k
- 2026-05-31 -- wave64 ballot RETURN-TYPE truncation (compiles clean, wrong at runtime): a `DI uint32_t ballot(bool){ return __ballot_sync(mask, pred); }` wrapper truncates a 64-lane wavefront's ballot to 32 bits -- lanes 32-63 are silently dropped, so `__popc(mask)` undercounts and `__ffs(mask)`/`mask & ((1<<lane)-1)` address the wrong lanes. The CUDA literal 32-bit ballot type survives compilation on HIP (the value just gets zero-extended), so only a runtime wrong-answer reveals it. FIX: make ballot() return the 64-bit WarpMask type on HIP, and give callers width-correct helpers -- `popc_mask`(__popcll on HIP), `ffs_mask`(__ffsll), `lane_bit(l)`=`WarpMask{1}<<l` -- then switch `uint32_t mask` -> `WarpMask mask`, `__popc`->popc_mask, `__ffs`->ffs_mask, `1u<<lane`->lane_bit at the ballot consumers (raft select_warpsort's two add() loops). CUDA stays 32-bit/`__popc`. Pairs with the host/device WarpSize fix above -- both are needed for warp-bitonic top-k on CDNA -- raft, cuvs select_k
- 2026-05-31 -- faiss-derived warp-bitonic single-warp sort (raft neighbors/detail/faiss_select MergeNetworkWarp) hardcodes `static_assert(WarpSize==32)` then a fixed 5-step merge sequence (`warpBitonicMergeLE16<1,2,4,8,16>`) that sorts exactly 32 lane-elements. On wave64 it must sort 64: add one stride-32 merge `warpBitonicMergeLE16<...,32,...>` (guarded `if constexpr(WarpSize==64)`; the merge primitive's own `static_assert(L<=WarpSize/2)` already permits L=32 at WarpSize=64). The wider FAISS BlockSelect/WarpSelect (Select.cuh) is more deeply wave32-bound -- `kNumWarpQRegisters = NumWarpQ/WarpSize` goes to 0 (zero-length register arrays) when a caller requests a warp-queue capacity of 32 on a 64-lane wavefront, plus `__any_sync(0xffffffff,...)` 32-bit masks; making it wave64-correct needs the per-call warp-queue capacity raised to >= WarpSize AND mask widening, a larger change -- so raft's OWN select_warpsort (which abstracts on WarpSize cleanly) is the reachable wave64 top-k, the FAISS path is a separate follow-up -- raft, cuvs
- 2026-05-31 -- CUTLASS distance kernels usually ship a NON-CUTLASS twin: raft's pairwise-distance + fused_L2/cosine_NN each have a CUTLASS (sm80) path AND a hand-written SIMT/sm60 path computing the IDENTICAL math (same distance_op core/epilog), selected at runtime by an arch probe. The fastest correctness-first ROCm bring-up is NOT to reimplement CUTLASS at all for the BUILD gate -- it is to (a) `#if !defined(USE_HIP)`-guard every `#include <cutlass/...>` and CUTLASS-only function body, (b) force the dispatcher to the SIMT/legacy branch on HIP (the runtime arch probe `cudaFuncGetAttributes().ptxVersion` is meaningless on HIP anyway), and (c) flip the SIMT-fallback `#if __CUDA_ARCH__ < 800` guards to ALSO compile under USE_HIP (the compat header sets __CUDA_ARCH__=800 on gfx90a, which would otherwise EMPTY the only HIP-viable kernel body). This is exactly what the project does on pre-Ampere NVIDIA GPUs, so it is faithful and low-risk; the CK/ck_tile MFMA reimplementation is then a PERFORMANCE follow-up for the large-matrix regime, not a prerequisite to a building, GPU-correct distance/neighbors. Reimplement against CK's classic DeviceGemmMultipleD_Xdl_CShuffle (stable fused `CDEElementOp(e,c,d0,d1)` epilogue = the 1:1 mapping of CUTLASS GemmUniversal+PairwiseDistanceEpilogue) where the shipped ck_tile high-level GEMM/epilogue API has drifted from its in-repo examples (true at ROCm 7.2.1). CK DeviceGemmMultipleD only supports Row-major D and a stride-0 Row D broadcasts the N-vector; the M-vector norm is materialized or the GEMM is transposed. Validated expanded-L2 (||x||^2+||y||^2-2xy, +sqrt) on gfx90a to fp32 (max_rel ~1.7e-7) -- raft, cuvs/cuml gate
- 2026-05-31 -- clang's -ffast-math is NOT a safe substitute for nvcc's --use_fast_math and SILENTLY NaNs the BACKWARD pass on gfx90a: a CUDA project whose Makefile compiles with --use_fast_math, ported by handing -ffast-math to hipcc, computed the forward loss CORRECTLY but produced all-NaN gradients (llm.c test_gpt2fp32cu: logits OK, loss OK, every grad nan, then "must forward with targets before backward" as the corrupted weights cascade). nvcc's --use_fast_math only swaps in fast intrinsics + FMA; clang's -ffast-math additionally enables -fassociative-math / -funsafe-math-optimizations / -fno-signed-zeros, which REASSOCIATE the online-softmax and layernorm-backward reductions in a way that diverges to NaN on AMD. Re-honoring nans/infinities (-fhonor-nans -fhonor-infinities) does NOT fix it -- the reassociation is the cause, not the inf/nan handling. FIX: drop -ffast-math; use `-ffp-contract=fast -fno-math-errno` (the FMA contraction that matters for perf, IEEE semantics preserved). Tell that fingerprints it: forward metric correct but backward/gradients NaN, and the NaN is masked by the test's own -ffast-math host comparison (isnan/`x<=tol` optimized away under -ffinite-math-only) so check_tensor falsely prints "OK <nan>"; rebuild the host check without -ffast-math to see it honestly. Same -Ofast/-ffinite-math-only breaks isnan() in the CPU C tests (a pre-existing upstream issue, not the port) -- llm.c
- 2026-06-01 -- the #1 wave64 RUNTIME FAULT for the "two-32-thread-tiles-per-wavefront" rasterizer class (gaussian-splatting, blockDim.x==32 launched as dim3(32,N)): a DIVERGENT full-wavefront `__any_sync`. The forward blend loop `for(...; __any_sync(0xffffffff, any_active!=0); ...)` early-exits per TILE, but on wave64 two tiles (each its own per-tile loop bounds: start/end index into its sorted point list) share one 64-lane wavefront. The literal CUDA 32-bit mask first fails to COMPILE on HIP (the AutoDock `sizeof(mask)==8` static_assert), and the obvious fix -- widen to a 64-bit full mask `__any_sync(0xffffffffffffffffULL, ...)` -- COMPILES AND RUNS but FAULTS at runtime (HSA_STATUS_ERROR_EXCEPTION code 0x1016, queue abort) the instant one tile's loop ends while its wavefront-sibling's continues, because a full-wavefront `_sync` is then reached by only half the named lanes (divergent). FIX: a per-tile `tile_any(pred)` = width-32 butterfly OR (`int v=pred; for(o=16;o>0;o>>=1) v|=__shfl_xor(v,o,32); return v!=0`) confined to the 32-lane tile group -- `__shfl_xor` is not a `_sync` op (no convergence requirement) and width=32 keeps each tile reading only its own all-active lanes, so it never diverges across tiles. DIAGNOSE the faulting kernel with `AMD_SERIALIZE_KERNEL=3 AMD_LOG_LEVEL=3` and read the last `ShaderName` before "aborting with error" -- it names the exact kernel+template-args. This is distinct from the popsift leader-election double-fire (a wrong-COUNT bug) and the unsynced-warp-tail race (a nondeterminism bug): here it is a hard memory/exec fault from divergent lane participation in a `_sync` primitive -- LiteGS
- 2026-06-01 -- HIP 7.2 DOES ship the sm_80 warp-redux intrinsics `__reduce_add_sync`/`__reduce_max_sync` (amd_warp_sync_functions.h, gated `-DHIP_ENABLE_WARP_SYNC_BUILTINS=1` which torch's CUDAExtension sets) -- but they reduce over the WHOLE 64-lane wavefront. For the two-tiles-per-wavefront rasterizer the reduction must be PER-TILE (each tile's 32 lanes do `if(threadIdx.x==0) atomicAdd`), so the full-wavefront intrinsic folds in the adjacent tile. Do NOT shim the intrinsic NAME (it collides with HIP's existing definition -> redefinition/overload errors); instead REPLACE the call sites with a renamed width-32 helper (`tile_warp_reduce_add<T>`/`tile_warp_reduce_max` = `__shfl_xor` butterfly all-reduce, width 32) under USE_ROCM, with a CUDA fallback that calls the real `__reduce_*_sync(0xffffffff,v)`. The butterfly preserves an integer reduction bit-exactly (the rasterizer rescales floats to int, reduces the int domain to dodge float-add nonassociativity, rescales back -- int add IS associative so the per-tile butterfly is deterministic). HIP's `__shfl_down`/`__shfl`/`__shfl_xor` correctly clamp to the `width`-lane aligned sub-group, so width=32 stays inside the tile on a 64-lane wavefront with no math change (strategy 3a: keep blockDim.x=32, force width-32; no need to re-derive the tile mapping for 64 lanes) -- LiteGS
- 2026-06-01 -- half2-heavy CUDA kernels (gaussian-splatting alpha-blend) need TWO extra HIP enablements beyond the usual hipify. (1) torch's CUDAExtension prepends `-D__HIP_NO_HALF_OPERATORS__=1 -D__HIP_NO_HALF_CONVERSIONS__=1` (COMMON_HIPCC_FLAGS) which disable HIP's half2(float,float) ctors and half2 +=/* operators -- a project's `remove_unwanted_pytorch_nvcc_flags()`-style helper that only strips the CUDA-named `-D__CUDA_NO_HALF_*` from COMMON_NVCC_FLAGS must ALSO strip the HIP-named pair from `torch.utils.cpp_extension.COMMON_HIPCC_FLAGS` (import it, list.remove the two, under `if torch.version.hip`), or every half2 ctor/operator fails to compile. (2) HIP `<hip/hip_fp16.h>` provides `__hge2/__hle2/__hgt2` (returning a half2 of 1.0/0.0), `h2rcp`, `h2exp2`, `__hmin` -- but NOT the `__h*2_mask` forms (CUDA's return a 32-bit word, 0xFFFF per passing half-lane), NOT `__hmin2`, and NOT the SIMD `__vcmpleu2`. Shim them USE_ROCM-guarded: `__h*2_mask(a,b)` = synthesize the 0xFFFF-per-half-lane mask from `__h*2(a,b)` (`__low2half(cmp)!=half(0)?0xFFFF:0` | `__high2half ...<<16`) so the downstream `reinterpret_cast<unsigned*>(&x)[0] &= mask` and `count += (0x00010001u & mask)` bit-masking match CUDA bit-for-bit; `__vcmpleu2(a,b)` = per-16-bit unsigned `<=` -> 0xFFFF; `__hmin2(a,b)` = per-half `__hlt ? a : b` repacked via `__halves2half2`. Inline PTX `asm("ex2.approx.f16x2 ...")` (hipcc cannot assemble PTX) -> the HIP intrinsic `h2exp2` (the code already pre-scales by log2(e)) -- LiteGS
- 2026-06-01 -- a MUSA-vendor (MooreThreads) CUDA codebase reformats EVERY kernel launch with spaces inside the triple-angle-brackets: `kernel << <grid, block >> > (...)` instead of `kernel<<<grid,block>>>(...)`. nvcc tolerates the whitespace; clang-HIP's parser does NOT -- it parses `kernel << <` as `(kernel) << (<expr)` and errors "expected expression" at the `<` (cols of the `<<` and `>>`). It is NOT a __global__/overload problem and fires even on a non-overloaded kernel. FIX: normalize `<< <`->`<<<` and `>> >`->`>>>` (correctness-neutral on both backends); safe via `sed 's/ << </<<</g; s/ >> >/>>>/g'` after grep-confirming no `>> >` appears in a real template-close/right-shift context (in a launch-only file there are none). Watch for it on any musify/musa-origin fork (LiteGS had 35 sites). Same forks also `#include "device_launch_parameters.h"` (a CUDA-toolkit IntelliSense header with no ROCm equivalent; hipify does not strip it -> "file not found") -- USE_ROCM-guard the include out; the threadIdx/blockIdx symbols it declares are hipcc intrinsics -- LiteGS
- 2026-06-01 -- a PyTorch CUDAExtension whose setup.py does `package_dir={'pkg':'.'}` + an empty `pkg/` dir (no __init__.py, the vanilla-3DGS simple-knn layout) installs CORRECTLY with `pip install <dir>` but BREAKS with `pip install -e <dir>` (editable): the editable finder registers `pkg` as a namespace package whose `_find_spec` needs `pkg/__init__.py` or `pkg.<suffix>`, neither of which exists, so `import pkg` / `from pkg._C import fn` resolve to the source dir as a namespace (file=None, 0 symbols) instead of the built `pkg.cpython-*.so`. It is a packaging quirk identical on CUDA, not a port bug. For GPU validation install NON-EDITABLE (copies the .so into site-packages and resolves cleanly), or put the build dir on sys.path. Tell: `import pkg; print(pkg.__file__)` is None and `dir(pkg)` has no kernels though the .so built fine -- LiteGS
- 2026-06-01 -- gaussian-splatting / 3DGS rasterizer-backward determinism gate: do NOT require bitwise-identical gradients across runs. The backward scatters per-primitive gradients via `atomicAdd` across the tiles that touch each primitive, and atomicAdd float ordering is nondeterministic on EVERY GPU (CUDA included), so grads differ run-to-run at fp32/fp16 ULP. The correct bar (cf. GPUMD/3DUNDERWORLD atomic-order lessons) is: (a) FORWARD bitwise-deterministic (it has no cross-tile atomic -- a forward nondeterminism WOULD indicate a real wave64 race), (b) backward grads FINITE, (c) order-INDEPENDENT quantities (the grad SUM per tensor) stable to ~1e-9, and (d) the grad VALUES correct vs a central finite-difference. Measured on LiteGS gfx90a: forward bit-identical, backward run-to-run rel spread ~1e-7 (only 2-5% of elements differ -- the ones with multi-tile contribution), grad sums stable to 1e-9, FD slope 1.000 / 100% sign agreement. CAUTION doing the FD: the rasterizer blends internally in fp16 (a SCALER=128 fixed-point trick), so its gradients are ~fp16-precision -- a finite-difference check needs a COARSE eps (~5e-2) and must target the LARGEST-|grad| entries, or fp16 round-off swamps the signal and a correct gradient falsely "fails" at 5% on near-zero entries -- LiteGS
- 2026-06-01 -- a fused-kernel-vs-pure-pytorch test harness (LiteGS litegs.utils.wrapper, the gsplat/fused-ssim model) can carry UPSTREAM failures that are NOT port defects and fail identically on CUDA -- triage each before treating a wrapper miss as a wave64 bug. Classes seen: (1) the fused kernel INTENTIONALLY does not compute an output the script reference does -- LiteGS's sh2rgb_backward leaves `dir_grad = zeros_like(dir)` with every direction-gradient write commented out (3DGS does not backprop to view directions), so the wrapper's autograd dir_grad reference can never match; the gradients the kernel DOES compute (sh_base/sh_rest) are exact. (2) eigendecomposition eigenVECTOR sign/order ambiguity -- `torch.linalg.eigh` and a fused 2x2 eigh pick opposite eigenvector signs (a `max|diff|==2.0` == |-1-1| tell), mathematically valid; compare `|eigvec|` (matches ~1e-5) or the reconstruction, not raw signed vectors. (3) ill-conditioned reference inputs -- a wrapper that feeds `randn*10` 2x2 matrices makes the inverse/eigenvalue legitimately sensitive; the failures concentrate at near-singular |det| and the fused inverse is still a TRUE inverse (`||A@Ainv-I||` ~6e-8 median, verified directly). (4) test_inputs lists that do not match the fused function signature (arg-count TypeError). Verify the kernel by an INDEPENDENT oracle (inverse residual, FD, reconstruction) + run-to-run determinism, not solely by the upstream wrapper's elementwise threshold -- LiteGS
- 2026-05-31 -- a compile-time-constant warp size that feeds BOTH host launch geometry AND device code must be ONE value per build, derived from the target arch by the BUILD, not keyed on __GFX9__: llm.c's `#define WARP_SIZE` is used as a host `dim3(WARP_SIZE, block_y)` launch dim AND as a device reduction/shared-size constant. HIP compiles each .cu in two passes and defines __GFX9__/__gfx90a__ ONLY in the DEVICE pass (confirmed via #pragma message: GFX9 defined at TU scope on the device pass, NOT defined on the host pass), so a `#if defined(__GFX9__)` WARP_SIZE is 64 on device but 32 on host for gfx90a -- the kernel expects 64-lane warps while the launch ships dim3(32,...), wrong geometry. FIX: the Makefile derives the wavefront width from the single target arch (`$(filter gfx9%,$(AMDGPU_TARGETS))` => 64 else 32) and -D-defines it (LLMC_WARP_SIZE) for both passes; the source uses that. This is correct because the project builds one arch at a time (a compile-time-constant warp size in a host launch dim is fundamentally impossible for a multi-arch fat binary anyway). Generalizes the AutoDock-GPU "grep host AND device for warp constants" lesson to the case where the constant must stay a compile-time literal -- llm.c
- 2026-05-31 -- BF16 GPU correctness MUST be validated by tolerance + loss-curve, never against the project's NVIDIA-bitwise-tuned per-tensor thresholds: llm.c's test_gpt2cu hardcodes grad thresholds (e.g. 4e-2 for ln1b) tuned so CUDA BF16 passes; on gfx90a wave64 two layernorm-BIAS gradients exceeded them on their single worst NEAR-ZERO element (worst 0.063 vs ref 0.011) while 14/16 tensors passed. This is the EXPECTED determinism shift (the wave64 FP32-accumulation order + the WARP_SIZE-seeded stochastic-rounding RNG differ from NVIDIA), NOT a bug. How to PROVE it is rounding and not a wave64 reduction error before dismissing it: (a) build the SAME kernels in FP32 precision -- if they agree with the reference to ~1e-4 (and the test's own 1e-6 FP32 threshold "fails" every tensor by pure reduction-order noise), the algorithm is correct; (b) the failing values are bit-identical run-to-run and INVARIANT under larger batch (-b 32) -- a real reduction-coverage bug would scale with B*T; (c) the authoritative gates pass: FP32 test overall-okay, the FP32 loss_checker_ci.py within 0.05%, and the BF16 tinyshakespeare smoke (loss decreases, val loss decreases, coherent generation, no NaN). A real wave64 reduction bug looks completely different: all-NaN, or 100%+ relative error on LARGE elements, or values that move with batch size -- llm.c
- 2026-05-31 -- do NOT `#define __CUDACC__` to satisfy a CUDA project's device-vs-host guards on HIP: rocThrust/rocPRIM key their BACKEND selection on it (`thrust/detail/config/compiler.h`: `#if defined(__CUDACC__) -> THRUST_DEVICE_COMPILER_CUDA`), so a faked __CUDACC__ pins rocThrust to its CUDA backend even with THRUST_DEVICE_SYSTEM unset, and EVERY thrust algorithm then falls into `thrust/system/detail/generic/for_each.h` whose `static_assert(depend_on_instantiation<...>::value, "unimplemented for this system")` fires (also "no member terminate_with_message in thrust::system::hip::detail", and `<cub/detail/detect_cuda_runtime.cuh> not found`). Isolated repro: a 3-line thrust::transform over a counting_iterator compiles under plain hipcc but fails the instant you add `-D__CUDACC__=1`. FIX: (a) leave __CUDACC__ undefined; extend each Open3D/project `#ifdef __CUDACC__` device guard to `defined(__CUDACC__) || defined(__HIPCC__)` at the site (mechanical sed over the kernel headers), and `#ifndef __CUDACC__` host fallbacks to `!defined(__CUDACC__) && !defined(__HIPCC__)`; (b) force `-DTHRUST_DEVICE_SYSTEM=5` (=THRUST_DEVICE_SYSTEM_HIP, the value from thrust/detail/config/device_system.h) as a compile def on every TU (host .cpp include thrust too) so rocThrust's auto-detect is bypassed regardless; (c) for the few `#if defined(__CUDA_ARCH__)` device-data-pointer / `#pragma unroll` guards, supply a project macro `= __HIP_DEVICE_COMPILE__` instead of __CUDA_ARCH__. This is the opposite of the MPPI/cudaKDTree "define __CUDACC__ for header-only libs" lesson -- that works when the project does NOT pull rocThrust into the same TU; a project that uses BOTH rocThrust AND __CUDACC__-gated device code (Open3D core) must take the extend-the-guards route -- Open3D
- 2026-05-31 -- stdgpu's experimental HIP backend (STDGPU_BACKEND_HIP, the default Open3D hashmap) BUILDS and its non-colliding ops are correct, but its concurrent duplicate-key DEDUP is not wave64-correct: `unordered_base::try_insert` guards a slot with a per-bucket `mutex::try_lock()` (atomic_ref-backed) + a `contains()` recheck + an occupied-bitset, and on a 64-lane wavefront two threads inserting the SAME key both report success (the lock/occupied memory ordering does not serialize same-key lanes the way CUDA SIMT does). Symptom: insert mask popcount is 1 too high for a batch with one duplicate (1024 vs 1023), and an off-by-one after Reserve()/rehash; basic Insert/Find/Erase/Clear/IO/multivalue-on-distinct-keys all PASS. This is an UPSTREAM stdgpu bug (vendored ExternalProject), not the consumer's kernel. Build-bringup fixes that DO work for the HIP backend at the pinned stdgpu commit (d7c07d0) / ROCm 7.2.1: pass CMAKE_CXX_COMPILER=hipcc (its host impl/*.cpp reach rocPRIM, clang-only) with the compiler overrides placed AFTER the parent's ExternalProject_CMAKE_ARGS (which set CXX=host g++), and PATCH_COMMAND-copy cmake/cuda/determine_thrust_paths.cmake into cmake/hip/ (the HIP dir omits it but install() references it). The newer commit's bundled cmake/hip/Findthrust.cmake already finds rocThrust, so the cupoch stdgpu-1.3.0 Findthrust/hip-config rot does NOT recur. Validating a stdgpu-backed hashmap on CDNA needs either a wave64 fix to stdgpu's atomic/lock dedup or a wave64 concurrent-hashtable swap (SlabHash rewrite) -- defer and document; the rest of the tensor core does not depend on it -- Open3D / stdgpu
- 2026-05-31 -- a CUDA `__shfl_*_sync(0xffffffff, ...)` warp-collective reached after a divergent early-`return` becomes a HARD ABORT on HIP (non-NDEBUG) and the fix is `__activemask()`, not the blanket all-ones mask. HIP's `__shfl_*_sync`/`__ballot_sync`/`__any_sync` macro-assert `mask == __ballot(true)` (the live-lane set); a kernel that returns out-of-range lanes BEFORE a warp reduction (raft `linalg::coalescedReduction`'s thin policy `return`s rows >= N before its logicalWarpReduce butterfly) leaves a partial wavefront, so passing the full mask trips the assert -> SIGABRT (in a release/NDEBUG build it instead reads undefined lanes). Tell that fingerprints it: aborts ONLY for certain shapes (here any ODD row count with D>=128, the rpw=1 thin policy) while even/other shapes pass, and the backtrace ends in `__hip_check_mask` inside `__shfl_xor_sync`. FIX (arch-unified, CUDA byte-identical): a `active_mask()` helper returning `__activemask()` on HIP and the project's full-mask constant on CUDA, passed as the shuffle mask in the reduction primitive; each still-active logical warp keeps all its lanes so results are unchanged (verify: reduction vs CPU ref + the project's reduce/norm suite). This is the RELEASE-build analogue of the ball_cover FAISS reconvergence note -- same root (AMD does not implicitly reconverge a divergent region before a collective as Volta does), but for a generic logical-warp reduction the live-mask is the clean fix; only hand-rolled bitonic networks need an explicit wave_barrier. NVIDIA tolerates the stale full mask, which is why upstream ships it -- raft
- 2026-05-31 -- wiring a CK kernel INTO an existing CUDA dispatch (vs a standalone proof): gate it behind the SAME alignment/dtype dispatch the CUDA code already does and keep the project's non-CUTLASS SIMT twin as the fallback, then validate through the PUBLIC API. raft's expanded L2/cosine: a HIP-only `dispatch_ck.cuh` (the CK `DeviceGemmMultipleD_Xdl_CShuffle` + a fused CDE op that REPRODUCES the project's own `*_cutlass_op` epilogue arithmetic byte-for-byte -- incl. the L2 self-neighbor round-off clamp, the sqrt variant, and applying `fin_op(distance,0)` exactly as the SIMT store does) is called from `pairwise_matrix_dispatch` only when a runtime `*_ck_can_dispatch(params)` says fp32 + row-major + N%4==0 + K%4==0 (the tuned CShuffle/A/B vector width); every other shape falls through to the SIMT/sm60 kernel. Reproducing the existing epilogue op (not a fresh formula) is what makes the CK output match the SIMT/CPU reference with no tolerance change. Validate by calling the PUBLIC entry (`raft::distance::pairwise_distance`) for both aligned (CK) and unaligned (SIMT) shapes against a double CPU reference, AND assert per-case which backend ran (call the can_dispatch gate in the test) so a silent fall-through to SIMT on an aligned shape fails. CAVEAT: a base library may have DROPPED its dense gtest to a downstream repo (raft v25.08 moved distance tests to cuVS), so author the validation test fresh rather than enabling a non-existent suite -- raft, cuvs/cuml gate
- 2026-05-31 -- rocPRIM's DeviceRadixSort REJECTS a cuda::std::tuple decomposer (the CCCL custom-radix-key API), and the fix is a REUSABLE adaptor shim, NOT a rocPRIM patch and NOT a per-project packed-key rewrite. CUB lets you radix-sort a custom struct key by passing a "decomposer" functor whose operator()(Key&) returns a tuple of lvalue references to the key's radix-sortable component fields (CCCL/cudf spell it `cuda::std::tuple<size_type&, float&>`; cudf uses it for the {nan-bias, float} stable float sort in sort/sort_radix.cu + sorted_order_radix.cu). hipCUB forwards the decomposer straight to rocprim::radix_sort_{keys,pairs}, but rocPRIM's radix codec for a NON-fundamental key only recognizes its OWN tuple: in rocprim/type_traits.hpp the `codec<Key,Descending,false>` uses `::rocprim::detail::is_tuple_of_references` / `for_each_in_tuple` / `::rocprim::tuple_size` / `::rocprim::tuple_element_t` and an internal `extract_digit_from_key_impl(const ::rocprim::tuple<Args...>&, ...)`, and in rocprim/type_traits_functions.hpp the PRIMARY `is_tuple_of_references<T>` is `static_assert(sizeof(T)==0, "is_tuple_of_references is only implemented for rocprim::tuple")` -- so a cuda::std::tuple matches none of them and the build fails before any kernel runs (same rocprim-tuple family cuCollections hits with DeviceSelect-over-cuco::pair, and raft hits too). FIX without touching rocPRIM (its codec is correct + validated): a tiny header-only decomposer ADAPTOR -- wrap the caller's decomposer so it calls it, then re-`rocprim::tie(cuda::std::get<I>(t)...)` the SAME lvalue references (index_sequence over cuda::std::tuple_size) into a `rocprim::tuple<Refs...>`; rocPRIM's existing codec then drives the sort unchanged. `rocprim::tie` is __host__ __device__ and returns tuple<Refs&...>; the references point into the LIVE key (not into the temporary cuda::std::tuple), so it is safe across the wrapper's return. Apply only at the decomposer-construction site under `#if defined(USE_HIP)` (CUDA byte-for-byte unchanged). Keep the header library-agnostic (it knows ONLY cuda::std::tuple + rocprim::tuple) so it lifts verbatim into cuco/raft. GPU-proven on gfx90a: 100000 floats via hipcub::DeviceRadixSort::SortKeys + the wrapped decomposer == host std::sort. Reference impl: cudf cpp/include/cudf/detail/utilities/hip/radix_tuple_key.cuh. RELATED rocThrust delta surfaced by the same sort: a thrust::transform writing through a zip_iterator (whose reference is a thrust::tuple) does NOT accept a functor returning cuda::std::pair on rocThrust (CCCL thrust interoperates with cuda::std, rocThrust does not) -- return thrust::tuple from the functor on HIP -- cudf / rocPRIM
- 2026-05-31 -- intermittent heap-corruption SIGSEGV/SIGABRT in a GPU gtest, where the crash frame is inside libamdhip64 / libhsa-runtime64 host malloc/free or __cxa_finalize (e.g. free of 0xff800000, or unlink_chunk in _int_malloc), but a standalone program making the IDENTICAL libcudf/library calls hundreds of times is CLEAN -- suspect the TEST HARNESS, not the port. Burned hours on cudf: a sort test's input helper `mixed_floats(n)` wrote fixed indices `h[10]/h[20]/h[100]/h[200]` unconditionally into a std::vector sized `n`, which is OUT OF BOUNDS for the small sizes in the size loop (n=1,33,64,65), smashing the host heap so a LATER malloc/free or the ROCm atexit finalizer crashed (nondeterministically, ~70%, layout-dependent). The kernels/shim were correct throughout. Tells: (a) crash is intermittent and only in the multi-test binary, never in a single-call standalone repro of the same library code; (b) the fault is in the runtime's OWN host allocator, not in a kernel (no device memory fault under AMD_LOG_LEVEL); (c) it survives MALLOC_CHECK_=3 (the overrun hits adjacent chunk metadata, not the freed pointer). DIAGNOSE by building the test's exact body as a standalone executable linking the real .so and looping it -- if clean, bisect the test file (remove helpers/tests) until the OOB index appears; guard every fixed array index against the parametrized size. Do NOT paper it over with an rmm pool or _exit()-past-finalizers: both "fix" it only by perturbing heap layout and HIDE the real out-of-bounds write -- cudf
- 2026-05-31 -- porting a CUTLASS REDUCING epilogue (a GEMM whose epilogue argmin-reduces each row to a {index, value} pair, e.g. raft fused_distance_nn / fusedL2NN's KeyValuePair{argmin col, min dist}) to CK: do NOT reach for CK's DeviceGemmReduce family. CK's `DeviceGemmReduce_Xdl_CShuffle` (and the bias/add/batched variants) reduce SCALAR values via a `ReduceOperation` (Min/Max/Add) and do not carry the argmin INDEX -- the reduce accumulator is a scalar and there is no exposed way to thread a (value,index) pair through its threadwise-reduce + global-atomic path, so the index is lost. Instead express it as TWO fused steps that REUSE the already-validated CK distance GEMM: (1) the same `DeviceGemmMultipleD_Xdl_CShuffle` instance materializes the full [M,N] result matrix with the fused CDE epilogue (here the distance), then (2) a tiny per-row argmin kernel (one block per row, a plain shared-memory tree reduction -- NO warp shuffles, so wave64-correct by construction) reduces row m to the {index,value} pair and applies the project's own reduce-op into the output. The argmin is numerically EXACT (the only fp error is the GEMM dot product, identical to what the non-reducing distance path already validates). Gate it behind the SAME alignment/dtype dispatch as the non-reducing CK path and keep the SIMT twin as the fallback (raft: fp32 + N%4==0 + K%4==0; everything else -> raft's SIMT fusedDistanceNNkernel). CRITICAL: reproduce the epilogue arithmetic the SIMT REDUCING kernel feeds its argmin, which may DIFFER from the non-reducing `*_cutlass_op` -- raft's fused-NN uses `l2_exp_distance_op::epilog` (the `val*(val>0)*!(clamp)` relu+clamp, then sqrt) not `l2_exp_cutlass_op` (sqrt-only relu); using the wrong twin shifts the min. -- raft, cuvs/cuml gate
- 2026-05-31 -- VALIDATING an argmin/arg-min-distance kernel: never use a periodic or low-entropy test-data generator. An `(i*A+B) % M` element generator has period M in the flat index, so with K elements per row two DISTINCT rows become byte-identical (raft fused-NN: `(i*40503+7)%1000` with K=65 made Y row 200 == row 0 exactly, since 200*65=13*1000 realigns the modular sequence), producing EXACT distance ties. Exact-tie argmin is inherently implementation-defined (a device row-reduction and a CPU left-to-right scan break ties oppositely), so a CORRECT kernel looks broken -- the tell is a constant index offset between device and reference (`got_key = ref_key + period`) with byte-identical values. FIX: generate each element from a hash of (row, col, salt) that EMBEDS the row index (a splitmix64-style mix) so no two distinct rows can collide -> the argmin is unambiguous. Additionally, make the test forgive an index disagreement ONLY when the reference distance at the device's chosen index is within fp tolerance of the reference min (a genuine fp32-GEMM-vs-fp64 near-tie); a real argmin bug still fails. Separately, instantiating a project's SIMT kernel for the first time under clang-as-hipcc can surface latent narrowing (raft simt_kernel.cuh `KVPair{unsigned col, val}` narrows unsigned->IdxT, a hard error on clang, only a warning on nvcc) -- fix with an explicit `static_cast<IdxT>` (value-identical on CUDA) -- raft
- 2026-05-31 -- rocPRIM's cub::DeviceSelect::If / DevicePartition over a cuda::std::tuple VALUE (compacting paired key+value slots) is FIXABLE with a reusable re-tie adaptor -- the DeviceSelect analogue of the radix-decomposer shim, but re-tie to thrust::tuple (not rocprim::tuple), and at the SCATTER boundary (not the codec). cuCollections static_map/dynamic_map retrieve_all streams the slot value through cub::DeviceSelect::If: input value_type is a cuco::pair (a cuda::std::tuple<K,V>, from get_slot's transform iterator), output is a thrust zip_iterator. hipCUB forwards DeviceSelect to rocPRIM's DevicePartition, which stages selected values in a rocprim::detail::raw_storage<ValueType> and scatter-assigns to the output reference: device_partition.hpp:584 does `get<0>(output)[i] = scatter_storage[i]` where the LHS is thrust::tuple_of_iterator_references (the zip reference) whose operator= accepts thrust::tuple / thrust::pair / thrust::reference but NOT cuda::std::tuple -> "no viable operator=" (and partition_scatter overload resolution fails on a cuda::std::tuple ValueType). Root cause is the SAME rocThrust-doesn't-interoperate-with-cuda::std wall as the radix shim, but the resolution point differs: here it is the staged-value <-> zip-output assignment, so re-tie to rocPRIM's NATIVE staged tuple = thrust::tuple. FIX (HIP-only, CUDA byte-identical, reusable): wrap the DeviceSelect INPUT iterator with a thrust::transform_iterator whose functor re-ties the cuda::std::tuple's element values into a thrust::tuple (thrust::make_tuple over an index_sequence on cuda::std::tuple_size), and wrap the predicate to re-tie BACK to cuda::std::tuple before calling the library's slot predicate (cuco's slot_is_filled reads cuda::std::get<0>); rocPRIM then stages a thrust::tuple and the scatter resolves through the existing tuple_of_iterator_references::operator=(const thrust::tuple&). The scalar (non-tuple, e.g. static_set) input forwards verbatim to cub::DeviceSelect::If -- branch on `is_cuda_std_tuple<value_type>` with `if constexpr`. Wire it in only at the retrieve_all DeviceSelect sites under `#if defined(USE_HIP)`; the `#else` is the upstream call unchanged, and include the shim header only under USE_HIP. Keep the header library-agnostic (knows ONLY cuda::std::tuple <-> thrust::tuple) so it lifts into raft/cudf. GPU-proven on gfx90a: a thrust::tuple-valued DeviceSelect::If scatters 4/4 even-key pairs through a thrust zip output correctly (probe), and the un-deferred cuco map TUs pass full -- STATIC_MAP 82->126 cases (contains/duplicate_keys/insert_or_assign/insert_or_apply/retrieve_if), DYNAMIC_MAP 12->18 (retrieve_all+multiplicity), deterministic, no regression. Reference impl: cuCollections include/cuco/detail/hip_device_select.cuh. RELATED test-side gotcha that rides along: a map test building insert input as thrust::make_zip_iterator(keys,vals) hands cuco's insert a thrust::tuple (whose `.first` cuco reads does not exist) -- build it as a cuco::pair-producing cuda::make_transform_iterator instead (NVIDIA-safe; matches cuco's own find_test.cu). NOT every cuco "tuple" wall is this one: hyperloglog is blocked by a `#ifndef __CUDA_ARCH__` host-side throw landing on device under HIP (+ a cuda::stream_ref/thread_rank mismatch), a host/device-guard issue unrelated to DeviceSelect -- cuCollections / rocPRIM
- 2026-05-31 -- porting a project's BESPOKE runtime-NVRTC JIT engine to hipRTC (arrayfire's compile_module.cpp: it NVRTC-compiles generated kernel source at runtime, links with the CUDA driver, launches): the symbol swap (nvrtc*->hiprtc*, drop the PTX+cuLink* block, hiprtcGetCode->hipModuleLoadData, --offload-arch=<gcnArchName stripped of :sramecc/:xnack>) is the easy part; the deep wall is that NVRTC compiles the WHOLE JIT TU as device code while clang/hipRTC treats unattributed free functions/struct members as HOST. Every helper reachable from a JIT kernel that lacks __device__/__host__ __device__ then fails "call to __host__ function from __device__ function" or "no matching function", and since the project's AF_CONSTEXPR macro is empty on the hipRTC path (the half ctors call non-constexpr intrinsics), nvcc's implicit `constexpr`->`__host__ __device__` promotion is also gone. SYSTEMATIC fix: audit every shared header the JIT pulls (half/complex/math helpers, minmax ops, the kernel-local index/offset/diff helpers) and tag free functions __device__ (kernel-local) or the project's __DH__ (host+device). Also: hipRTC's bundled `std` is SMALLER than NVRTC's (add the integer traits + numeric_limits<float/double> incl. infinity()/isnan/isinf the device code uses to the RTC std-shim), it REJECTS empty-string ("") header bodies and the NVRTC flag --device-as-default-execution-space, and the shared DefineValue macros emit " -D NAME=val" as ONE string which clang reads as a filename (split each option on whitespace into separate argv). Define -D__CUDACC_RTC__ and -D__CUDA_ARCH__=<NNN>=530 so embedded headers take their RTC + device-intrinsic paths. End state: arrayfire afhip 126/132 CUDA.* gtest binaries on gfx90a, the JIT engine itself 1781/1781 -- arrayfire
- 2026-05-31 -- a shared embedded .cuh that defines MULTIPLE kernel TEMPLATES under a runtime-JIT: EVERY getKernel launcher compiling that source must pass each `-D` any template's body references as a NON-dependent identifier (e.g. THREADS_X / THREADS_PER_BLOCK / a block-dim constant), even templates that launcher never instantiates. NVRTC only phase-2-instantiates the requested name expression and tolerates the unused template; clang/hipRTC does phase-1 name lookup on ALL of them, so a bcast/sibling launcher that omitted the define fails "use of undeclared identifier THREADS_X". Fix: give every launcher of the shared source the full DefineValue set (arrayfire: scan_first/scan_dim bcast, ireduce, lookup, medfilt, flood_fill, susan, scan_by_key) -- arrayfire
- 2026-05-31 -- a templated DEVICE kernel doing a bare `a * b` on a complex T where T lowers to HIP_vector_type<float,2> (float2) silently binds to HIP's COMPONENTWISE friend operator* (wrong: a complex product is intended) instead of the project's complex operator, OR ties with it. Two complementary fixes, both arch-unified: (1) make the JIT complex type a plain POD {x,y} (no operators) so only the project's complex operator matches -- but do it ONLY under __CUDACC_RTC__ in the embedded cuComplex shim, because the host/compiled path's compat header (hip_compat.h) force-#defines cuFloatComplex=hipFloatComplex and a POD there collides; (2) where ADL still picks componentwise (arrayfire's convolve, whose operands come from a __constant__-buffer reinterpret), spell the complex product out via .x/.y in a tiny local helper -- a two-type template `auto mul(Ta,Tb)->decltype(a*b)` for the real/mixed (int*float) cases PLUS exact cfloat/cdouble overloads. Keep dtype_traits::getName() = "cuFloatComplex" (the JIT instantiation name): renaming it to the project alias breaks kernels where only cuFloatComplex is in scope -- arrayfire
- 2026-05-31 -- a hipCUB block primitive placed in a UNION with a project scratch buffer can corrupt LDS on rocPRIM even when the primitive is correct standalone. arrayfire topk put `BlockRadixSort::TempStorage` in a union with the pre-sort rearrange buffer (the classic CUB shared-memory-reuse idiom); on rocPRIM the kernel GPU-faulted on every input (even single-block n=100), while the identical BlockRadixSort<float,256,4,uint> + SortBlockedToStriped sorts fine in a standalone kernel. Give the primitive its OWN __shared__ allocation (the two together were ~16KB, well within the 64KB LDS budget). Distinct from the back-to-back-reuse TempStorage RACE (which __syncthreads fixes) -- this is the union ALIASING the live scratch. topk 0 -> 110/110 -- arrayfire
- 2026-05-31 -- hipblasGemmEx / hipblasGemmBatchedEx take a `hipblasComputeType_t` (HIPBLAS_COMPUTE_32F/64F/32I/16F), NOT a `hipDataType` as cuBLAS's cublasGemmEx historically did -- passing getComputeType()'s hipDataType is a hard "no matching function" error; map the accumulate type to HIPBLAS_COMPUTE_*. The Ex path is also commonly gated `#if __CUDACC_VER_MAJOR__ >= 10`, a CUDA-only macro (undefined on HIP) that silently compiles the Ex branch OUT so every gemm falls to the typed hipblasSgemm/Cgemm path -- which has no int8 specialization; guard it with __HIP_PLATFORM_AMD__ to reach it, but route ONLY the type that needs Ex (int8) through it (enabling Ex for half regressed accuracy vs the typed Hgemm). rocBLAS does NOT support 8I input -> 32F output gemm (HIPBLAS_STATUS_NOT_SUPPORTED); int8 gemm needs int32 accumulate + a post-convert, a follow-on -- arrayfire
- 2026-05-31 -- arrayfire SPARSE on hipSPARSE: the cuSPARSE surface ports near-1:1 (generic CreateCsr/Csc/Coo/DnVec/DnMat + SpMV/SpMM + DenseToSparse/SparseToDense + SpMatGetSize + Csr/CscSetPointers, legacy Xcsrsort/Xcoosort/Xcsr2coo/Xcoo2csr/CreateIdentityPermutation, typed csrgeam2 + Xcsrgeam2Nnz -- ALL present in hipSPARSE 4.2). Keep the .cu in cuSPARSE spelling via a forwarding shim (nvrtc_shims/cusparse_v2.h: include <hipsparse/hipsparse.h>, #define each cusparse*/CUSPARSE_* -> hipsparse*/HIPSPARSE_*, and #define CUSPARSE_VERSION high so the project's generic-API + csrgeam2 #if branches select). Two non-1:1 deltas: (a) THE void*-aliasing fault class bites the DESCRIPTORS, not just the handles -- hipsparseDnVecDescr_t AND hipsparseDnMatDescr_t (and hipsparseHandle_t/hipsparseMatDescr_t) are all `typedef void*` (only hipsparseSpMatDescr_t is a distinct struct ptr), so the shared type-keyed common::unique_handle<T>/DEFINE_HANDLER(T) redefines ResourceHandler<void*> the moment one TU pulls two of them; use a tag-keyed TaggedHandle<Raw,Tag> (DEFINE_HIP_HANDLE) per logical descriptor instead, with a make_handle<TaggedT>() convenience so the .cu call sites read like the CUDA make_handle<cusparseMatDescr_t>(). (b) hipsparseSpMV/SpMM take the COMPUTE type as a hipDataType (getType<T>()), NOT the hipblasComputeType_t that the same backend's getComputeType<T>() returns for hipblasGemmEx -- a different enum family; the sparse path must call getType<T>() there. Unlike the NVIDIA build's runtime-dlopen cusparseModule plugin, link roc::hipsparse directly (drop getCusparsePlugin()._. and call the functions by name). Typed complex csrgeam2 takes const hipComplex*/hipDoubleComplex*, but arrayfire's cfloat/cdouble are distinct layout-compatible POD structs on the compiled HIP path, so reinterpret_cast the complex value/alpha/beta pointers at the call boundary (the cublas Hgemm reinterpret precedent). Result on gfx90a: sparse 86/86, sparse_convert 41/41, sparse_arith 123/123, threading 9/9 -- arrayfire
- 2026-05-31 -- the CUDA-driver OPAQUE-TYPE bridge is a name-mangling fault class, not just a build-friction one: a project that forward-declares `struct CUstream_st;` (or CUgraph_st/CUgraphNode_st/CUgraphExec_st/CUevent_st) and uses CUstream_st* as its PUBLIC stream type must alias that name to the matching HIP opaque struct (#define CUstream_st ihipStream_t, CUevent_st ihipEvent_t, CUgraph_st ihipGraph, CUgraphNode_st hipGraphNode, CUgraphExec_st hipGraphExec -- verify the exact spellings in hip_runtime_api.h) CONSISTENTLY in EVERY library/binary that touches the type, not just the .cu library that defines the functions. If only the GPU lib gets the alias, its definitions mangle as ...(ihipStream_t*) while the host caller lib and the test executables (which include the same public header but compile as plain host C++) mangle the call as ...(CUstream_st*) -> link fails "undefined reference to overlap_gpu(..., CUstream_st*)" even though the symbol is right there. Fix: put ONLY the opaque-type #defines (forward-decls, no HIP runtime needed for a pointer type) in a tiny header and force-include it (`-include`, full path) with -DUSE_HIP into the main lib AND every test target too, alongside the GPU lib's fuller compat header. A pointer-type bridge needs no <hip/hip_runtime.h>, so it adds zero HIP dependency to host C++ TUs -- gtsam_points
- 2026-05-31 -- the rmm/cupoch `namespace thrust::cuda = thrust::hip` exec-policy alias works cleanly as a FORCE-INCLUDED compat-header line for a mixed host/device target, with two requirements: (1) it must come AFTER thrust::hip exists, so the compat header itself must `#include <thrust/system/hip/execution_policy.h>` before the alias (a force-include lands above the source's own thrust includes, so you cannot rely on those); (2) guard the include+alias on `#if defined(__HIPCC__)` -- only the .cu compiled `-x hip` by clang define __HIPCC__ and can parse the rocThrust/rocPRIM execution-policy header, whereas the GPU lib's host-only .cpp (streams + async alloc, compiled by g++ as CXX) cannot parse it and do not use thrust::cuda::par anyway. g++ defines neither __HIP__ nor __HIPCC__; clang -x hip defines both. Standalone-verified the alias compiles AND dispatches correctly on gfx90a -- gtsam_points
- 2026-05-31 -- CONFIRMATION of the cudaKDTree atomicMax fault class boundary: an unsigned atomicMax(reinterpret_cast<unsigned*>(&float_in_device_buffer), __float_as_uint(v)) for a per-voxel max-intensity is CORRECT on gfx90a when the buffer is FINE-GRAINED device memory (cudaMallocAsync -> hipMallocAsync, the stream-ordered pool), exactly because the "silently dropped" failure is specific to COARSE-GRAINED/managed (hipMallocManaged) allocations. So before reaching for the atomicCAS-loop emulation, check the allocation kind: a hipMallocAsync/hipMalloc buffer needs no workaround. Verified end-to-end (gtsam_points GaussianVoxelMapGPU::insert; test_voxelmap VoxelMapGPU_Intensity passes, intensities correct). The watch item is real but did not bite -- gtsam_points
- 2026-05-31 -- `#define cub hipcub` (the cudaKDTree CUB-namespace alias) does NOT rewrite the `#include <cub/...>` PATH (the preprocessor does not expand tokens inside <...>); a project that includes <cub/device/device_reduce.cuh> still needs a path shim. Drop forwarders at hip_compat/cub/device/device_{reduce,select}.cuh that `#include <hipcub/device/device_{reduce,select}.hpp>` on a PRIVATE+BEFORE include dir; the namespace #define then handles the cub::DeviceReduce/DeviceSelect spellings in code. Also: the `#if (CUDA_VERSION < 11000)` cuda_malloc_async downgrade trap and a CUDA-12 vs 13 graph-API arity branch (cudaGraphAddDependencies 4-arg vs 5-arg, cudaGraphInstantiate 5-arg) are both settled by pinning CUDA_VERSION into [11000,13000) in the compat header -- ROCm 7.x hipGraphAddDependencies is the 4-arg form and hipGraphInstantiate the 5-arg form, matching the <13000 / CUDA-12 branches -- gtsam_points
- 2026-05-31 -- GTSAM as a dependency: Ubuntu 24.04 apt `libgtsam-dev` (4.2.0) ships gtsam ONLY -- no gtsam_unstable lib, no gtsam_unstable headers, no GTSAM_UNSTABLEConfig.cmake, and there is no libgtsam-unstable-dev package. Any project with `find_package(GTSAM_UNSTABLE REQUIRED)` (e.g. gtsam_points, which links gtsam_unstable for ISAM2) CANNOT use the apt package; source-build borglab/gtsam @ 4.3a0 (GTSAM_BUILD_UNSTABLE defaults ON; GTSAM_USE_SYSTEM_EIGEN=ON to share the Eigen ABI, GTSAM_WITH_TBB=OFF, no tests/python). Runtime gotcha: the source-built libgtsam pulls a bundled libmetis-gtsam.so in <prefix>/lib; consumer test binaries' RUNPATH does not cover that TRANSITIVE dep (RUNPATH != RPATH for transitive libs), so gtest_discover_tests (runs each binary at build time) and ctest need LD_LIBRARY_PATH=<prefix>/lib -- gtsam_points
- 2026-05-31 -- a JIT-only `.cuh` struct functor reached from a __global__ kernel (arrayfire kernel/sparse_arith.cuh `arith_op<T,op>::operator()`) needs __device__ on its member operator() under hipRTC: clang treats the unattributed member as HOST, so the device kernel's `arith_op<T,op>()(a,b)` call fails "no matching function for call to object of type arith_op<...>". Same root as the free-function JIT fault, just on a struct member. The .cuh is JIT source (embedded as a string, never compiled directly), so __device__ is correct and host-invisible. Tag every operator() in the functor (incl. the primary template). Combined with the multi-template -D fix (csrArith* use TX/TY, cooArith* use THREADS, so every launcher passes all three), arrayfire sparse_arith goes 0 -> 123/123 -- arrayfire
- 2026-05-31 -- arrayfire int8 GEMM CLOSED (was the documented rocBLAS 8I->32F gap): rocBLAS rejects int8-in/float32-out|compute, but gfx9/CDNA HAS int8 MFMA and supports int8 x int8 -> int32 accumulate. Route the schar path through hipblasGemmEx with HIP_R_8I inputs + HIP_R_32I output + HIPBLAS_COMPUTE_32I into a temp Array<int>, then copyArray<int,To> casts the int32 result into arrayfire's f32 output (s8's out_type). Use `if constexpr (is_same<Ti,schar>)` so the int32 cast/path is emitted ONLY for the schar instantiation (a plain runtime `if` makes static_cast<int>(cfloat) a hard error in the float/complex instantiations). alpha/beta are integer scales on the int path; round the float scalars. MatrixMultiply.schar passes, blas 126/127 -> 127/127. So int8->float32 was the only (correct) rocBLAS rejection; int8->int32 is fully supported -- arrayfire
- 2026-05-31 -- raft/cuvs `neighbors/detail/faiss_select/` is a VENDORED COPY of FAISS's gpu/utils select files (Select.cuh, MergeNetworkWarp/Block.cuh, key_value_block_select.cuh, StaticUtils.h, ...), vendored in the CUDA (wave32) form. Do NOT reinvent the wave64 warp-select: upstream FAISS (facebookresearch/faiss) ships a ROCm/HIP GPU port that made exactly these files wave64-correct, so port FAISS-ROCm's deltas into raft's copies (clone faiss as the reference). Mechanism: (1) kWarpSize = rocprim::arch::wavefront::max_size() (64 on CDNA) parameterizes ALL select/merge logic -- no hardcoded 32 (raft uses raft::WarpSize, host-correct via RAFT_HOST_WARP_SIZE); (2) the bitonic merge networks get ONE extra log2 stage on wave64 (6 stages 1..32 vs 5 stages 1..16), `if constexpr (kWarpSize==64)`, in BOTH the single-warp sort AND the BitonicMergeStep<...,1,...,true> merge base case (the latter must merge size-32, not size-16, or lanes 32-63 are dropped); (3) THE KEY FIX for the __hip_check_mask abort: FAISS-ROCm uses the MASKLESS warp builtins on ROCm (__shfl/__shfl_xor/__any, NO _sync, NO mask) so a bitonic merge reached from a DIVERGENT region (ball_cover's per-lane Cayton-prune `return` + `if (i<R_size)` tail leave a partial wavefront) is well-defined -- a full-mask __shfl_xor_sync(RAFT_LANE_MASK_ALL) traps (HSA exception 0x1016, mask != __ballot(true)) and under NDEBUG reads dead lanes and mis-sorts. Implement as a faiss-local WarpShuffles.cuh (mirroring FAISS-ROCm's) so the maskless shuffles are scoped to faiss_select and the generic raft::shfl* (distance/fused-NN/select_warpsort) is untouched. Canonical for BOTH raft neighbors AND cuvs (which carries its own faiss_select copy). SEPARATELY, ball_cover's eps_nn/RBC kernels (registers-inl.cuh, no FAISS select) have their own wave32-mask bugs: `int lane_mask=ballot(); __brev/__clz/0x7fffffff` ballot-bit iteration and `(1u<<lid)-1`+`__popc` CSR positions drop landmarks 32-63 (undercount); fix arch-unified with WarpMask + ffs_mask(lowest-bit)+`m&=m-1` + lane_bit + popc_mask. PLUS two host-geometry wave32 bakes: a CSR/max_k grid `ceildiv(n,2)` (the 2 = tpb64/wave32) drops half the queries on wave64 -> `ceildiv(n, 64/WarpSize)`, and a copy kernel launched with 32 threads but rounding by Pow2<WarpSize=64> drops a >32-element tail -> round by Pow2<tpb>. ball_cover KNN + eps RBC then fully GPU-validated on gfx90a (BallCover{AllKNN,KNNQuery} 18/18, EpsNeighRbc 42/42) -- raft
- 2026-05-31 -- GL/CUDA-interop ingest does NOT port to a headless CDNA datacenter GPU (MI250X gfx90a): the chip is COMPUTE-ONLY (no graphics/display blocks), so Mesa's hardware GL driver refuses a context -- `radeonsi: error: can't create a graphics context on a compute chip` -- and there is no GPU-resident GL context for hipGraphicsGLRegisterImage to share. The HIP GL-interop API is fully present (hip/hip_gl_interop.h: hipGraphicsGLRegisterImage/Buffer, hipGLGetDevices, map/unmap/unregister, the flags enum; all in hipify's map) but has nothing to bind to. Fallbacks fail the same way: no Vulkan ICD installed -> zink (GL-over-Vulkan) `vkCreateInstance VK_ERROR_INCOMPATIBLE_DRIVER`; the EGL software device (llvmpipe) DOES give a GL 4.5 context but it is host-memory, so `hipGLGetDevices -> hipErrorNoDevice` (count 0) and `hipGraphicsGLRegisterImage -> hipErrorUnknown` (HIP cannot import a CPU-rendered texture onto the gfx90a). Probe in ~5 min with EGL `eglQueryDevicesEXT` + `eglGetPlatformDisplayEXT(EGL_PLATFORM_DEVICE_EXT, ...)` over the amdgpu DRM nodes (they enumerate + advertise 112 OpenGL configs but eglCreateContext then errors), then llvmpipe + hipGraphicsGLRegisterImage. CONSEQUENCE for a project whose GPU inputs arrive ONLY via GL textures (ElasticFusion: RGBDOdometry maps GL surfel/depth/RGB textures to cudaArray_t; no CUDA-only ingest): the full app cannot GPU-validate headless, AND the "GL-readback bounce" fallback is moot (the readback still needs a GL context to RENDER the GLSL surfel prediction; llvmpipe would run the whole front end on the CPU). Validate the ported kernels DIRECTLY with a device-array harness (feed fixed DeviceArray inputs to the kernel entry points, no GL) -- this proves the actual ported GPU code; the live GL path belongs on a follower with a real DISPLAY GPU (gfx1100 has graphics blocks + hardware radeonsi GL, where interop may work). For the wave64 reduction in such a harness, the int-typed reduction (count/sum) is the strongest check: integer adds are order-independent, so device==CPU recount is an EXACT arithmetic proof the 64-lane reduction loses/doubles no lane, beyond just two-run determinism -- ElasticFusion
- 2026-05-31 -- rocThrust zip_iterator + rocPRIM reduce_by_key do NOT interoperate with cuda::std::tuple the way CCCL's thrust does, which breaks any CCCL-native lib (cudf/raft/cugraph/cuml) that uses cuda::std::tuple at a thrust algorithm boundary. (1) `thrust::make_zip_iterator(cuda::std::make_tuple(it1,it2))` does not form an iterator -- rocThrust's zip_iterator is parameterized on thrust::tuple and its iterator_traits has no reference/value_type for a cuda::std::tuple-of-iterators; use thrust::make_tuple (canonical on CUDA too). (2) the zip-iterator's dereferenced reference is a thrust::tuple<T&...>, which has no `operator=` from a cuda::std::tuple/pair value (a transform/reduce functor returning cuda::std::tuple -> "no viable overloaded ='"); (3) rocPRIM reduce_by_key stores its accumulator in a thrust/rocprim tuple and internally does `get<1>(reduction)` assignment, so the reduce VALUE type itself must be a thrust::tuple, not cuda::std::tuple. Fix at the zip boundaries only with a file-local zip_tuple/zip_get/zip_make_tuple alias = thrust:: on HIP, cuda::std:: on CUDA (per-site bodies unchanged, CUDA byte-for-byte identical). Same family as the sorted_order_radix thrust::tuple-vs-cuda::std::pair fix -- cudf
- 2026-05-31 -- clang (hence HIP) rejects an execution-space attribute written BETWEEN operator() and its parameter list: `RET operator() __device__(args)` and `RET operator() CUDF_HOST_DEVICE(args)` (nvcc tolerates this placement) -> "'operator()' cannot be the name of a variable or data member" + "expected ';'". Move the attribute before operator() (`__device__ RET operator()(args)` / `CUDF_HOST_DEVICE RET operator()(args)`), accepted by both. Mechanical and arch-unified (no guard). Hit at 10 sites in one cudf tdigest TU -- cudf
- 2026-05-31 -- clang forbids a `__host__ __device__` function from calling a `__device__`-only callee (nvcc only warns), so a host+device functor that indexes a device-only container fails. cudf's device_span::operator[] was __device__-only, but cudf has CUDF_HOST_DEVICE functors that index a device_span AND are instantiated for a host CPU path (tdigest generate_cluster_limit runs on host for small group counts, over pinned/host-addressable spans). Fix: USE_HIP-guard device_span::operator[] to CUDF_HOST_DEVICE (matches the host span's indexer; the device pass is unchanged, and nvcc accepts the device-only form so CUDA is byte-for-byte unchanged). It is a force-included core header so it recompiles most TUs, but only ADDS host-callability (low risk) -- cudf
- 2026-05-31 -- find_package(cudf) dependency-export for the HIP build (so cugraph/cuml consume jeffdaily/cudf @ moat-port): add install(TARGETS cudf EXPORT cudf-exports) + install(EXPORT ... NAMESPACE cudf:: FILE cudf-targets.cmake) + a configure_package_config_file-generated cudf-config.cmake that does find_dependency(hip)+find_dependency(rmm) then includes the targets, plus install the include/cudf tree and the generated version_config.hpp/logger_macros.hpp. PRIVATE deps of a SHARED lib (cuco::cuco, hip::host) are NOT in the link interface so the export does not reference them (only the PUBLIC rmm::rmm needs find_dependency). KEY consumer constraint: the cudf-including downstream TU MUST compile as HIP -- cudf's PUBLIC headers pull rocPRIM device intrinsics (__builtin_amdgcn_wavefrontsize) that a plain-CXX TU cannot compile. Verify end-to-end (configure+link+RUN on GPU a make_numeric_column), not just that the config parses -- cudf
- 2026-05-31 -- a Strategy-B torch-extension forward-render test that asserts hardcoded golden PIXEL values (places=4/5) can FAIL on HIP for a reason that is NOT a port bug: a stale/wrong upstream golden, or a golden recorded tighter than float32 epsilon. Discriminate decisively WITHOUT a CUDA GPU before touching tolerances or reaching for -ffp-contract: (1) re-implement the exact failing computation in pure float64 on the CPU (numpy/torch, replicating the kernel's arithmetic) and compare to the golden -- if the EXACT float64 math also disagrees with the golden, the golden is simply wrong (e.g. joeyan/gaussian_splatting test_projection: exact f64 uv=573.98641 rounds to .9864 but golden recorded .9863, and HIP's f32 .98645 is CLOSER to truth than the golden). (2) For a composite/blend, find how many elements actually contribute at the asserted pixel (dump the per-splat list) -- if ONE, the value is a closed-form single term you can recompute in Python; reproducing the HIP value EXACTLY (not the golden) proves HIP is right (gaussian_splatting test_rasterize SH paths: pure-CPU single-splat recompute = HIP [0.6331,...], not golden [0.5363,...]). (3) Zero-out the feature under test (set higher-order SH coeffs to 0) and confirm the path collapses to a PASSING golden -- isolates the broken golden to one term. (4) Check upstream CI: a repo with NO workflows never gated these goldens, so staleness is expected. A gradcheck PASSING does NOT clear a forward golden: gradcheck only verifies analytic-vs-finite-difference SELF-consistency of the (possibly drifted) forward, never an absolute value. -ffp-contract=on cannot fix a wrong golden -- do not apply it for these; reserve it for a genuine 5th-decimal FMA-contraction miss where the f64 math AGREES with the golden -- gaussian_splatting
- 2026-06-01 -- tiled_partition<32>::ballot() DIVERGENCE FAULT on gfx1100 (wave32): a `tile.ballot(pred)` call inside a per-lane `while (i < size)` grid-stride loop causes `HSA_STATUS_ERROR_EXCEPTION` (hardware exception code 0x1016) on gfx1100 when size%32 != 0. Root: the "partial tail" leaves some tile members at a different PC (they have exited the while loop); calling a CG collective from only SOME tile members is invalid. On gfx90a (wave64) the 32-tile is a sub-wave of the 64-lane hardware wavefront -- all 64 lanes execute together in lockstep so the tile ballot is always hardware-converged even when logically "some exited". On gfx1100 (wave32) the 32-tile IS the hardware wavefront, and divergence within it triggers the fault. FIX: restructure the loop so ALL tile members call ballot together, with out-of-range members voting false. Correct pattern: `while(tile.any(i < size)) { bool active = (i<size); bool vote = active && pred(*(begin+i)); bitmask = ballot_32(vote); if(lane_id==0 && active) output[word_index(i)] = bitmask; i+=stride; }`. The `tile.any(i<size)` keeps the tile in the loop as long as ANY member has work; every member calls ballot with out-of-range members voting false. The output write is guarded by `active` so only in-range members write. This is the EXACT same structural issue as the LiteGS divergent `__any_sync` (2026-06-01 entry above) -- the underlying mechanism is always: a HIP CG collective reached from divergent code paths on gfx1100 aborts the queue. Confirmed: converged ballot (all 32 lanes at the same PC) = PASS; divergent-if ballot (only lane 0 calls it) = CRASH. cudf `valid_if_kernel` in `valid_if.cuh` is the affected site -- cudf
- 2026-05-31 -- 4th gaussian-splatting-family port (gsplat/fused-ssim/LiteGS prior) re-confirms the gsplat fault #4 template as the ENTIRE port for a wave-agnostic 3DGS torch extension: one file, cg::reduce(tile<32>, v, cg::plus<T>()) -> a USE_ROCM templated warpReduceSum butterfly (`for(o=warp.size()/2;o>0;o>>=1) v+=warp.shfl_xor(v,o)`) routed through a WARP_REDUCE_SUM macro (cg::reduce on CUDA), plus guarding out the nonexistent `#include <cooperative_groups/reduce.h>` under `#if !defined(USE_ROCM)` (hipify leaves that include verbatim; it maps only `<cooperative_groups.h>` -> `<hip/hip_cooperative_groups.h>`). Confirmations specific to a CUDAExtension: torch's HIP compile passes `-DUSE_ROCM=1` automatically (grep the build line; no setup.py -D needed), and the float64 autograd gradchecks are the decisive gate -- they exercise the double instantiation, so the warpReduceSum MUST be templated over T (HIP tile shfl_xor supports double). Block 16x16=256 -> 4 wave64 wavefronts each holding 2 independent tile<32>; each tile's rank-0 atomicAdd matches CUDA warp granularity, no lane-math rework -- gaussian_splatting
- 2026-06-01 -- 5th gaussian-splatting-family port (op43dgs: stock Inria diff-gaussian-rasterization, 3 camera variants pinhole/fisheye/panorama all declaring the same module diff_gaussian_rasterization._C as mutually-exclusive installs, + simple-knn) re-confirms the wave-agnostic 3DGS torch-extension template and adds two NEW guard-cascade traps. EASIER than the gaussian_splatting precedent: this codebase uses cg::reduce ZERO times (only block/grid scaffolding this_grid/thread_rank/this_thread_block/sync), so the `<cooperative_groups/reduce.h>` fix is a PURE include guard (`#if !defined(USE_ROCM)`) with NOTHING to replace -- no warpReduceSum butterfly needed. (1) Guarding out the CUDA-only `#include "device_launch_parameters.h"` on HIP (ROCm ships no shim, hipify has no mapping; HIP gets threadIdx/blockIdx/blockDim from <hip/hip_runtime.h>) CASCADES: that header transitively pulled in <cfloat> (FLT_MAX) and the __trap decl through the CUDA toolkit, so once it is gone those go UNDECLARED. Fix the fallout explicitly: add `#include <cfloat>` for FLT_MAX (standard, idempotent on CUDA) and map __trap (HIP has no such intrinsic) to the clang device builtin: `#if defined(USE_ROCM) #define __trap __builtin_trap #endif` (illegal-instruction trap, call site unchanged). GENERAL RULE: when you guard out a CUDA convenience header on HIP, grep the TU for symbols it was silently providing (FLT_MAX/DBL_MAX from <cfloat>, INT_MAX from <climits>, device intrinsics) and add the portable include/shim. (2) Inria simple_knn.cu carries a stray `#define __CUDACC__` right before <cooperative_groups.h> (a stock trick to force CG device qualifiers) -- guard it out on ROCm (`#if !defined(USE_ROCM)`): HIP's hip_cooperative_groups.h needs no hint and a stray __CUDACC__ under hipcc risks steering headers down a CUDA path. The bundled-GLM hipify monkeypatch (gsplat fault: add the glm dir to hipify ignores + strip from header_include_dirs in setup.py, keyed on PYTORCH_ROCM_ARCH or /opt/rocm so CUDA is untouched) was needed per-variant as expected; GLM 0.9.9.9 behaves exactly like gsplat's 1.0.2 (has a GLM_COMPILER_HIP path, so no __CUDACC__ shim, just protect the .inl files). The cub:: namespace was NOT an issue (torch hipify maps both the include and the namespace) -- no `namespace cub = hipcub;` alias needed; the cub SortPairs begin_bit is 0 (full-width sort, NOT the cudaKDTree nonzero-begin_bit bug). The 27 spaced MUSA/Inria launches "<< <"/">> >" -> "<<<"/">>>" is the LiteGS normalization (the whole repo uses the spaced form, zero standard launches). VALIDATION SUBTLETY worth recording: op43dgs's optimal-projection backward (backward.cu replaces the stock dL_dmean2D with cross-terms * tan_fovx/tan_fovy -- the paper's contribution) makes the means3D analytic gradient an APPROXIMATE local-affine descent direction, so its FD slope is a scaled constant (pinhole ~0.16, panorama ~1.77) eps-INDEPENDENT with ~100% sign agreement -- NOT a port bug (the port never touches the math; identical on CUDA). Gate cov2D-dependent grads (means all variants; scales on the curved fisheye/panorama) on SIGN agreement + grad-sum stability, and slope~1.0 only on the camera-independent quantities (opacity ~0.98, sh ~1.0); the short training fit (loss-down/PSNR-up) is the end-to-end proof the scaled-but-correct-direction gradients optimize. All 3 rasterizer variants + simple-knn built and GPU-validated on gfx90a, zero wave64 lane-math rework -- op43dgs
- 2026-06-01 -- 6th gaussian-splatting-family port (splatad = gsplat v1.0.0 + a LIDAR spherical-rasterization path; NOT OptiX -- lidar_proj -> isect_lidar_tiles -> rasterize_to_points is the same block-per-tile + shared-mem + cub-sort structure as the camera rasterizer). The full gsplat fault set re-applied to the flat csrc layout (helpers.cuh warpReduceSum/Max butterfly + LabeledGroup via match_any; reduce.h include guard; namespace cub=hipcub; const-void* cudaFuncSetAttribute x78; fast-math off on ROCm; the GLM hipify-ignore monkeypatch), and the lidar half ported with the camera half (no staged split). Camera test_basic math + test_rasterization (24) + lidar test_basic math (14) + test_lidar_rasterization[3,32,128] all pass vs the pure-torch oracle; the warp.any() blend-loop CG TILE op does NOT fault on wave64 (forward bit-deterministic), confirming it is tile-relative (not the LiteGS divergent-__any_sync class). TWO NEW BUNDLED-GLM TRAPS that gsplat's notes did not surface because gsplat's build.py masked them, but a setup.py that lacks the same flags hits them: (1) GLM's operator[] bounds check (GLM_ASSERT_LENGTH in type_vec*/type_mat*.inl) expands to assert() whose __assert_fail is a __host__ fn referenced from GLM's __host__ __device__ accessors -- nvcc supplies a device assert and tolerates it, ROCm clang errors "reference to __host__ function '__assert_fail' in __host__ __device__ function". Fix: add -DNDEBUG to the ROCm nvcc flags (the standard release define; the indices are compile-time-constant and always in range). (2) glm::make_vec/make_mat (type_ptr.inl) call bare memcpy from __host__ __device__ code; HIP provides a __device__ memcpy overload only once <hip/hip_runtime.h> is in scope, but a splatting repo often includes GLM as the FIRST include (before any torch/HIP header) so the device pass binds the __host__ libc memcpy -> "reference to __host__ function 'memcpy'". Fix: on ROCm #include <hip/hip_runtime.h> BEFORE the GLM includes in the header(s) every TU pulls first (here helpers.cuh + bindings.h). GENERAL: a bundled GLM under torch-hipify needs THREE things together -- the hipify-ignore monkeypatch (leave GLM's .inl/.hpp pristine), -DNDEBUG (kill the device-illegal assert), and the GPU runtime header before GLM (device memcpy for make_vec). The cg::reduce(warp,bin_final,cg::greater<int>()) "warp_bin_final" sites are inside the rasterizer .cu (not just helpers.cuh) -- route them through the SAME warpReduceMax helper. LIDAR validation gap = the camera nerfacc gap: test_rasterize_to_points/test_accumulate_until_points reference imports nerfacc (CUDA _C None on ROCm); substitute the gsplat independent check on lidar_rasterization (forward finite + 2-run bit-exact; backward grads finite + grad-sum stable + FD slope ~1.0 on the linear lidar_features grad; 80-step Adam loss-down). nerfacc 0.5.3 installs as a pure-python wheel but still no-ops without its CUDA _C. Host test-tooling: pytest<9 (pytest 9 makes a mark-on-fixture a fatal collection error) + typing_extensions>=4.13 (typeguard plugin needs NoExtraItems) -- splatad
- 2026-06-02 -- FIRST OptiX -> HIPRT reimplementation (EnvGS Stage 2, diff-surfel-tracing): MOAT's first port of an NVIDIA OptiX ray-tracing pipeline onto AMD HIPRT (GPUOpen, separate SDK, NOT hipRTC; on CDNA gfx90a there is no RT hardware so HIPRT runs software BVH traversal, HIPRT_RTIP=0, wave64). MAPPING that worked: optixAccelBuild(TRIANGLES GAS) -> hiprtCreateGeometry/hiprtBuildGeometry over the SAME host disk tessellation (1:1); raygen/closesthit/miss COLLAPSE into one ordinary HIP __global__ (less code than OptiX -- no program groups/pipeline/SBT/stack-size); __anyhit__ record-and-ignore -> a HIPRT FILTER FUNCTOR returning true to enumerate every hit t-sorted (textbook); optixLaunch -> oroModuleLaunchKernel of a JIT kernel (hiprtBuildTraceKernels, Orochi->hiprtc, disk-cached) -- OptiX ships offline PTX, HIPRT JITs HIP source at runtime, so SHIP the kernel sources (vendor the HIPRT SDK in third_party/ and point HIPRT_PATH at it) and carry the cache-name fix (Compiler.cpp: device names like "AMD Instinct MI250X / MI250" contain '/', which std::filesystem::path treats as a dir sep -> cache write fails; sanitize '/'+'\\' to '_'). payload is SIMPLER (a real void*, no OptiX 2x32-bit packPointer). BUILD STRUCTURE TRAP: Orochi's hipew driver loader redeclares the HIP driver API and CONFLICTS with torch's <hip/hip_runtime.h> -- they cannot share a translation unit. Solution: compile the HIPRT/Orochi glue as a STANDALONE static lib (g++, no torch headers), have the torch CUDAExtension compile only the torch-facing TUs, and bridge them through a POD/void* header (no HIPRT/Orochi type crosses the boundary); keep the public C++ class + Python API names byte-stable so bindings/autograd are unchanged. ROCm 7.2.x hiprtc/comgr CODEGEN FAULT CLASS for register-heavy traversal kernels (this is the hard part, distinct from the ray-tracing model): (1) per-ray hit chunk buffer MUST be GLOBAL scratch, not a kernel stack array -- above a register-pressure threshold the JIT's writes through a stack-payload pointer become INVISIBLE to the caller; a global buffer is pressure-immune; (2) pass --gpu-max-threads-per-block=64 to the JIT (declare the small launch block) or the traversal miscompiles; (3) make the traversal step __noinline__, RETURN the chunk count from it (do not read the filter-updated payload field in a heavy caller frame -- it reads stale/zero), and __threadfence after; (4) mark heavy backward device helpers __noinline__ and read forward outputs/saved tensors STRAIGHT FROM GLOBAL (params.*) rather than staging them into large kernel-local arrays (the local-array loads come back stale/NaN under pressure). LATENT-BUG-EXPOSED-BY-HIP: an UNINITIALIZED stack variable that NVCC happened to leave benign (here `cutoff`, set only in OptiX's primary-bounce branch but used on the reflected bounces) is NaN on HIP and poisons gradients -- grep for use-before-init in any branch the port newly exercises and initialize it. VALIDATION: the forward tracer is straightforward to GPU-validate (build the disk BVH, trace, assert a non-trivial image with genuine hits+misses and finite depths) and the LINEAR backward gradient (colors) checks out by FD. The DIFFERENTIABLE GEOMETRIC backward (means/scales/rotations/opacity) is where a DETERMINISTIC, LOAD-CORRELATED NaN can appear (NaNs on the MOST heavily-hit primitives, finite on the lightly-hit ones; INSTRUMENTATION-SENSITIVE -- a printf/probe flips it to a finite correct value; gradient math byte-identical to the OptiX reference). CRITICAL LESSON (corrected 2026-06-02, this entry originally MISDIAGNOSED this as a hiprtc codegen miscompile): that exact triad -- load-correlated + instrumentation-sensitive + math-identical -- is the classic signature of UNDEFINED BEHAVIOR (a missing/implicit return in a value-returning function, or an uninitialized variable), NOT a compiler bug. Here the actual root cause was TWO value-returning __device__ functions that fall off the end without a return (auxiliary.h quat_to_rotmat_transpose declared float3, kernels.h compute_transmat_xy_backward declared bool) -- latent UB inherited verbatim from the OptiX source that nvcc tolerated (callers discard the return) but hipcc/hiprtc does not. quat_to_rotmat_transpose feeds R[2] = the surfel normal into compute_transmat_uv; the UB poisons the normal, NaNs dL_dalpha, and worsens with hit count (more contributors = more poisoned terms). Inline -> a probe perturbs inlining and masks it (the heisenbug). FIX: return void (callers discard the result); unconditional, since it is UB on CUDA too. The reason it is invisible: the HIPRT JIT (hiprtBuildTraceKernels) does not pass -w but also does not surface its own warnings -- so ALWAYS syntax-check the JIT translation unit yourself with `clang++ --offload-arch=gfx90a -x hip -fsyntax-only -Werror=return-type -Wall -Wextra kernels.h -I... -D'__trap()=__builtin_trap()'` BEFORE blaming codegen; it flags every missing return in the TU. Before filing a ROCm compiler bug, RULE OUT UB: audit every value-returning device function for a fall-off-the-end path and every local for use-before-init. (The chunk-buffer-in-global and cutoff-init fixes above ARE genuine and still needed; the __noinline__/threadfence/launch_bounds/mid_val-from-global tweaks were applied while chasing this non-bug and merely perturb inlining around the UB -- they are not the fix.) EnvGS Stage 2 now PORTED: geometric backward finite + FD-correct on gfx90a (validate_stage2.py + validate_geom_fd.py), jeffdaily/diff-surfel-tracing @ moat-port 5991683, EnvGS @ moat-port 2890415. EnvGS
- 2026-06-02 -- multi-arch HIP fat binary + a kernel templated on the HARDWARE warp width: the host pass is compiled ONCE, so a single host compile-time WarpSize constant cannot match both device arches -> the host launches an instantiation the device never compiled ("Cannot find Symbol ...Lm64E" / SIGABRT on the follower arch, e.g. wave32 gfx1100 while wave64 gfx90a is silently fine because host64==device64). Distinguish the TWO needs: host launch-GEOMETRY ints (grid/block dims) -> a runtime warp_size() query (hipDeviceGetAttribute, cached) is right; a COMPILE-TIME template arg that selects a kernel symbol -> a runtime value cannot be a template arg, and the host constant breaks the fat binary. Fix the template-arg site by DECOUPLING it from the hardware warp: audit whether it truly needs the wavefront width or just a fixed vectorization/tile width (raft linewise_op MaxOffset was only the tail-kernel block width, needing >= VecBytes and warp-aligned, NOT the hardware warp), and use a fixed compile-time constant (e.g. 32 -- warp-aligned on both wave32 and wave64, and = the CUDA WarpSize so host and every device pass select ONE identical instantiation). Alternative: instantiate BOTH widths device-side + dispatch at runtime via the warp-size query. Audit ALL host-pass `constexpr ...WarpSize` template-arg and `<<<...WarpSize...>>>` launch sites; the bug fingerprint is "passes on the lead arch, SIGABRT only on the follower" -- raft
- 2026-06-02 -- compat-shim keying for an `#include`d header MUST test `__HIP__`, not just `__HIP_PLATFORM_AMD__`: the Strategy-A idiom is one `cuda_to_hip.h` that, under the AMD branch, pulls the HIP runtime + aliases the cuda*/cufft* symbols, and under `#else` pulls the original CUDA headers so the NVIDIA build stays byte-identical. The trap: `hipcc -x hip` defines `__HIP__` at PREPROCESS time but does NOT define `__HIP_PLATFORM_AMD__` until <hip/hip_runtime.h> is included -- so a shim that keys its `#if` only on `__HIP_PLATFORM_AMD__` (or `USE_HIP`) silently takes the `#else` branch and tries to `#include <cuda_runtime.h>`/`<cufft.h>` on an AMD box, failing with "cuda_runtime.h: No such file" (or, worse, finding a stale CUDA header). Verify with `hipcc -x hip -dM -E empty.cu | grep __HIP` -- you will see `__HIP__ 1` and no `__HIP_PLATFORM_AMD__`. Key the branch `#if defined(__HIP__) || defined(__HIP_PLATFORM_AMD__) || defined(USE_HIP)` so it fires both at the hipcc preprocess gate AND for any TU that already has the HIP runtime in scope. (Distinct from the gtsam_points `__HIPCC__` note, which is about a FORCE-INCLUDE'd thrust exec-policy header in a mixed g++/clang build; this is about an ordinary `#include`d cuFFT/runtime alias shim.) Keep libc (`<cstdlib>/<cstring>/<cmath>`) ABOVE the HIP runtime include in the same shim so host calloc/free/round do not bind to HIP __device__ overloads (gpuRIR lesson). -- 3P-ADMM-PC2 (cuFFT->hipFFT Paillier modexp; 6144/6144 exact vs gmpy2 on gfx90a)
- 2026-06-02 -- hipCUB/rocPRIM DeviceScan (and likely DeviceRadixSort/DeviceReduce) temp storage SUB-ALLOCATED from a shared/packed buffer must be ALIGNED (>=256B). catboost split_points.cu SortWithoutCub (one-bit reorder for tree splits) packed the cub::DeviceScan working temp immediately after an int scan output, at offset 4*part.Size (only 4-byte aligned); rocPRIM keeps lookback tile-state in its temp and requires alignment, so a misaligned temp corrupts the exclusive scan NON-DETERMINISTICALLY at tile boundaries (CUB tolerates the misalignment, rocPRIM does not). Symptom: wrong one-bit-reorder => wrong post-split doc indices (docs that land nowhere read back 0), FLAKY run-to-run, surfacing as a downstream GPU-vs-CPU index mismatch (catboost: TPointwiseMultiStatHistogramTest CheckAndMakeSplit, cpu 37888 vs gpu 0, first mismatch at scan tile boundaries). Fix: round the sub-allocated scan-temp OFFSET up to 256 (USE_HIP; CUDA path unchanged). Isolate WITHOUT a full rebuild via a standalone hipcub reproducer that toggles shared-vs-separate temp and alignment: separate hipMalloc temp = always correct, shared 4*N-offset = non-deterministic, 256-aligned offset = correct -- proves it is alignment, not the custom iterator or the algorithm. Audit every site that carves a rocPRIM device-primitive temp out of a shared buffer (sort.cpp/segmented_sort.cpp temp packing). Related: each crash fix here unmasked the next masked correctness layer, so re-run the FULL ut suite after every fix -- catboost
