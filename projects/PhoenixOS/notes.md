# PhoenixOS notes

Maintainers advertise a "ROCm-developing" track (README); high-leverage upstream even though no ROCm code exists yet. Driver-hooking C/R reimplemented against HIP/ROCr + amdgpu.

Maintainers advertise a ROCm-developing track (high-leverage; coordinate). C/R must target HIP/ROCr + amdgpu instead of the CUDA driver.
